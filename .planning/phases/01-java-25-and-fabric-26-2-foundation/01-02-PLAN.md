---
phase: 01-java-25-and-fabric-26-2-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - build.gradle
  - src/main/java/dev/developershell/DevelopersHell.java
  - src/main/java/dev/developershell/registry/ModItemIds.java
  - src/main/java/dev/developershell/registry/ModItems.java
  - src/main/java/dev/developershell/module/ModuleId.java
  - src/main/java/dev/developershell/module/ModuleGate.java
  - src/test/java/dev/developershell/module/ModuleGateTest.java
  - src/main/resources/assets/developers_hell/lang/en_us.json
autonomous: true
requirements: [FND-02, FND-04]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Foundation Token moves from the compact tracer into dedicated unconditional registry/catalog classes without changing its public ID or packaged client resource path."
    - "Exactly eight anthology module keys share one immutable common-side behavior gate; all-enabled, all-disabled, and explicit gates leave the stable content catalog identical."
    - "The wrapper's test task uses Loader JUnit 0.19.3 only as a test dependency and proves null, defensive-copy, immutability, name uniqueness, and catalog invariants."
  artifacts:
    - path: "src/main/java/dev/developershell/registry/ModItemIds.java"
      provides: "Immutable stable item-key catalog"
      exports: ["ModItemIds", "FOUNDATION_TOKEN", "all"]
    - path: "src/main/java/dev/developershell/registry/ModItems.java"
      provides: "Unconditional item registration provider"
      exports: ["ModItems", "FOUNDATION_TOKEN", "initialize"]
    - path: "src/main/java/dev/developershell/module/ModuleId.java"
      provides: "Eight exact stable behavior-module keys"
      exports: ["ModuleId", "serializedName"]
    - path: "src/main/java/dev/developershell/module/ModuleGate.java"
      provides: "Immutable behavior-only gate"
      exports: ["allEnabled", "allDisabled", "of", "isEnabled", "enabledModules"]
    - path: "src/test/java/dev/developershell/module/ModuleGateTest.java"
      provides: "Fail-first pure gate and catalog invariant tests"
  key_links:
    - from: "DevelopersHell.onInitialize"
      to: "ModItems.initialize"
      via: "unconditional call before any behavior hook"
      pattern: "ModItems\\.initialize"
    - from: "ModItems.FOUNDATION_TOKEN"
      to: "ModItemIds.FOUNDATION_TOKEN"
      via: "current 26.2 Registry.register path"
      pattern: "FOUNDATION_TOKEN"
    - from: "ModuleGateTest"
      to: "ModItemIds.all"
      via: "catalog equality across every gate state"
      pattern: "ModItemIds\\.all"
  flagged_assumptions:
    - "[UNRESOLVED][verification=null][FND-04][adjacency] Probe: When two things are exactly equal or just touch, do they merge, collide, or separate? No registry-specific resolution was supplied."
    - "[UNRESOLVED][verification=null][FND-04][empty] Probe: What is the result for empty, single-element, or null input? No concrete gate/catalog shape was classified."
    - "[UNRESOLVED][verification=null][FND-04][ordering] Probe: When elements compare equal, is output order specified and stable? No registry/catalog ordering resolution was supplied."
    - "[UNRESOLVED][verification=null][FND-04][idempotency] Probe: What happens if this runs twice on the same input? No repeated-initialization/gate resolution was supplied."
    - "[UNRESOLVED][verification=null][FND-04][concurrency] Probe: If interrupted or run in parallel, what is guaranteed? No concurrency contract was supplied."
  prohibitions: []
---

# Phase 1 Plan 02 — Stable Registry Extraction and Behavior-Gate Unit Proof

## Phase Goal

**As a** player or contributor, **I want to** install, launch, and rebuild one frozen Fabric 26.2 JAR on client and dedicated server, **so that** Developer's Hell remains playable offline after its official dependencies are primed.

<objective>
Expand the Plan 01 tracer into the final cross-phase registry and module-gate interfaces under a fail-first Loader JUnit test.

Purpose: Make the save-safety rule executable: behavior toggles can change later mechanics but cannot alter registered identity.
Output: Dedicated item catalog/provider, eight-key immutable behavior gate, localized Foundation Token name, and focused unit proof wired into wrapper `test`.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.agents/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-java-25-and-fabric-26-2-foundation/01-CONTEXT.md
@.planning/phases/01-java-25-and-fabric-26-2-foundation/01-RESEARCH.md
@.planning/phases/01-java-25-and-fabric-26-2-foundation/01-PATTERNS.md
@.planning/phases/01-java-25-and-fabric-26-2-foundation/01-VALIDATION.md
@.planning/phases/01-java-25-and-fabric-26-2-foundation/01-01-SUMMARY.md

<interfaces>
- Consume Plan 01's exact `DevelopersHell.MOD_ID`, `id(String)`, `FOUNDATION_TOKEN_ID`, `FOUNDATION_TOKEN`, and unconditional common initialization behavior.
- Preserve the public key `developers_hell:foundation_token` and the already packaged `items/foundation_token.json` → `models/item/foundation_token.json` path.
- `ModuleId` enumerates exactly `GRADUATION_ANYFAIL`, `METADATA_ROULETTE`, `PYTHON_TOOLS`, `CODEX_RICH_KID_TERMINAL`, `GIT_HAPPENS`, `STACK_OVERFLOW_TOTEM`, `RUBBER_DUCK_ENGINEERING`, and `THREE_DAY_DEADLINE` with unique snake-case serialized names.
- `ModuleGate` exposes `allEnabled()`, `allDisabled()`, `of(Set<ModuleId>)`, `isEnabled(ModuleId)`, and immutable `enabledModules()`; it cannot register content.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract the stable registry and lock the eight-module behavior gate under unit test</name>
  <reversibility rating="costly">The registry classes, module keys, and gate methods become the save/configuration/behavior contract consumed by every later campaign and sandbox phase.</reversibility>
  <read_first>
    - `build.gradle` — preserve exact tuple and direct runtime dependencies; add only test-scoped Loader JUnit and JUnit Platform wiring.
    - `src/main/java/dev/developershell/DevelopersHell.java` — extract its inline tracer registration without changing ID or initialization order.
    - `src/main/java/dev/developershell/registry/ModItemIds.java`, `src/main/java/dev/developershell/registry/ModItems.java`, `src/main/java/dev/developershell/module/ModuleId.java`, `src/main/java/dev/developershell/module/ModuleGate.java`, `src/test/java/dev/developershell/module/ModuleGateTest.java` — create if absent; preserve any compatible Plan 01 facts.
    - `src/main/resources/assets/developers_hell/lang/en_us.json` — add only the Phase 1 Foundation Token translation.
    - `.planning/phases/01-java-25-and-fabric-26-2-foundation/01-CONTEXT.md` — unconditional stable-registration and no-extra-runtime-library decisions.
    - `.planning/phases/01-java-25-and-fabric-26-2-foundation/01-RESEARCH.md`, `01-PATTERNS.md`, and `.planning/REQUIREMENTS.md` — exact registry pattern, ModuleGate no-analog contract, eight module names, and FND-04.
    - `.planning/phases/01-java-25-and-fabric-26-2-foundation/01-01-SUMMARY.md` — actual tracer interface and resolved Loom result.
    - `.planning/phases/01-java-25-and-fabric-26-2-foundation/01-TOOLCHAIN-EVIDENCE.md` — exact Temurin archive/runtime/binary hashes and redacted path hash that every wrapper run must reselect.
  </read_first>
  <files>build.gradle, src/main/java/dev/developershell/DevelopersHell.java, src/main/java/dev/developershell/registry/ModItemIds.java, src/main/java/dev/developershell/registry/ModItems.java, src/main/java/dev/developershell/module/ModuleId.java, src/main/java/dev/developershell/module/ModuleGate.java, src/test/java/dev/developershell/module/ModuleGateTest.java, src/main/resources/assets/developers_hell/lang/en_us.json</files>
  <behavior>
    - Every exact module is disabled by `allDisabled()` and enabled by `allEnabled()`.
    - A one-module input enables only that module; `of` defensively copies, rejects null set/member, and exposes no mutable view.
    - All eight serialized names are unique and match the locked snake-case names.
    - `ModItemIds.all()` contains only the unchanged Foundation Token key and is equal before/after every gate construction/query.
    - `DevelopersHell.onInitialize()` calls `ModItems.initialize()` unconditionally before any later behavior hook.
  </behavior>
  <action>
    Re-select `.work/toolchain/temurin-25.0.4+7-x64`, require its Java/Javac binary and canonical-path hashes to match `01-TOOLCHAIN-EVIDENCE.md`, set task-local `JAVA_HOME`/`PATH`, disable Gradle toolchain auto-detection/download, and use that exact root for every wrapper/test process. Add `net.fabricmc:fabric-loader-junit:0.19.3` as `testImplementation` and enable JUnit Platform; do not add it to any runtime configuration.

    Before writing or running `ModuleGateTest`, create compiling, signature-complete production skeletons at their final paths. `ModuleId` must already expose the final eight enum constants and `serializedName()`; `ModuleGate` must already expose `allEnabled()`, `allDisabled()`, `of(Set<ModuleId>)`, `isEnabled(ModuleId)`, and `enabledModules()`; `ModItemIds` must already expose `FOUNDATION_TOKEN` and `all()`; `ModItems` must already expose `FOUNDATION_TOKEN` and `initialize()`; and `DevelopersHell.onInitialize()` must call `ModItems.initialize()` unconditionally. Move the unchanged Foundation Token key/item and 26.2 registration helper into those registry types now, with `ModItemIds.all()` containing exactly that key. No registry class may import, accept, or query `ModuleGate`; add no future boss/effect/payload/command IDs. Make only one behavior deliberately wrong for RED: implement `ModuleGate.allDisabled()` by delegating to `allEnabled()`, so it compiles but reports `GRADUATION_ANYFAIL` enabled. While the test path is still absent, delete `.work/module-gate-skeleton-compile.log`, run `compileJava --rerun-tasks --no-daemon` with the same Loom probe/exact JDK, capture its fresh stream/numeric zero exit, and require its configured/resolved/artifact-SHA tuple to match toolchain evidence; only this successful production compile authorizes creation of the test.

    Write the final `ModuleGateTest` after those production signatures compile. Its first focused method is exactly `allDisabledDisablesEveryModule`, containing the unchanged assertion `assertFalse(ModuleGate.allDisabled().isEnabled(ModuleId.GRADUATION_ANYFAIL), "allDisabled must disable graduation_anyfail")`; also add the final all-on/off/one, exact serialized-name, null, defensive-copy, immutable-view, and stable-catalog cases, but do not edit any test after recording RED. Before RED, delete `.work/module-gate-red.log`, `.work/module-gate-red-junit.xml`, `.work/module-gate-tdd-evidence.json`, and the current `build/test-results/test/TEST-dev.developershell.module.ModuleGateTest.xml`. Run exactly the focused method with `--tests '*ModuleGateTest.allDisabledDisablesEveryModule' --rerun-tasks --info --console=plain`, `--no-daemon`, and the retained Loom probe; capture that invocation's combined stream and numeric exit. Require the exact `:test` task to execute and fail, reject only `> Task :test UP-TO-DATE`, `> Task :test NO-SOURCE`, or `> Task :test SKIPPED` for skip detection, and explicitly allow unrelated tasks such as `> Task :processTestResources NO-SOURCE`. Reject resolution/setup/compile/network/transient failure or no-tests-found. Require a newly written JUnit XML naming `ModuleGateTest` and the exact target method (allowing Gradle's optional trailing `()` display-name suffix), with one assertion failure whose message proves expected `false` but was `true` for `allDisabled must disable graduation_anyfail`. Copy that fresh XML to ignored `.work/module-gate-red-junit.xml`. From the same RED stream derive configured Loom spelling, concrete resolved build, and artifact SHA-256 and require exact equality to toolchain evidence. Write an ignored receipt binding the exact command, numeric nonzero exit, RED log/XML SHA-256 values, unchanged final test-file SHA-256, test/method name, deliberate `allDisabled_returns_all_modules` failure category, and start/completion timestamps.

    After that captured assertion RED, correct only the deliberate behavior in the same production file: `ModuleGate.allDisabled()` must construct an empty enabled set. Complete the immutable pure gate semantics with no Fabric registry, physical-client, filesystem, network, clock, random, environment, or configuration-parser dependency. Before GREEN, delete `.work/module-gate-green.log` and the current JUnit XML; run `--tests '*ModuleGateTest' --rerun-tasks --info --console=plain` with the same Loom probe, capture the exact combined stream/numeric zero exit, require exact `:test` execution while rejecting only `:test`-scoped UP-TO-DATE/NO-SOURCE/SKIPPED results, and require a fresh JUnit suite naming `ModuleGateTest` with the target method present and all cases passing. Independently compare GREEN configured/resolved/artifact-SHA markers to toolchain evidence. Refactor without changing the public/test contract and repeat this GREEN proof if production/test code changes. Add only `item.developers_hell.foundation_token = Foundation Token` to `en_us.json`; retain Plan 01's two item/model JSONs. Finally delete the full-build log, run repository `build` with the same probe, capture its fresh stream/exit, and require the same exact Loom equality. GameTest is not expected until Plan 03.
  </action>
  <acceptance_criteria>
    - Loader JUnit is exact `0.19.3` and test-only. Before tests exist, every final production signature compiles; the sole deliberate RED defect is `allDisabled()` returning all modules, and the unchanged `allDisabledDisablesEveryModule` assertion fails specifically because `GRADUATION_ANYFAIL` is `true` when `false` is expected.
    - Dedicated RED/GREEN logs and JUnit results are freshly overwritten. RED log/XML/test hashes and exact command/exit/category/timestamps are bound to the ignored receipt; GREEN emits fresh zero-failure XML containing the target method. Only exact `:test` UP-TO-DATE/NO-SOURCE/SKIPPED markers are rejected, so unrelated `:processTestResources NO-SOURCE` output is allowed.
    - Every RED/GREEN/build wrapper process uses the checksum-bound Eclipse Temurin `25.0.4+7` root retained by Plan 01; no other installed JDK or auto-downloaded toolchain is accepted.
    - Each focused RED invocation, each focused GREEN/refactor invocation, and the later full build independently derives selected Loom, concrete resolved build, and `resolved_loom_sha256` from its own stream and exactly matches evidence; no stale/shared-cache marker can pass.
    - The extracted registry still exposes and unconditionally registers exact `developers_hell:foundation_token`; `DevelopersHell` no longer owns inline registration.
    - Gate/name/null/copy/immutability/catalog cases all pass, and registry classes have no gate dependency.
    - The translation is packaged alongside the unchanged current 26.2 item/model resources.
    - Repository `build` passes without claiming GameTest coverage before Plan 03.
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'
$jdkRoot = (Resolve-Path -LiteralPath '.work/toolchain/temurin-25.0.4+7-x64').Path; $env:JAVA_HOME = $jdkRoot; $env:Path = "$(Join-Path $jdkRoot 'bin');$env:Path"; $jvmArgs = @("-Dorg.gradle.java.installations.paths=$jdkRoot", '-Dorg.gradle.java.installations.auto-detect=false', '-Dorg.gradle.java.installations.auto-download=false'); $evidence = Get-Content -LiteralPath '.planning/phases/01-java-25-and-fabric-26-2-foundation/01-TOOLCHAIN-EVIDENCE.md' -Raw; foreach ($tool in @('java','javac')) { $actual = (Get-FileHash -LiteralPath (Join-Path $jdkRoot "bin/$tool.exe") -Algorithm SHA256).Hash; $expected = [regex]::Match($evidence, '(?m)^jdk_' + $tool + '_sha256:\s*([0-9a-fA-F]{64})\s*$'); if (-not $expected.Success -or $actual -ne $expected.Groups[1].Value) { throw "Verified $tool binary mismatch" } }; $sha = [Security.Cryptography.SHA256]::Create(); try { $pathHash = ([BitConverter]::ToString($sha.ComputeHash([Text.Encoding]::UTF8.GetBytes($jdkRoot.ToLowerInvariant())))).Replace('-','').ToLowerInvariant() } finally { $sha.Dispose() }; $pathMarker = [regex]::Match($evidence, '(?m)^jdk_path_sha256:\s*([0-9a-f]{64})\s*$'); if (-not $pathMarker.Success -or $pathHash -ne $pathMarker.Groups[1].Value) { throw 'Verified JDK canonical path hash mismatch' }
$loomProbe = '.work/loom-resolution.init.gradle'; if (-not (Test-Path -LiteralPath $loomProbe -PathType Leaf)) { throw 'Retained Loom probe missing' }; function Evidence([string] $Name) { $m = [regex]::Match($evidence, '(?m)^' + [regex]::Escape($Name) + ':\s*(.+?)\s*$'); if (-not $m.Success) { throw "Evidence marker missing: $Name" }; $m.Groups[1].Value.Trim() }; $configured = [regex]::Match((Get-Content -LiteralPath 'gradle.properties' -Raw), '(?m)^loom_version=(.+?)\s*$').Groups[1].Value.Trim(); function Probe([string] $Text, [string] $Name) { $m = [regex]::Match($Text, '(?m)^DEVELOPERS_HELL_' + [regex]::Escape($Name) + '=(.+?)\s*$'); if (-not $m.Success) { throw "Loom marker missing: $Name" }; $m.Groups[1].Value.Trim() }; function Assert-Loom([string] $Text, [string] $Label) { if ((Probe $Text 'LOOM_SELECTED') -ne $configured -or (Probe $Text 'LOOM_SELECTED') -ne (Evidence 'loom_selected') -or (Probe $Text 'LOOM_RESOLVED') -ne (Evidence 'resolved_loom_build') -or (Probe $Text 'LOOM_ARTIFACT_SHA256') -ne (Evidence 'resolved_loom_sha256')) { throw "$Label did not resolve the frozen Loom artifact" }; if ($configured -eq '1.17-SNAPSHOT' -and (Probe $Text 'LOOM_RESOLVED') -eq '1.17-SNAPSHOT') { throw "$Label retained an unresolved snapshot alias" } }
$testSkipPattern = '(?m)^\s*> Task :test (?:UP-TO-DATE|NO-SOURCE|SKIPPED)\s*$'; $redLog = '.work/module-gate-red.log'; $redXmlPath = '.work/module-gate-red-junit.xml'; $redReceiptPath = '.work/module-gate-tdd-evidence.json'; if (-not (Test-Path -LiteralPath $redLog -PathType Leaf) -or -not (Test-Path -LiteralPath $redXmlPath -PathType Leaf) -or -not (Test-Path -LiteralPath $redReceiptPath -PathType Leaf)) { throw 'Fresh RED evidence is missing' }; $redReceipt = Get-Content -LiteralPath $redReceiptPath -Raw | ConvertFrom-Json; $redText = Get-Content -LiteralPath $redLog -Raw; $redXmlText = Get-Content -LiteralPath $redXmlPath -Raw; [xml] $redResult = $redXmlText; $redSuite = $redResult.testsuite; $redCases = @($redSuite.testcase); $redHash = (Get-FileHash -LiteralPath $redLog -Algorithm SHA256).Hash.ToLowerInvariant(); $redXmlHash = (Get-FileHash -LiteralPath $redXmlPath -Algorithm SHA256).Hash.ToLowerInvariant(); $testHash = (Get-FileHash -LiteralPath 'src/test/java/dev/developershell/module/ModuleGateTest.java' -Algorithm SHA256).Hash.ToLowerInvariant(); $redStarted = [DateTime]::Parse([string] $redReceipt.started_utc).ToUniversalTime(); $redCompleted = [DateTime]::Parse([string] $redReceipt.completed_utc).ToUniversalTime(); $redTargetPresent = @($redCases | Where-Object { [string] $_.name -match '^allDisabledDisablesEveryModule(?:\(\))?$' }).Count -eq 1; if ($redCompleted -le $redStarted -or [int] $redReceipt.exit_code -eq 0 -or $redReceipt.failure_kind -ne 'allDisabled_returns_all_modules' -or $redReceipt.test_name -ne 'allDisabledDisablesEveryModule' -or ([string] $redReceipt.command -notmatch "--tests '\*ModuleGateTest[.]allDisabledDisablesEveryModule' --rerun-tasks --info --console=plain") -or ([string] $redReceipt.log_sha256).ToLowerInvariant() -ne $redHash -or ([string] $redReceipt.junit_sha256).ToLowerInvariant() -ne $redXmlHash -or ([string] $redReceipt.test_file_sha256).ToLowerInvariant() -ne $testHash -or $redText -match $testSkipPattern -or $redText -match '(?i)No tests found|plugin resolution|minecraft setup|compilation failed|unknown host|connection reset|timed out' -or $redText -notmatch '(?m)^\s*> Task :test FAILED\s*$' -or [int] $redSuite.tests -ne 1 -or [int] $redSuite.failures -ne 1 -or [int] $redSuite.errors -ne 0 -or [string] $redSuite.name -notmatch 'ModuleGateTest' -or -not $redTargetPresent -or $redXmlText -notmatch '(?is)allDisabled must disable graduation_anyfail.*expected:.*false.*but was:.*true') { throw 'RED evidence is stale, skipped, non-executed, non-compiling, or not the deliberate allDisabled assertion failure' }; Assert-Loom $redText 'Focused RED'
$greenLog = '.work/module-gate-green.log'; $resultPath = 'build/test-results/test/TEST-dev.developershell.module.ModuleGateTest.xml'; Remove-Item -LiteralPath $greenLog,$resultPath -Force -ErrorAction SilentlyContinue; $greenStarted = [DateTime]::UtcNow; $greenLines = @(& .\gradlew.bat @jvmArgs test --tests '*ModuleGateTest' --rerun-tasks --info --console=plain --no-daemon --init-script $loomProbe 2>&1); $greenExit = $LASTEXITCODE; [IO.File]::WriteAllLines([IO.Path]::GetFullPath($greenLog), [string[]] $greenLines); $greenText = $greenLines -join "`n"; if ($greenExit -ne 0 -or $greenText -match $testSkipPattern -or $greenText -match '(?i)No tests found' -or $greenText -notmatch '(?m)^\s*> Task :test\s*$' -or -not (Test-Path -LiteralPath $resultPath -PathType Leaf) -or (Get-Item -LiteralPath $resultPath).LastWriteTimeUtc -lt $greenStarted) { throw "Focused GREEN test was not freshly executed; exit $greenExit" }; [xml] $result = Get-Content -LiteralPath $resultPath -Raw; $suite = $result.testsuite; $greenCases = @($suite.testcase); $greenTargetPresent = @($greenCases | Where-Object { [string] $_.name -match '^allDisabledDisablesEveryModule(?:\(\))?$' }).Count -eq 1; if ([int] $suite.tests -le 0 -or [int] $suite.failures -ne 0 -or [int] $suite.errors -ne 0 -or [string] $suite.name -notmatch 'ModuleGateTest' -or -not $greenTargetPresent) { throw 'Focused GREEN JUnit result does not prove ModuleGateTest and its RED target passed' }; Assert-Loom $greenText 'Focused GREEN'
$buildLog = '.work/unit-build-loom.log'; Remove-Item -LiteralPath $buildLog -Force -ErrorAction SilentlyContinue; $buildLines = @(& .\gradlew.bat @jvmArgs build --no-daemon --init-script $loomProbe 2>&1); $buildExit = $LASTEXITCODE; [IO.File]::WriteAllLines([IO.Path]::GetFullPath($buildLog), [string[]] $buildLines); if ($buildExit -ne 0) { throw "Repository build failed with exit $buildExit" }; Assert-Loom ($buildLines -join "`n") 'Full build'
$bad = rg -n 'ModuleGate' src/main/java/dev/developershell/registry; if ($LASTEXITCODE -eq 0) { throw "Registry depends on gate: $bad" }; if ($LASTEXITCODE -gt 1) { throw 'Registry audit failed' }
$jar = 'build/libs/developers-hell-0.1.0.jar'; if (-not (Test-Path -LiteralPath $jar)) { throw 'Ordinary production JAR missing' }; $entries = @(& jar --list --file $jar); if ($LASTEXITCODE -ne 0) { throw "JAR listing failed with exit $LASTEXITCODE" }; foreach ($entry in @('LICENSE_developers-hell','dev/developershell/registry/ModItemIds.class','dev/developershell/registry/ModItems.class','dev/developershell/module/ModuleId.class','dev/developershell/module/ModuleGate.class','assets/developers_hell/lang/en_us.json')) { if ($entries -notcontains $entry) { throw "Missing expanded artifact entry: $entry" } }</automated>
  </verify>
  <done>The compact tracer is expanded into final unconditional registry and eight-module behavior-gate interfaces, with Loader JUnit proving the stable catalog cannot vary with gate state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Module gate → registry/behavior | Local toggle state may select behavior but must never change registered identity. |
| Test dependency → production runtime | Loader JUnit is trusted for tests but must not leak into the shipped/runtime dependency surface. |
| Tracer extraction → saved public ID | Refactoring must retain the exact key and unconditional initialization order. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-07 | Tampering | ModuleGate and registry catalog | high | mitigate | Pure immutable gate, no registry dependency, exhaustive gate/catalog unit test. |
| T-01-08 | Denial of Service | stable registry refactor | high | mitigate | Preserve exact key, unconditional initializer call, build/archive proof, and live GameTest in Plan 03. |
| T-01-09 | Tampering | direct dependency scopes | high | mitigate | Add exact Loader JUnit only to testImplementation; Plan 03 mechanically audits every direct declaration. |
</threat_model>

## Artifacts this phase produces

- `build.gradle` — exact test-scoped Loader JUnit 0.19.3 and JUnit Platform wiring.
- `DevelopersHell.onInitialize()` — unconditional `ModItems.initialize()` call.
- `ModItemIds` — `FOUNDATION_TOKEN`, immutable `all()` catalog.
- `ModItems` — `FOUNDATION_TOKEN`, current registration helper, `initialize()`.
- `ModuleId` — eight exact constants and `serializedName()`.
- `ModuleGate` — `allEnabled()`, `allDisabled()`, `of(...)`, `isEnabled(...)`, `enabledModules()`.
- `ModuleGateTest` — fail-first gate/name/null/copy/immutability/catalog cases.
- `en_us.json` — `item.developers_hell.foundation_token` translation.

<verification>
- Run the focused Loader JUnit test, complete repository build, registry/gate dependency audit, and ordinary-JAR entry assertions.
- Preserve the deliberate unit RED and restored GREEN evidence in `01-02-SUMMARY.md`.
</verification>

<success_criteria>
- Toggle policy is incapable of changing stable content identity.
- The exact Foundation Token survives extraction and is localized without changing its Plan 01 client-resource path.
- No production runtime dependency is added by the unit-test layer.
</success_criteria>

<output>Create `.planning/phases/01-java-25-and-fabric-26-2-foundation/01-02-SUMMARY.md` when done.</output>
