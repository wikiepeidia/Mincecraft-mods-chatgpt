---
phase: 01-java-25-and-fabric-26-2-foundation
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - README.md
  - .planning/phases/01-java-25-and-fabric-26-2-foundation/01-FOUNDATION-EVIDENCE.md
autonomous: false
requirements: [FND-01, FND-02, FND-03]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A player can install the one ordinary Developer's Hell JAR beside exact Fabric 26.2 prerequisites and enter, save, and leave a singleplayer world from a primed production profile while operating-system network isolation is verified."
    - "A contributor using the checksum-bound Eclipse Temurin 25.0.4+7 runtime can reproduce the same ordinary production JAR hash from a canonically guarded detached clean checkout of committed HEAD using its tracked Wrapper 9.5.1, first online and then from the same cache offline."
    - "The one verified distribution JAR reaches ready state and stops cleanly on a production dedicated server online and under verified operating-system network isolation without client-only linkage or runtime network behavior."
    - "Final evidence repeats the comprehensive source, dependency, archive, repository-hygiene, and no-network audit and records exact provenance, commands, exits, hashes, profiles, isolation method, and UAT outcome."
  artifacts:
    - path: "scripts/verify-foundation.ps1"
      provides: "Fail-closed tuple, provenance, comprehensive audit, archive, reproducibility, production-server, isolation, and client-preflight harness"
      exports: ["SelfCheck", "PrimeAndCompare", "RunServerSmoke", "ClientPreflight", "SuperviseInteractiveUat", "ValidateEvidence", "RequireUatPass", "EvidencePath", "SessionDirectory", "SessionPointerPath", "SessionReceiptPath", "DistributionPath"]
    - path: "README.md"
      provides: "Exact player installation and contributor online-prime/same-cache-offline instructions"
    - path: ".planning/phases/01-java-25-and-fabric-26-2-foundation/01-FOUNDATION-EVIDENCE.md"
      provides: "Machine-checkable clean-checkout/tracked-manifest/artifact/runtime evidence plus the bounded human client-world result"
  key_links:
    - from: "scripts/verify-foundation.ps1"
      to: "scripts/audit-foundation.ps1"
      via: "mandatory first and final comprehensive source/dependency/archive audit"
      pattern: "audit-foundation[.]ps1"
    - from: "committed HEAD"
      to: "disposable detached Developer's Hell worktree"
      via: "tracked manifest plus committed-wrapper online/offline artifact proof"
      pattern: "git worktree add --detach"
    - from: "scripts/verify-foundation.ps1"
      to: ".planning/phases/01-java-25-and-fabric-26-2-foundation/01-TOOLCHAIN-EVIDENCE.md"
      via: "exact provenance, tuple, command, exit, and Loom-outcome marker validation"
      pattern: "template_commit|loom_selected|fixed_build_exit"
    - from: "clean online/offline ordinary JAR"
      to: "dist/developers-hell-0.1.0.jar"
      via: "copy before worktree removal and three-way SHA-256 equality"
      pattern: "online_jar_sha256|offline_jar_sha256|distribution_sha256"
    - from: "verified two-rule Java-process network block"
      to: "offline production server and client UAT"
      via: "unique group, exact java/javaw rules, reachable/blocked probe, and finally cleanup"
      pattern: "isolation_group_id|java_rule_id|javaw_rule_id"
  flagged_assumptions:
    - "[UNRESOLVED][verification=null][FND-01][unclassified] Probe: unclassified — review manually. Retain this row while the explicit production installation, world-entry, save/quit, and offline UAT contract supplies the actual phase proof."
  prohibitions: []
---

# Phase 1 Plan 04 — Installable Offline Artifact and Human World Proof

## Phase Goal

**As a** player or contributor, **I want to** install, launch, and rebuild one frozen Fabric 26.2 JAR on client and dedicated server, **so that** Developer's Hell remains playable offline after its official dependencies are primed.

<objective>
Produce fail-closed artifact/runtime evidence automatically, pause at one observation-only blocking-human checkpoint for the client-world interaction that automation cannot truthfully assert, then machine-finalize the reported observations.

Purpose: Convert the tested walking skeleton into a reproducible, offline-installable handoff without confusing Gradle cache mode with runtime network isolation.
Output: Execution of Plan 03's committed verification harness, exact install/build documentation, automated evidence, a human-confirmed online/offline production-client world result, and a validated public-safe final evidence commit.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-java-25-and-fabric-26-2-foundation/01-CONTEXT.md
@.planning/phases/01-java-25-and-fabric-26-2-foundation/01-RESEARCH.md
@.planning/phases/01-java-25-and-fabric-26-2-foundation/01-PATTERNS.md
@.planning/phases/01-java-25-and-fabric-26-2-foundation/01-VALIDATION.md
@.planning/phases/01-java-25-and-fabric-26-2-foundation/01-01-SUMMARY.md
@.planning/phases/01-java-25-and-fabric-26-2-foundation/01-02-SUMMARY.md
@.planning/phases/01-java-25-and-fabric-26-2-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove the ordinary JAR, same-cache rebuild, comprehensive offline surface, and both production sides</name>
  <reversibility rating="reversible">The harness, evidence schema, and documentation can evolve without changing the frozen public mod/content identity.</reversibility>
  <read_first>
    - `scripts/verify-foundation.ps1` — immutable, committed Plan 03 prerequisite; verify it exists at `HEAD` and has no working-tree diff before executing it.
    - `README.md`, `.planning/phases/01-java-25-and-fabric-26-2-foundation/01-FOUNDATION-EVIDENCE.md` — create or replace only with results backed by this run.
    - `scripts/audit-foundation.ps1` — Plan 03's authoritative comprehensive `src/main` plus `src/client`, direct-dependency, archive, and repository-hygiene audit; invoke it rather than duplicating a narrower grep.
    - `.planning/phases/01-java-25-and-fabric-26-2-foundation/01-TOOLCHAIN-EVIDENCE.md` — exact official provenance, fixed/fallback Loom result, tuple, commands, exits, and wrapper integrity that must be parsed, not merely checked for existence.
    - `settings.gradle`, `gradle.properties`, `build.gradle`, `gradle/wrapper/gradle-wrapper.properties`, `src/main/resources/fabric.mod.json` — authoritative identity, version, Java, dependency, task, and ordinary-JAR declarations.
    - `src/main`, `src/client`, `src/test`, `src/gametest` — complete source/resource surfaces covered by the final audit and archive expectations.
    - `.planning/phases/01-java-25-and-fabric-26-2-foundation/01-CONTEXT.md`, `01-RESEARCH.md`, `01-PATTERNS.md`, and `01-VALIDATION.md` — locked proof boundary, exact commands, actual OS-isolation requirement, and evidence contract.
    - `AGENTS.md` and `.gitignore` — offline/privacy/security constraints and ignored disposable run/profile locations.
  </read_first>
  <files>README.md, .planning/phases/01-java-25-and-fabric-26-2-foundation/01-FOUNDATION-EVIDENCE.md</files>
  <action>
    Execute the unchanged `scripts/verify-foundation.ps1` committed by Plan 03. Before invocation, require `git cat-file -e HEAD:scripts/verify-foundation.ps1` and `git diff --quiet HEAD -- scripts/verify-foundation.ps1`; do not edit or generate the harness in this plan. Re-select `.work/toolchain/temurin-25.0.4+7-x64`, bind task-local `JAVA_HOME`/`PATH`, disable Gradle Java-toolchain auto-detection/download, and require the canonical-path hash, exact runtime/vendor/VM-vendor/architecture, and Java/Javac executable hashes to equal `01-TOOLCHAIN-EVIDENCE.md`. It resolves repository root from its own committed location, propagates every nonzero exit, invokes `scripts/audit-foundation.ps1 -SourceAndDependencies`, and fails unless its complete `src/main` plus `src/client` network/SDK scan, direct-runtime-declaration allowlist, common/client linkage check, repository policy, and Git-hygiene checks pass. It parses the toolchain evidence for actual template tree/ref/commit, exact verified Temurin artifact/runtime hashes, Gradle 9.5.1, Minecraft 26.2, Loader 0.19.3, Fabric API 0.158.0+26.2, requested/selected/mechanically resolved Loom build and artifact hash, fresh commands/numeric exits, eligible same-stream failure classification when applicable, wrapper distribution/checksum-pin state, and actual wrapper-JAR SHA-256. Reject Yarn, mappings, legacy remap configuration/task expectations, dynamic/Git/file runtime dependencies, or an output other than ordinary `jar`.

    For `-PrimeAndCompare`, have the precommitted harness resolve canonical repository/temp/home roots using ordinal case-insensitive Win32 final-path/long-name semantics so 8.3 aliases cannot bypass guards. Create exactly one non-reparse GUID-owned direct child `developers-hell-<32 lowercase hex>` under canonical `[System.IO.Path]::GetTempPath()`; require parent equality to temp and require neither its container nor `worktree` child is equal to or an ancestor of temp, repository, or home. Being below the normal Windows home profile is valid. Add the worktree with `git -C <canonical-repository> worktree add --detach <canonical-worktree> HEAD`, then parse `git worktree list --porcelain -z` for that exact case-normalized path, expected detached HEAD, and detached marker before trusting it. This is separate from Plan 01's upstream Fabric template checkout. Require clean `git status --porcelain` and use `git ls-files --error-unmatch` inside it to prove that source `LICENSE`, wrapper JAR/scripts/properties, build files, every required main/client/test/GameTest source/resource, `scripts/loom-resolution.init.gradle`, `scripts/audit-foundation.ps1`, and `scripts/verify-foundation.ps1` are tracked. Run only the clean worktree's committed wrapper with the exact verified Temurin root as sole Gradle Java installation. For the online `clean build`, remove its old ignored log first, attach the committed Loom probe, capture that exact command's combined stream/numeric exit, and require configured Loom, concrete resolved build, and artifact SHA-256 to equal `loom_selected`, `resolved_loom_build`, and `resolved_loom_sha256` in toolchain evidence. Locate exactly one non-sources/non-javadoc `build/libs/developers-hell-0.1.0.jar` and invoke the clean checkout's audit script with `-SourceAndDependencies -JarPath`.

    Require exactly one root `fabric.mod.json` whose parsed ID is `developers_hell`, exactly one official-rule license entry `LICENSE_developers-hell`, production common/client and registry/module classes, English translation, and both Foundation Token item/model JSONs. Reject unrenamed/duplicate license entries, `dev/developershell/gametest/**`, `FoundationGameTests*.class`, `ModuleGateTest*.class`, every other test output, `developers_hell_test`, and a `fabric-gametest` entrypoint/test metadata from the production JAR. Reject secrets, EULA, worlds, logs, run profiles, local JDK settings, telemetry/OpenAI/network SDK namespaces, or duplicate production artifacts. Save the online JAR only in the guarded sibling location inside the exact owned GUID container and record size/SHA-256. Then remove the prior offline log, run same-cache `--offline clean build` with that same committed wrapper/exact JDK/committed Loom probe, capture its fresh combined stream/numeric exit, independently derive the configured/resolved/artifact-SHA Loom tuple and require evidence equality, and require identical archive entries and SHA-256. Before removing the worktree, atomically copy that verified clean-checkout artifact to ignored workspace `dist/developers-hell-0.1.0.jar`, hash the copied bytes, and require `online_jar_sha256`, `offline_jar_sha256`, and `distribution_sha256` to be the same 64-hex value. Retain this declared distribution artifact after guarded worktree/container cleanup; never substitute a separately resolved or rebuilt workspace artifact.

    Capture `git worktree list --porcelain -z` byte-for-byte before adding the owned worktree. In `finally`, re-run canonical containment/ownership guards; if registered, re-confirm the exact path/HEAD/detached record, call only `git -C <canonical-repository> worktree remove --force -- <exact canonical registered worktree>`, and require zero exit. Perform no repository-wide registration cleanup, never recursively delete a computed path, and never alter unrelated registrations. Prove the exact target record/path is gone and the post-remove porcelain bytes equal the pre-add bytes exactly; then remove the exact owned empty GUID container/sibling artifact and prove repository/home/temp roots remain. On pre-registration failure remove only the guarded empty GUID child. Evidence must include `temp_child_valid`, `worktree_registered`, `worktree_removed`, `worktree_registry_restored`, `temp_container_removed`, and root-preservation PASS. `--offline` proves cache completeness only; it is not runtime network isolation.

    For every automated isolated server run, use the harness's sole fail-closed Windows Defender Firewall primitive around the exact verified Temurin runtime. Require elevation/control up front; create one fresh unique group containing exactly two unique outbound-block rules—one for exact `java.exe`, one for exact sibling `javaw.exe`—and record the group ID, both exact rule IDs, both executable SHA-256 identities, and active membership count `2`. From exact verified Java, require the bounded probe target reachable before creation and blocked after both rules become active. Always remove both rules by exact ID in `finally`; independently require both IDs absent and group membership `0`. Never enumerate/remove unrelated firewall objects or label Gradle `--offline` as runtime isolation. Any unavailable elevation/control, unexpected group member, uncertain probe, or incomplete cleanup fails closed.

    For `-RunServerSmoke`, use an ignored disposable production-server directory and only the exact `dist/developers-hell-0.1.0.jar` produced above. If the production task requires a copy under `build/libs` or a profile `mods` directory, copy those verified bytes without rebuilding, hash the copy immediately before and after launch, and prove the resolved launch command/mod classpath names that exact file; reject any unequal or separately generated JAR. Accept the EULA only in that ignored server directory, bound startup/stop timeouts, require both the Developer's Hell common-initializer marker and Minecraft ready marker, send `stop` through standard input, and require normal exit with no client-class linkage/crash. Run once online and once with the same profile while the exact two-rule block is active; hash the distribution itself before/after both launches and record ready, clean-stop, probe, group/two-rule membership, both rule cleanups, and hashes separately. For `-ClientPreflight`, use a separate ignored production-client profile and the same exact distribution bytes/copy contract, require both entrypoints and menu readiness online, then close through the safest supported bounded mechanism. Record distribution/runtime-copy hashes before and after, prime this exact profile for Task 2, and do not claim world entry, save/quit, clean GUI exit, or offline client success before the blocking checkpoint.

    Create `01-FOUNDATION-EVIDENCE.md` from observed outputs with parseable `key: value` markers: timestamp/OS; normalized exact JDK archive/runtime/vendor/architecture/binary/path hashes and Gradle version; exact tuple and machine-derived template provenance; `loom_selected`, exact `resolved_loom_build`, exact `resolved_loom_sha256`, and separate fresh-probe PASS/command/exit/log-hash markers for the detached online and offline builds; clean-project detached HEAD, tracked-manifest/clean-status PASS, `temp_child_valid`, `worktree_registered`, `worktree_removed`, `worktree_registry_restored`, `temp_container_removed`, and root-preservation PASS; direct-dependency/comprehensive-audit PASS; ordinary JAR path/size/required-entry including `LICENSE_developers-hell` PASS and test/GameTest-exclusion PASS; 64-hex online/offline/distribution SHA-256 equality; exact relative `distribution_path: dist/developers-hell-0.1.0.jar`; Gradle cache/profile identifiers; distribution/runtime-copy pre/post hashes; online and isolated server ready/clean-stop; `server_isolation_group_id`, `server_isolation_java_rule_id`, `server_isolation_javaw_rule_id`, both program hashes, membership `2`, reachable/blocked probes, each rule absent, final membership `0`, and cleanup PASS; online client-preflight result; and `uat_status: PENDING`. Reserve corresponding `client_isolation_*` receipt-derived markers for Task 3. Reject blank, synthetic, or missing machine values except the intentional pending UAT/client-isolation fields. Normalize paths so no username/private home, secret, employer, school, or claimed sponsor/billing fact is published. `-ValidateEvidence` must fail on schema/JDK/tuple/Loom/hash/audit/isolation/clean-checkout/archive/license-exclusion mismatches; `-RequireUatPass` is reserved for Task 3 after the observation-only checkpoint.

    Update `README.md` with the exact player tuple and ignored `dist/developers-hell-0.1.0.jar` handoff plus its three-way SHA relationship. Separate online priming from offline use; document exact Temurin/Wrapper/Loom-probe/build/GameTest/audit/clean-worktree contracts, license/test exclusions, and that Plan 04 automation—not the checkpoint user—starts a hidden supervisor which launches both visible Minecraft sessions, manages the two-rule transition/cleanup, and writes a hashed ignored receipt. The human checklist contains only in-game observations and normal client exits. Task 3 alone consumes the machine receipt plus human response and records/validates public evidence; the parody has no runtime API, telemetry, factual sponsor, or endorsement claim.

    After all synchronous gates and pending evidence pass, prepare the checkpoint automatically. Require the current process is elevated and the exact two-rule primitive already proved usable; otherwise fail here before any checkpoint. Create a fresh ignored canonical `.work/interactive-uat-<32 lowercase hex>` directory with empty `control.json`, atomic `status.json`, `receipt.json`, and supervisor log targets, plus ignored pointer `.work/interactive-uat-active.json`; reject reuse, reparse points, pre-existing pointer, or any terminal artifact. Start a separate `powershell.exe -File scripts/verify-foundation.ps1 -SuperviseInteractiveUat ...` with `Start-Process -WindowStyle Hidden -PassThru`, passing exact evidence/distribution/session/pointer paths. The background PowerShell stays hidden, while its Minecraft client launches must be visible. Poll only until a freshly parsed atomic status reports `ONLINE_READY`, matching session/profile/distribution hash, the returned supervisor PID, a distinct live client PID, and menu-ready marker; also require both processes live and no FAILED/terminal receipt. Bound readiness and kill no client: on timeout/early exit, fail before checkpoint and require the supervisor's own `finally` cleanup. Persist only the ignored active-session pointer for Task 3. The supervisor remains alive across the checkpoint, automatically waits for normal online exit, establishes/proves isolation, launches/waits for the isolated client, cleans both rules, hashes the receipt, and exits.
  </action>
  <acceptance_criteria>
    - The harness begins and ends with the comprehensive Plan 03 audit; both source sets, direct declarations, archive namespaces, common/client boundary, first-party repositories, and Git hygiene must pass.
    - The harness is present and unchanged at committed `HEAD`; the disposable detached clean Developer's Hell worktree proves every required build/source/resource/script is tracked and uses only its committed wrapper under the exact checksum-bound Eclipse Temurin `25.0.4+7` root.
    - Toolchain evidence is parsed for every machine-derived provenance/tree/diff/tuple/command/numeric-exit/fallback/wrapper checksum marker; a missing or contradictory marker fails rather than passing on file existence.
    - One ordinary JAR from the clean checkout contains exactly `LICENSE_developers-hell`, passes exact production inclusion and test/GameTest exclusion assertions, and yields identical online, same-cache-offline, and copied `dist/developers-hell-0.1.0.jar` SHA-256 values before worktree removal.
    - Both clean-checkout builds run the committed Loom probe against fresh captured streams and independently match exact evidenced configured Loom, concrete resolved build, and artifact SHA-256.
    - The worktree is an exact porcelain-registered detached HEAD under a canonical GUID-named direct temp child; guarded exact `git worktree remove` removes only that registration, preserves the pre-existing porcelain bytes exactly, never recursively targets a computed broad path, and proves repo/home/temp roots remain.
    - The production dedicated server reaches ready state and stops normally online and under a unique exactly-two-rule Java/javaw block; both exact IDs are absent and the group has zero members after `finally`.
    - Server and automated client preflight consume only the declared distribution bytes (or an immediately hash-equal explicit copy), prove pre/post hash equality, and client preflight primes the exact profile prepared for Task 2 without claiming human world behavior.
    - Before the checkpoint, automation proves elevation/isolation availability, starts the committed supervisor via hidden `Start-Process`, and hands off only after atomic `ONLINE_READY` plus matching live supervisor/visible-client PIDs, exact session/profile/distribution contract, and no failure receipt.
    - Evidence has no blank or synthetic value except intentional `uat_status: PENDING`; README explains detached-clean-HEAD proof and test-output exclusions and cleanly separates online prime, cache-only Gradle offline mode, and actual runtime network isolation.
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'
$jdkRoot = (Resolve-Path -LiteralPath '.work/toolchain/temurin-25.0.4+7-x64').Path; $env:JAVA_HOME = $jdkRoot; $env:Path = "$(Join-Path $jdkRoot 'bin');$env:Path"
function Get-EvidenceValue([string] $Name) { $text = Get-Content -LiteralPath '.planning/phases/01-java-25-and-fabric-26-2-foundation/01-FOUNDATION-EVIDENCE.md' -Raw; $match = [regex]::Match($text, '(?m)^' + [regex]::Escape($Name) + ':\s*(.+?)\s*$'); if (-not $match.Success) { throw "Evidence marker missing: $Name" }; $match.Groups[1].Value.Trim() }
& git cat-file -e 'HEAD:scripts/verify-foundation.ps1'; if ($LASTEXITCODE -ne 0) { throw 'Verification harness is not committed at HEAD' }
& git diff --quiet HEAD -- scripts/verify-foundation.ps1; if ($LASTEXITCODE -ne 0) { throw 'Verification harness differs from committed HEAD' }
& powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-foundation.ps1 -PrimeAndCompare -RunServerSmoke -ClientPreflight -ValidateEvidence -DistributionPath .\dist\developers-hell-0.1.0.jar -EvidencePath .\.planning\phases\01-java-25-and-fabric-26-2-foundation\01-FOUNDATION-EVIDENCE.md; if ($LASTEXITCODE -ne 0) { throw "Foundation verification failed with exit $LASTEXITCODE" }
$principal = [Security.Principal.WindowsPrincipal]::new([Security.Principal.WindowsIdentity]::GetCurrent()); if (-not $principal.IsInRole([Security.Principal.WindowsBuiltInRole]::Administrator)) { throw 'Interactive UAT supervisor requires elevation before the checkpoint' }; if (-not (Get-Command Get-NetFirewallRule -ErrorAction SilentlyContinue)) { throw 'Firewall isolation control unavailable before checkpoint' }
$workRoot = (Resolve-Path -LiteralPath '.work').Path; $sessionId = [guid]::NewGuid().ToString('N'); $sessionDirectory = Join-Path $workRoot ("interactive-uat-$sessionId"); $pointerPath = Join-Path $workRoot 'interactive-uat-active.json'; if (Test-Path -LiteralPath $pointerPath) { throw 'Stale interactive UAT pointer exists' }; New-Item -ItemType Directory -Path $sessionDirectory -ErrorAction Stop | Out-Null; $controlPath = Join-Path $sessionDirectory 'control.json'; $statusPath = Join-Path $sessionDirectory 'status.json'; $receiptPath = Join-Path $sessionDirectory 'receipt.json'; [pscustomobject]@{ command='RUN'; session_id=$sessionId } | ConvertTo-Json -Compress | Set-Content -LiteralPath $controlPath -Encoding UTF8; $scriptPath = (Resolve-Path -LiteralPath '.\scripts\verify-foundation.ps1').Path; $argumentList = @('-NoProfile','-ExecutionPolicy','Bypass','-File',('"' + $scriptPath + '"'),'-SuperviseInteractiveUat','-EvidencePath',('"' + (Resolve-Path '.\.planning\phases\01-java-25-and-fabric-26-2-foundation\01-FOUNDATION-EVIDENCE.md').Path + '"'),'-DistributionPath',('"' + (Resolve-Path '.\dist\developers-hell-0.1.0.jar').Path + '"'),'-SessionDirectory',('"' + $sessionDirectory + '"'),'-SessionPointerPath',('"' + $pointerPath + '"')); $supervisor = Start-Process -FilePath (Get-Command powershell.exe).Source -ArgumentList $argumentList -WindowStyle Hidden -PassThru
try { $deadline = [DateTime]::UtcNow.AddMinutes(5); $status = $null; do { if ($supervisor.HasExited) { throw "Interactive supervisor exited before ONLINE_READY: $($supervisor.ExitCode)" }; if (Test-Path -LiteralPath $statusPath -PathType Leaf) { try { $status = Get-Content -LiteralPath $statusPath -Raw | ConvertFrom-Json } catch { $status = $null }; if ($null -ne $status -and $status.state -eq 'FAILED') { throw "Interactive supervisor failed before checkpoint: $($status.failure_code)" } }; if ($null -eq $status -or $status.state -ne 'ONLINE_READY') { Start-Sleep -Milliseconds 500 } } while (($null -eq $status -or $status.state -ne 'ONLINE_READY') -and [DateTime]::UtcNow -lt $deadline); if ($null -eq $status -or $status.state -ne 'ONLINE_READY' -or [int] $status.supervisor_pid -ne $supervisor.Id -or [string] $status.session_id -ne $sessionId -or [string] $status.distribution_sha256 -ne (Get-EvidenceValue 'distribution_sha256') -or -not (Get-Process -Id ([int] $status.client_pid) -ErrorAction SilentlyContinue) -or $status.menu_ready -ne $true -or (Test-Path -LiteralPath $receiptPath)) { throw 'ONLINE_READY/liveness contract not established before checkpoint' }; [pscustomobject]@{ session_id=$sessionId; session_directory=$sessionDirectory; status_path=$statusPath; receipt_path=$receiptPath; supervisor_pid=$supervisor.Id } | ConvertTo-Json -Compress | Set-Content -LiteralPath $pointerPath -Encoding UTF8 } catch { if (-not $supervisor.HasExited) { [pscustomobject]@{ command='CANCEL'; session_id=$sessionId } | ConvertTo-Json -Compress | Set-Content -LiteralPath $controlPath -Encoding UTF8; Wait-Process -Id $supervisor.Id -Timeout 60 -ErrorAction SilentlyContinue }; if (Get-Process -Id $supervisor.Id -ErrorAction SilentlyContinue) { throw 'Supervisor failed readiness and did not complete its scoped cleanup' }; throw }</automated>
  </verify>
  <done>All artifact/server/preflight gates pass and automation has launched a live ONLINE production client under the committed hidden supervisor; the checkpoint receives only in-game observation responsibility.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Observe production-client install, world entry, save/quit, and isolated repeat</name>
  <what-built>Automation has already launched the visible ONLINE Minecraft client from the exact verified game files. A hidden committed supervisor owns both Minecraft sessions and prepares the machine receipt; evidence remains pending until the finalizer consumes that receipt and your in-game observations.</what-built>
  <how-to-verify>
    1. In the already-open ONLINE Minecraft client, observe Developer's Hell 0.1.0 and Fabric API in the mod list; create/enter a world; run `/give @s developers_hell:foundation_token`; observe translated name/model; save/quit to title and exit normally.
    2. Wait for the supervisor to launch the second visible Minecraft client automatically. In that ISOLATED session, repeat mod-list, world-entry, token, and save/quit observations, then exit normally.
    3. Report only the eight observations below. No action outside the two visible Minecraft windows is part of this checkpoint.
  </how-to-verify>
  <human-check>Report `online_mod_list`, `online_world_entry`, `online_token`, `online_save_exit`, `isolated_mod_list`, `isolated_world_entry`, `isolated_token`, and `isolated_save_exit`, each as `PASS` or `FAIL`; on failure add only the first failed in-game step and a public-safe concise symptom.</human-check>
  <resume-signal>Reply `approved` with all eight named observations explicitly `PASS`, or `failed` with the first failing observation and public-safe symptom. Auto mode must wait.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Finalize, validate, and commit the human UAT evidence</name>
  <read_first>
    - The Task 2 structured resume response — sole source for human-observed online/isolated UAT values; never infer an omitted value.
    - `.work/interactive-uat-active.json` and its referenced unique session `status.json`/`receipt.json` — ignored automation-owned supervisor state; require exact pointer containment, terminal supervisor exit, and a complete hashed receipt rather than inferring machine state from the human response.
    - `.planning/phases/01-java-25-and-fabric-26-2-foundation/01-FOUNDATION-EVIDENCE.md` — Task 1 machine evidence and exact marker schema; update only receipt-derived client-isolation and human UAT fields.
    - `README.md` — checkpoint contract and public-safe terminology; modify only if the observed procedure exposed a factual documentation correction.
    - `scripts/verify-foundation.ps1` — unchanged committed validator invoked with `-ValidateEvidence -RequireUatPass` after evidence mutation.
  </read_first>
  <files>.planning/phases/01-java-25-and-fabric-26-2-foundation/01-FOUNDATION-EVIDENCE.md, README.md</files>
  <action>
    Consume the blocking-human response only after Task 2 resumes. Require all eight named in-game observations and accept only literal `PASS` or `FAIL`; never infer omitted values. Resolve the unique session only through ignored `.work/interactive-uat-active.json`, require its canonical directory is the expected GUID child under `.work`, and wait at most 60 seconds for the recorded supervisor PID to exit after the second client. Parse atomic terminal status plus receipt and recompute `receipt_payload_sha256` over the canonical payload. Require fresh `COMPLETE`, matching session/evidence/profile IDs, ONLINE_READY and ISOLATED_READY milestones, two distinct client PIDs with normal exits, exact distribution/runtime-copy pre/post SHA equality, verified Java/javaw hashes, reachable then blocked probes, exactly two active rule IDs, both absent afterward, group zero, cleanup PASS, and supervisor exit zero. Copy only public-safe receipt fields to reserved machine evidence markers. Set `uat_status: PASS` only for `approved`, eight explicit PASS values, and every receipt condition; otherwise record FAIL plus the first sanitized failure and halt.

    Write no user path, username, host name, account, secret, employer, school, factual sponsorship/billing claim, raw log, or uncontrolled free-form response into evidence. Independently query both receipt rule names and require each absent, then query only the recorded group and require zero members. This finalizer must not remove rules, enumerate/delete unrelated objects, change network state, or perform broad cleanup. Rehash `dist/developers-hell-0.1.0.jar` and require exact equality with Task 1 evidence and receipt pre/post values. Require the retained exact Temurin Java executable/path hashes still equal toolchain evidence, then run the unchanged committed harness with `-ValidateEvidence -RequireUatPass -EvidencePath <phase evidence> -SessionReceiptPath <ignored receipt> -DistributionPath <exact dist JAR>`. Commit the finalized evidence and README only if README factually changed; no checkpoint task owns a mutation or commit.
  </action>
  <acceptance_criteria>
    - All eight human observations are explicit; `uat_status: PASS` is possible only for `approved` plus eight PASS values, while missing/failed observations block validation.
    - The pointer identifies one guarded session; supervisor/status are terminal, both automatically launched clients reached readiness and exited normally, and the canonical-payload SHA authenticates the fresh COMPLETE receipt.
    - The receipt binds exact distribution/profile/evidence, records exactly two active rules plus both program hashes/reachable-blocked probes, and proves both IDs absent/group zero after `finally`.
    - The finalizer only observes cleanup: both exact receipt rule names remain absent and the recorded group has no members; it performs no firewall or broad cleanup action.
    - Final evidence contains only public-safe normalized fields and still validates every exact-JDK, provenance, tuple, per-build Loom, worktree, license/archive, audit, three-way distribution hash, server/client, two-rule isolation, receipt, and cleanup marker.
    - `verify-foundation.ps1 -ValidateEvidence -RequireUatPass` exits zero, and the normal task commit contains only finalized evidence plus README if it actually changed.
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'
function Get-EvidenceValue([string] $Name) { $text = Get-Content -LiteralPath '.planning/phases/01-java-25-and-fabric-26-2-foundation/01-FOUNDATION-EVIDENCE.md' -Raw; $match = [regex]::Match($text, '(?m)^' + [regex]::Escape($Name) + ':\s*(.+?)\s*$'); if (-not $match.Success) { throw "Evidence marker missing: $Name" }; $match.Groups[1].Value.Trim() }
$observations = @('online_mod_list','online_world_entry','online_token','online_save_exit','isolated_mod_list','isolated_world_entry','isolated_token','isolated_save_exit'); foreach ($name in $observations) { if ((Get-EvidenceValue $name) -ne 'PASS') { throw "Human UAT observation is not PASS: $name" } }; if ((Get-EvidenceValue 'uat_status') -ne 'PASS') { throw 'Final UAT status is not PASS' }
$pointerPath = '.work/interactive-uat-active.json'; if (-not (Test-Path -LiteralPath $pointerPath -PathType Leaf)) { throw 'Interactive UAT pointer is missing' }; $pointer = Get-Content -LiteralPath $pointerPath -Raw | ConvertFrom-Json; $workRoot = (Resolve-Path -LiteralPath '.work').Path; $sessionDirectory = (Resolve-Path -LiteralPath ([string] $pointer.session_directory)).Path; if ((Split-Path -Parent $sessionDirectory) -ne $workRoot -or (Split-Path -Leaf $sessionDirectory) -notmatch '^interactive-uat-[0-9a-f]{32}$') { throw 'Interactive UAT session containment is invalid' }; $supervisorProcess = Get-Process -Id ([int] $pointer.supervisor_pid) -ErrorAction SilentlyContinue; if ($supervisorProcess) { Wait-Process -Id $supervisorProcess.Id -Timeout 60 -ErrorAction SilentlyContinue }; if (Get-Process -Id ([int] $pointer.supervisor_pid) -ErrorAction SilentlyContinue) { throw 'Interactive supervisor did not exit after both human sessions' }; $statusPath = (Resolve-Path -LiteralPath ([string] $pointer.status_path)).Path; $receiptPath = (Resolve-Path -LiteralPath ([string] $pointer.receipt_path)).Path; if ((Split-Path -Parent $statusPath) -ne $sessionDirectory -or (Split-Path -Leaf $statusPath) -ne 'status.json' -or (Split-Path -Parent $receiptPath) -ne $sessionDirectory -or (Split-Path -Leaf $receiptPath) -ne 'receipt.json') { throw 'Supervisor status/receipt escaped the unique session' }; $status = Get-Content -LiteralPath $statusPath -Raw | ConvertFrom-Json; $receipt = Get-Content -LiteralPath $receiptPath -Raw | ConvertFrom-Json; $payloadJson = $receipt.payload | ConvertTo-Json -Compress -Depth 20; $sha = [Security.Cryptography.SHA256]::Create(); try { $payloadHash = ([BitConverter]::ToString($sha.ComputeHash([Text.Encoding]::UTF8.GetBytes($payloadJson)))).Replace('-','').ToLowerInvariant() } finally { $sha.Dispose() }; if ($payloadHash -ne ([string] $receipt.receipt_payload_sha256).ToLowerInvariant() -or $status.state -ne 'COMPLETE' -or [string] $status.session_id -ne [string] $pointer.session_id -or $receipt.payload.status -ne 'COMPLETE' -or [string] $receipt.payload.session_id -ne [string] $pointer.session_id -or [int] $receipt.payload.supervisor_pid -ne [int] $pointer.supervisor_pid -or $receipt.payload.supervisor_exit -ne 0 -or $receipt.payload.online.ready -ne $true -or $receipt.payload.online.exit -ne 'NORMAL' -or $receipt.payload.isolated.ready -ne $true -or $receipt.payload.isolated.exit -ne 'NORMAL' -or [int] $receipt.payload.online.pid -eq [int] $receipt.payload.isolated.pid -or $receipt.payload.firewall.member_count_active -ne 2 -or $receipt.payload.firewall.member_count_after -ne 0 -or $receipt.payload.firewall.java_rule_absent -ne $true -or $receipt.payload.firewall.javaw_rule_absent -ne $true -or $receipt.payload.cleanup_status -ne 'PASS' -or $receipt.payload.probe.online -ne 'PASS' -or $receipt.payload.probe.isolated -ne 'BLOCKED') { throw 'Supervisor receipt hash/session/cleanup contract failed' }; $groupId = [string] $receipt.payload.firewall.group_id; $javaRuleId = [string] $receipt.payload.firewall.java_rule_id; $javawRuleId = [string] $receipt.payload.firewall.javaw_rule_id; if ([string]::IsNullOrWhiteSpace($groupId) -or [string]::IsNullOrWhiteSpace($javaRuleId) -or [string]::IsNullOrWhiteSpace($javawRuleId) -or $javaRuleId -eq $javawRuleId) { throw 'Interactive receipt rule identity is invalid' }; if (-not (Get-Command Get-NetFirewallRule -ErrorAction SilentlyContinue)) { throw 'Cannot independently confirm firewall cleanup' }; if (Get-NetFirewallRule -Name $javaRuleId -ErrorAction SilentlyContinue) { throw 'Recorded java firewall rule still exists' }; if (Get-NetFirewallRule -Name $javawRuleId -ErrorAction SilentlyContinue) { throw 'Recorded javaw firewall rule still exists' }; if (@(Get-NetFirewallRule -Group $groupId -ErrorAction SilentlyContinue).Count -ne 0) { throw 'Recorded firewall group still has members' }
$distributionPath = 'dist/developers-hell-0.1.0.jar'; if (-not (Test-Path -LiteralPath $distributionPath -PathType Leaf)) { throw 'Verified distribution JAR is missing' }; $distributionHash = (Get-FileHash -LiteralPath $distributionPath -Algorithm SHA256).Hash.ToLowerInvariant(); if ($distributionHash -ne (Get-EvidenceValue 'distribution_sha256').ToLowerInvariant() -or $distributionHash -ne ([string] $receipt.payload.distribution.sha256_before).ToLowerInvariant() -or $distributionHash -ne ([string] $receipt.payload.distribution.sha256_after).ToLowerInvariant()) { throw 'Distribution hash no longer matches evidence and supervisor receipt' }
$finalEvidence = Get-Content -LiteralPath '.planning/phases/01-java-25-and-fabric-26-2-foundation/01-FOUNDATION-EVIDENCE.md' -Raw; if ($finalEvidence -match '(?i)(?:C:\\Users\\[^\\\r\n]+|/Users/[^/\r\n]+|password\s*:|api[_-]?key\s*:|secret\s*:|employer\s*:|school\s*:|sponsor(?:ed|ship)?\s*:)') { throw 'Private or unsupported factual content found in public evidence' }
$jdkRoot = (Resolve-Path -LiteralPath '.work/toolchain/temurin-25.0.4+7-x64').Path; $javaHash = (Get-FileHash -LiteralPath (Join-Path $jdkRoot 'bin/java.exe') -Algorithm SHA256).Hash.ToLowerInvariant(); $javawHash = (Get-FileHash -LiteralPath (Join-Path $jdkRoot 'bin/javaw.exe') -Algorithm SHA256).Hash.ToLowerInvariant(); if ($javaHash -ne ([string] $receipt.payload.firewall.java_program_sha256).ToLowerInvariant() -or $javawHash -ne ([string] $receipt.payload.firewall.javaw_program_sha256).ToLowerInvariant()) { throw 'Supervisor receipt program identities do not match verified JDK' }; $env:JAVA_HOME = $jdkRoot; $env:Path = "$(Join-Path $jdkRoot 'bin');$env:Path"; & powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-foundation.ps1 -ValidateEvidence -RequireUatPass -EvidencePath .\.planning\phases\01-java-25-and-fabric-26-2-foundation\01-FOUNDATION-EVIDENCE.md -SessionReceiptPath $receiptPath -DistributionPath $distributionPath; if ($LASTEXITCODE -ne 0) { throw "Completed evidence validation failed with exit $LASTEXITCODE" }</automated>
  </verify>
  <done>The observation-only human response is converted into public-safe machine-validated PASS evidence, network restoration is independently confirmed, and the final evidence/README task commit is ready for the Phase 1 summary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Pinned build inputs/cache → production artifact | Cached first-party dependencies and wrapper outputs become the distributed JAR. |
| Project source/declarations/archive → offline claim | Hidden network APIs, SDKs, endpoints, dependencies, or shaded code could violate runtime locality. |
| Built JAR → production dedicated server/client | Client-only linkage, missing resources, or transformed-runtime failures appear only outside development source sets. |
| Verification harness → host network controls | Two exact temporary firewall rules require unique grouping, independent proof, `finally` restoration, and fail-closed handling. |
| Evidence/README → public repository | Local paths, identities, secrets, or invented sponsorship/billing claims must not cross into committed output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-15 | Tampering | committed checkout, wrapper, cache, pins, and production JAR | high | mitigate | Build from a clean detached HEAD with tracked-manifest proof, parse exact toolchain evidence, rerun the comprehensive audit, inspect production inclusions/test exclusions, and require equal online/offline SHA-256. |
| T-01-16 | Elevation of Privilege | common code on dedicated server | high | mitigate | Retain the recursive side audit and require built-JAR server ready/clean-stop proof online and isolated. |
| T-01-17 | Information Disclosure | runtime network/SDK surface | high | mitigate | Fail closed across both source sets, direct declarations, shaded archive namespaces, and an independently verified Java-process OS network block. |
| T-01-18 | Denial of Service | temporary network isolation and temp worktree | high | mitigate | Use exactly two unique-group rules bound to verified java/javaw hashes, remove only both exact IDs in `finally`, require zero group members, and independently confirm cleanup; worktree removal is exact-registration only with byte-identical preservation of prior registrations. |
| T-01-19 | Repudiation | automated and human evidence | medium | mitigate | Automation launches/supervises both clients and hashes its receipt; the checkpoint reports only eight in-game values, and the auto finalizer verifies supervisor exit, receipt, cleanup, and evidence before commit. |
| T-01-20 | Information Disclosure | public documentation/evidence | high | mitigate | Normalize machine paths and reject secrets, real identities, employer/school data, or factual sponsorship/billing claims. |
</threat_model>

## Artifacts this phase produces

- `README.md` — exact player installation, contributor build/test, cache-only offline build, OS-isolated runtime, cleanup, and bounded client-world checklist.
- `.planning/phases/01-java-25-and-fabric-26-2-foundation/01-FOUNDATION-EVIDENCE.md` — actual provenance, tuple, commands, exits, dependencies/audits, artifact entries/hashes, cache/profile, server/client, isolation/probe/cleanup, and UAT markers.
- `dist/developers-hell-0.1.0.jar` — intentionally ignored installable artifact copied from the verified equal clean-checkout build before exact worktree removal.
- `.work/interactive-uat-active.json` plus `.work/interactive-uat-<guid>/{control,status,receipt}.json` — intentionally ignored automation-owned pointer/state and canonical-payload-hashed receipt for both supervised client sessions and cleanup.

## Multi-Source Coverage Audit

| Source | Item | Status | Plan coverage |
|---|---|---|---|
| GOAL | One reproducible, offline-installable Fabric 26.2 foundation proven on client and dedicated server | COVERED | 01-01-01 proves the pristine prerequisite; 01-01-02 completes the item tracer; 01-02-01 extracts/tests stable registration gates; 01-03-01 adds GameTest, production tasks, and the comprehensive audit; 01-04-01 proves artifact/server/cache/isolation; 01-04-02 observes the real client world; 01-04-03 safely records and validates it. |
| REQ | FND-01 offline install and world entry | COVERED | 01-04-01 documents and primes the exact production install; 01-04-02 observes online and OS-isolated world entry/save/exit; 01-04-03 records and machine-validates the complete observation. |
| REQ | FND-02 fresh-checkout wrapper and frozen Java 25/Fabric 26.2 tuple | COVERED | 01-01-01 records machine-derived official-template help/build evidence; 01-03-01 commits the harness; 01-04-01 runs that harness from detached clean project HEAD and proves byte-identical same-cache output. |
| REQ | FND-03 client and dedicated-server safety | COVERED | 01-01-02 establishes split entrypoints, 01-03-01 audits/tests them, 01-04-01 proves the exact distribution on server/client, 01-04-02 completes world UAT, and 01-04-03 validates its evidence. |
| REQ | FND-04 stable registrations behind independent gates | COVERED | 01-01-02 registers the token unconditionally; 01-02-01 extracts the stable catalog and exhaustively tests all eight independent module states. |
| RESEARCH | Exact verified JDK/tuple; fixed Loom first/fallback; no-remap ordinary JAR and renamed license; non-shipping GameTest; productionRuntimeMods; guarded clean-checkout/offline/production validation; comprehensive offline-security audit | COVERED | 01-01-01, 01-01-02, 01-02-01, 01-03-01, 01-04-01, 01-04-02, and 01-04-03 implement each accepted outcome and its evidence boundary. |
| CONTEXT | Pristine scaffold commit before customization; side split; frozen identity/catalog; clean profiles; online prime; same-cache build; actual offline runtime; bounded world UAT; public-safe evidence | COVERED | The seven ordered tasks implement every locked Phase 1 decision without pulling campaign, boss, HUD, terminal, module gameplay, bespoke art/audio, showcase, multiloader, publishing, or broad CI work forward. |
| SPECLESS EDGE | Exactly eight supplied fallback rows remain explicit and unresolved | COVERED | 01-01 retains FND-02 and FND-03 unclassified; 01-02 retains FND-04 adjacency, empty, ordering, idempotency, and concurrency; 01-04 retains FND-01 unclassified. No row is dismissed or invented. |
| SPECLESS PROHIBITION | Public defaults stay fictional/public-safe and runtime stays local/offline | COVERED | Exactly two descriptor-less, flagged/unverified prohibitions remain under 01-01 `must_haves.prohibitions`; tasks and threats enforce them without adding prohibition rows. |

<verification>
- Run the unchanged committed `scripts/verify-foundation.ps1 -PrimeAndCompare -RunServerSmoke -ClientPreflight -ValidateEvidence -DistributionPath .\dist\developers-hell-0.1.0.jar`; every automated marker except human UAT/client interactive receipt is PASS.
- Complete observation-only Task 2 only after Task 1 has launched the hidden supervisor and visible online client; Task 2 performs only eight in-game observations/normal exits while automation launches the isolated client. Task 3 consumes the hashed COMPLETE receipt plus observations, confirms supervisor exit and two-rule/group cleanup, and runs `-ValidateEvidence -RequireUatPass`.
- Confirm `git status --short` contains no cache, build, run, world, EULA, log, IDE, dump, recording, local-JDK, or private evidence dirt.
</verification>

<success_criteria>
- FND-01: the exact `dist/developers-hell-0.1.0.jar` enters, saves, and exits a production singleplayer world online and under independently verified two-rule OS isolation from the same primed profile.
- FND-02: a detached clean checkout of committed HEAD proves every required input tracked, then Java 25, the exact tuple, and its committed Wrapper 9.5.1 plus per-build Loom probe produce online/offline artifacts whose SHA-256 equals the retained distribution.
- FND-03: the same distribution bytes reach production client readiness and dedicated-server readiness/clean stop without client linkage; isolated runtime proof is distinct from Gradle cache mode and leaves both exact rules absent/group empty.
- FND-04 remains preserved by the prior stable catalog/gate test and packaged Foundation Token.
- The phase closes with six automated tasks plus one blocking-human observation checkpoint; the post-checkpoint auto finalizer produces complete public-safe evidence.
</success_criteria>

<output>Create `.planning/phases/01-java-25-and-fabric-26-2-foundation/01-04-SUMMARY.md` only after Task 3 validates and commits the finalized checkpoint evidence.</output>
