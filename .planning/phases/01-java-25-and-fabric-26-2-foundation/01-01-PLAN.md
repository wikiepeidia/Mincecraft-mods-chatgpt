---
phase: 01-java-25-and-fabric-26-2-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - settings.gradle
  - gradle.properties
  - build.gradle
  - gradlew
  - gradlew.bat
  - gradle/wrapper/gradle-wrapper.jar
  - gradle/wrapper/gradle-wrapper.properties
  - .planning/phases/01-java-25-and-fabric-26-2-foundation/01-TOOLCHAIN-EVIDENCE.md
  - src/main/java/dev/developershell/DevelopersHell.java
  - src/client/java/dev/developershell/client/DevelopersHellClient.java
  - src/main/resources/fabric.mod.json
  - src/main/resources/assets/developers_hell/items/foundation_token.json
  - src/main/resources/assets/developers_hell/models/item/foundation_token.json
autonomous: true
requirements: [FND-02, FND-03, FND-04]
estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Repository hygiene is active before any wrapper is copied or invoked: the wrapper JAR is explicitly trackable while build caches, run profiles/worlds/EULA/logs, IDE state, dumps, recordings, and local JDK settings are ignored."
    - "The official Eclipse Temurin 25.0.4+7 Windows x64 ZIP is checksum-verified, machine-queried, and selected task-locally; that exact runtime proves the actual untouched official Fabric 26.2 example with fresh captured help/build/Loom-resolution evidence before customization."
    - "The same plan then completes a real walking-skeleton tracer: one ordinary Developer's Hell JAR packages side-split entrypoints, unconditional developers_hell:foundation_token registration, and both required 26.2 client item/model resources."
  artifacts:
    - path: ".gitignore"
      provides: "Wrapper-safe, fail-closed repository hygiene before first build"
      contains: "!gradle/wrapper/gradle-wrapper.jar"
    - path: ".planning/phases/01-java-25-and-fabric-26-2-foundation/01-TOOLCHAIN-EVIDENCE.md"
      provides: "Exact verified JDK provenance/runtime/binary hashes plus upstream commit/ref, tuple, fresh command exits/logs, mechanically resolved Loom build, and untouched help/build anchors"
    - path: "src/main/java/dev/developershell/DevelopersHell.java"
      provides: "Common initializer and inline unconditional Foundation Token tracer registration"
      exports: ["DevelopersHell", "MOD_ID", "FOUNDATION_TOKEN_ID", "FOUNDATION_TOKEN", "id", "onInitialize"]
    - path: "src/client/java/dev/developershell/client/DevelopersHellClient.java"
      provides: "Physical-client entrypoint seam"
      exports: ["DevelopersHellClient", "onInitializeClient"]
    - path: "src/main/resources/fabric.mod.json"
      provides: "Frozen Loader identity and side-specific entrypoints"
  key_links:
    - from: ".gitignore"
      to: "gradle/wrapper/gradle-wrapper.jar"
      via: "explicit negation after the repository's broad JAR rule"
      pattern: "!gradle/wrapper/gradle-wrapper\\.jar"
    - from: "01-TOOLCHAIN-EVIDENCE.md"
      to: ".work/fabric-example-mod-26.2"
      via: "canonical origin/ref/40-hex commit and observed command outcomes"
      pattern: "template_commit"
    - from: "01-TOOLCHAIN-EVIDENCE.md"
      to: ".work/toolchain/temurin-25.0.4+7-x64"
      via: "official archive/sidecar equality, runtime properties, executable hashes, and redacted canonical-path hash"
      pattern: "jdk_archive_sha256|jdk_runtime_version|jdk_path_sha256"
    - from: "src/main/resources/fabric.mod.json"
      to: "DevelopersHell and DevelopersHellClient"
      via: "main/client Loader entrypoints"
      pattern: "dev\\.developershell"
    - from: "src/main/resources/assets/developers_hell/items/foundation_token.json"
      to: "src/main/resources/assets/developers_hell/models/item/foundation_token.json"
      via: "Minecraft 26.2 client-item model indirection"
      pattern: "developers_hell:item/foundation_token"
  flagged_assumptions:
    - "[UNRESOLVED][verification=null][FND-02][unclassified] Probe: unclassified — review manually. Retain this row while exact Java, wrapper, tuple, provenance, and unchanged-template evidence remain authoritative."
    - "[UNRESOLVED][verification=null][FND-03][unclassified] Probe: unclassified — review manually. Retain it while the side split and later production-runtime proof remain explicit."
  prohibitions:
    - statement: "MUST NOT encode real people, universities, employers, private budget claims, or official sponsorship/endorsement claims in public metadata or defaults."
      status: flagged-unverified
    - statement: "MUST NOT add runtime networking, telemetry, remote configuration, an OpenAI/ChatGPT account or API dependency, secrets, or runtime asset downloads."
      status: flagged-unverified
---

# Phase 1 Plan 01 — Proven Scaffold and Registered-Item Walking Skeleton

## Phase Goal

**As a** player or contributor, **I want to** install, launch, and rebuild one frozen Fabric 26.2 JAR on client and dedicated server, **so that** Developer's Hell remains playable offline after its official dependencies are primed.

<objective>
Retire toolchain risk in an atomic untouched-template checkpoint, then complete the plan with the first real client-visible registered-item tracer.

Purpose: Honor the locked prerequisite exception without ending the first plan on a horizontal scaffold.
Output: Wrapper-safe repository hygiene, an exact official-template proof record, and an ordinary side-split Developer's Hell JAR containing the Foundation Token client resource path.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.agents/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-java-25-and-fabric-26-2-foundation/01-CONTEXT.md
@.planning/phases/01-java-25-and-fabric-26-2-foundation/01-RESEARCH.md
@.planning/phases/01-java-25-and-fabric-26-2-foundation/01-PATTERNS.md
@.planning/phases/01-java-25-and-fabric-26-2-foundation/01-VALIDATION.md
@.planning/phases/01-java-25-and-fabric-26-2-foundation/01-SKELETON.md
@AGENTS.md
@LICENSE
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove and atomically commit the pristine official scaffold prerequisite</name>
  <reversibility rating="costly">The Java, Minecraft, Loader, Fabric API, Loom, and wrapper line govern every later compile and runtime task; changing them later requires coordinated compatibility proof.</reversibility>
  <read_first>
    - `.gitignore` — existing broad `*.jar` rule and repository hygiene baseline; this is the first file changed.
    - `settings.gradle`, `gradle.properties`, `build.gradle`, `gradlew`, `gradlew.bat`, `gradle/wrapper/gradle-wrapper.jar`, `gradle/wrapper/gradle-wrapper.properties` — inspect if present before copying the verified official root/wrapper surface.
    - `.planning/phases/01-java-25-and-fabric-26-2-foundation/01-TOOLCHAIN-EVIDENCE.md` — retain only prior facts backed by captured output; otherwise replace with the exact schema below.
    - `.planning/phases/01-java-25-and-fabric-26-2-foundation/01-CONTEXT.md` — locked untouched-template, wrapper, pin, failure, hygiene, and checkpoint decisions.
    - `.planning/phases/01-java-25-and-fabric-26-2-foundation/01-RESEARCH.md` — exact official tuple, first-party sources, resolved Loom/output questions, and local environment audit.
    - `.planning/phases/01-java-25-and-fabric-26-2-foundation/01-PATTERNS.md` — official root/wrapper and `.gitignore` analogs.
    - `AGENTS.md` and `LICENSE` — project constraints and files that must not be replaced by the upstream template.
  </read_first>
  <files>.gitignore, settings.gradle, gradle.properties, build.gradle, gradlew, gradlew.bat, gradle/wrapper/gradle-wrapper.jar, gradle/wrapper/gradle-wrapper.properties, .planning/phases/01-java-25-and-fabric-26-2-foundation/01-TOOLCHAIN-EVIDENCE.md</files>
  <action>
    This task is the locked prerequisite exception to tracer-first ordering; Task 2 below is the walking-skeleton tracer. Make `.gitignore` the first edit, before copying or invoking any repository wrapper. Preserve useful existing rules and add `.work/`, `.gradle/`, `build/`, `dist/`, `out/`, `run/`, explicit production-client/server directories plus their worlds/EULA/logs, `.idea/`, `*.iml`, `.settings/`, `.vscode/`, `bin/`, crash dumps, heap/profile dumps, recordings, and machine-local JDK/settings files. Keep the existing broad JAR rule only with an explicit later negation `!gradle/wrapper/gradle-wrapper.jar`. Immediately prove with `git check-ignore --no-index` that the wrapper JAR path is not ignored and representative cache/build/distribution/run/EULA/log/IDE/dump/recording/local-setting paths are ignored.

    Acquire the exact official Windows x64 HotSpot JDK ZIP `OpenJDK25U-jdk_x64_windows_hotspot_25.0.4_7.zip` from `https://github.com/adoptium/temurin25-binaries/releases/download/jdk-25.0.4%2B7/OpenJDK25U-jdk_x64_windows_hotspot_25.0.4_7.zip` and its same-release `.sha256.txt` sidecar into ignored `.work/toolchain`; refuse a stale/mismatched file rather than trusting its name. Parse one 64-hex official checksum from the sidecar, compute the archive SHA-256, require equality, and extract to deterministic ignored `.work/toolchain/temurin-25.0.4+7-x64`. Set task-local `JAVA_HOME`, prepend only that `bin` to `PATH`, and disable Gradle Java-toolchain auto-download/auto-detection in favor of this root. Machine-query `java.runtime.version`, `java.vendor`, `java.vm.vendor`, `os.arch`, and `java.home` with that absolute `java.exe`; normalize only the documented optional `-LTS` suffix so the version equals `25.0.4+7`. Require vendor and VM vendor to identify Eclipse Adoptium/Temurin, architecture `amd64`/`x86_64`, `java.home` equal to the canonical verified root, the first `where.exe java`/`javac` results under that same root, `javac 25.0.4`, and 64-hex SHA-256 values for both binaries. Record a public-safe path class and SHA-256 of the canonical path, never the path itself.

    Clone the canonical `https://github.com/FabricMC/fabric-example-mod.git` branch `26.2` into ignored `.work/fabric-example-mod-26.2`; retain its `.git` metadata through verification and record the actual 40-hex `HEAD`, checked-out branch/ref, canonical origin URL, and clean upstream tree. On that complete official source tree, change only `loom_version` from `1.17-SNAPSHOT` to fixed `1.17.19`; require the actual `git diff --name-only` output to equal only `gradle.properties`. Derive Minecraft 26.2, Loader 0.19.3, Fabric API 0.158.0+26.2, Java/release 25, Wrapper distribution 9.5.1, and Loom first-attempt values from the checkout's `gradle.properties`, `build.gradle`, and wrapper properties rather than copying prose into evidence.

    Use one `Invoke-CapturedGradle` helper for every initial run and verification rerun: remove only that command's deterministic ignored log first, invoke the checkout's wrapper with `2>&1`, retain the exact combined stream in the fresh log, and capture `$LASTEXITCODE` immediately from that same invocation. Add an ignored `.work/loom-resolution.init.gradle` probe that derives the applied Loom plugin's resolved module/component version, implementation version/code-source artifact, and artifact SHA-256 from Gradle's actual plugin resolution; it must not echo `loom_version` from properties. Attach it to every `help`, `build`, and resolution command, including fixed/fallback verification reruns, so each successful invocation's own fresh stream mechanically equals the selected/resolved/SHA evidence. A failing fixed attempt may lack resolved markers only when its same fresh stream/exit qualifies for the tightly bounded fallback classification below. Fixed success requires the derived selected and resolved values both equal `1.17.19` and every successful fresh invocation's artifact hash equal evidence.

    If and only if the fixed value fails in exactly category `plugin-resolution` or `minecraft-setup`, retain that failed fixed checkout unchanged for verification and preserve the exact failing command, numeric nonzero `fixed_failure_exit`, category, fresh deterministic log path, and log SHA-256 from that same invocation. Classify only that fresh stream: require an eligible plugin/Loom/Minecraft-resolution anchor and reject compilation, source, test, network, TLS, DNS, timeout, HTTP-status, rate-limit, or other transient anchors. Create a second ignored checkout at `.work/fabric-example-mod-26.2-fallback` from the same official commit/ref, leave its official `1.17-SNAPSHOT` Loom value intact, require a clean Git diff there, and run fresh captured help/build/resolution-probe commands with zero exits. Mechanically derive `loom_selected: 1.17-SNAPSHOT` from its actual property and `resolved_loom_build` plus Loom artifact SHA-256 from the fresh probe; reject a blank value or literal `1.17-SNAPSHOT` as the concrete resolved build. A failure outside the two eligible categories blocks the phase. Fixed success records both exits zero and `snapshot_fallback_used: false`. Do not alter identity, metadata, sources, Minecraft 26.2, Loader 0.19.3, Fabric API 0.158.0+26.2, the verified JDK, Wrapper 9.5.1, or another pin during either proof.

    After the selected official template passes, copy its root Gradle/wrapper surface byte-for-byte into the repository, including the selected Loom outcome; the seven copied scaffold files must hash-equal the selected retained checkout at Task 1 verification. Do not copy upstream README, LICENSE, CI, source, mixin, icon, or repository history. Compute SHA-256 from both the selected checkout and copied `gradle-wrapper.jar`, require equality, and record that exact 64-hex digest. Validate `distributionUrl` names Gradle 9.5.1; if the official wrapper properties carry `distributionSha256Sum`, validate and record that 64-hex official pin, otherwise record `wrapper_distribution_sha256: absent-in-official-template` without inventing one. Create `01-TOOLCHAIN-EVIDENCE.md` with exact scalar markers for `jdk_artifact_filename`, `jdk_artifact_source`, `jdk_checksum_source`, `jdk_official_sha256`, `jdk_archive_sha256`, normalized `jdk_runtime_version`, `jdk_vendor`, `jdk_vm_vendor`, `jdk_arch`, `jdk_java_sha256`, `jdk_javac_sha256`, `jdk_path_class`, `jdk_path_sha256`, `java_version`, `javac_version`, `template_url`, `template_ref`, `template_commit`, `template_remote_commit`, `template_tree`, `template_diff_mode`, `template_origin_verified`, `gradle_version`, `minecraft_version`, `loader_version`, `fabric_api_version`, `loom_requested`, mechanically derived `loom_selected`, `resolved_loom_build`, `resolved_loom_sha256`, exact fixed help/build/resolution commands, numeric exits, fresh log paths/hashes, `snapshot_fallback_used`, conditional fixed-failure command/exit/category/log/hash and fallback help/build/resolution commands/exits/log hashes, plus `wrapper_distribution`, `wrapper_distribution_sha256`, `wrapper_sha256`, timestamps, and observed anchors. `template_diff_mode` is `fixed-one-file` on fixed success or `snapshot-clean-selected` on eligible fallback. Never record the canonical JDK path, username/home path, secrets, or private identity. Run the machine-derived verification below while the ignored JDK, logs, probe, and checkouts still exist, inspect `git status --short`, and commit exactly this task atomically before beginning Task 2. Retain the verified ignored JDK and ignored Loom probe for every later pre-Plan-03 build; proof checkouts/logs may be removed only after verification/evidence has completed.
  </action>
  <acceptance_criteria>
    - `.gitignore` explicitly unignores `gradle/wrapper/gradle-wrapper.jar`; `git check-ignore` proves it is trackable and proves every representative build/run/private-dirt path is ignored before any repository wrapper use.
    - The official Temurin archive and sidecar agree on SHA-256; machine-derived runtime/vendor/VM-vendor/architecture/home/executable locations and Java/Javac binary hashes prove exact Eclipse Temurin `25.0.4+7` Windows x64 from one canonical ignored JDK root, with only a path class/hash committed.
    - The retained fixed checkout has canonical FabricMC origin, actual branch/ref `26.2`, an exact 40-hex commit, and `git diff --name-only` equal to only `gradle.properties`; any fallback checkout has the same origin/ref/commit and a clean diff at the official snapshot value.
    - The actual complete official template exits 0 for both `help` and `build` on Java 25 with fixed Loom 1.17.19, or evidence records a qualifying fixed-pin nonzero failure followed by zero-exit official `1.17-SNAPSHOT` `help` and `build` with no other pin change.
    - Every initial command and verifier rerun overwrites its own ignored log, binds the numeric exit and classification to that fresh combined stream, rejects transient/network/compile/test fallback causes, and mechanically derives the actual Loom resolved build/artifact hash; file existence, a stale log, literal snapshot alias, or prose cannot pass.
    - Every copied scaffold file hash-equals the selected tested checkout, and both wrapper JAR copies hash to the exact `wrapper_sha256` evidence value; wrapper distribution URL/checksum state is derived from actual properties.
    - The atomic task commit contains only the nine listed repository files, no `src`/identity/content customization, and `git status --short`/`git ls-files` expose no cache, build, run, world, EULA, log, IDE, dump, recording, or local-JDK dirt.
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'
function Get-EvidenceValue([string] $Name) { $match = [regex]::Match($evidence, '(?m)^' + [regex]::Escape($Name) + ':\s*(.+?)\s*$'); if (-not $match.Success) { throw "Evidence marker missing: $Name" }; $match.Groups[1].Value.Trim() }
function Get-EvidenceExit([string] $Name) { $number = 0; $value = Get-EvidenceValue $Name; if (-not [int]::TryParse($value, [ref] $number)) { throw "Evidence exit is not numeric: $Name=$value" }; $number }
function Get-JavaProperty([string] $Text, [string] $Name) { $match = [regex]::Match($Text, '(?m)^\s*' + [regex]::Escape($Name) + '\s*=\s*(.+?)\s*$'); if (-not $match.Success) { throw "Java property missing: $Name" }; $match.Groups[1].Value.Trim() }
function Get-ProbeValue([string] $Text, [string] $Name) { $match = [regex]::Match($Text, '(?m)^DEVELOPERS_HELL_' + [regex]::Escape($Name) + '=(.+?)\s*$'); if (-not $match.Success) { throw "Fresh Loom probe marker missing: $Name" }; $match.Groups[1].Value.Trim() }
function Get-StringSha256([string] $Value) { $sha = [System.Security.Cryptography.SHA256]::Create(); try { ([BitConverter]::ToString($sha.ComputeHash([Text.Encoding]::UTF8.GetBytes($Value)))).Replace('-', '').ToLowerInvariant() } finally { $sha.Dispose() } }
function Invoke-CapturedGradle([string] $Directory, [string[]] $Arguments, [string] $LogPath) { $absoluteLog = [IO.Path]::GetFullPath((Join-Path (Get-Location) $LogPath)); $parent = Split-Path -Parent $absoluteLog; if (-not (Test-Path -LiteralPath $parent)) { New-Item -ItemType Directory -Path $parent | Out-Null }; Remove-Item -LiteralPath $absoluteLog -Force -ErrorAction SilentlyContinue; Push-Location $Directory; try { $lines = @(& .\gradlew.bat @script:GradleJvmArgs @Arguments 2>&1); $exit = $LASTEXITCODE } finally { Pop-Location }; [IO.File]::WriteAllLines($absoluteLog, [string[]] $lines); [pscustomobject]@{ Exit = $exit; Text = ($lines -join "`n"); Log = $absoluteLog } }
$evidence = Get-Content -LiteralPath '.planning/phases/01-java-25-and-fabric-26-2-foundation/01-TOOLCHAIN-EVIDENCE.md' -Raw
$jdkRoot = (Resolve-Path -LiteralPath '.work/toolchain/temurin-25.0.4+7-x64').Path.TrimEnd('\'); $archive = '.work/toolchain/OpenJDK25U-jdk_x64_windows_hotspot_25.0.4_7.zip'; $sidecar = "$archive.sha256.txt"; if (-not (Test-Path -LiteralPath $archive) -or -not (Test-Path -LiteralPath $sidecar)) { throw 'Verified Temurin archive/sidecar is missing' }; $sidecarText = Get-Content -LiteralPath $sidecar -Raw; $sidecarHashes = @([regex]::Matches($sidecarText, '(?i)(?<![0-9a-f])[0-9a-f]{64}(?![0-9a-f])') | ForEach-Object Value | Sort-Object -Unique); if ($sidecarHashes.Count -ne 1) { throw 'Official Temurin sidecar must contain exactly one checksum' }; $archiveHash = (Get-FileHash -LiteralPath $archive -Algorithm SHA256).Hash.ToLowerInvariant(); if ($archiveHash -ne $sidecarHashes[0].ToLowerInvariant()) { throw 'Temurin archive does not match official sidecar' }
$javaExe = Join-Path $jdkRoot 'bin/java.exe'; $javacExe = Join-Path $jdkRoot 'bin/javac.exe'; if (-not (Test-Path -LiteralPath $javaExe) -or -not (Test-Path -LiteralPath $javacExe)) { throw 'Complete verified JDK is missing java.exe or javac.exe' }; $env:JAVA_HOME = $jdkRoot; $env:Path = "$(Join-Path $jdkRoot 'bin');$env:Path"; $script:GradleJvmArgs = @("-Dorg.gradle.java.installations.paths=$jdkRoot", '-Dorg.gradle.java.installations.auto-detect=false', '-Dorg.gradle.java.installations.auto-download=false'); $whereJava = @(& where.exe java); $whereJavac = @(& where.exe javac); if ($whereJava.Count -lt 1 -or $whereJavac.Count -lt 1 -or -not [string]::Equals([IO.Path]::GetFullPath($whereJava[0]), [IO.Path]::GetFullPath($javaExe), [StringComparison]::OrdinalIgnoreCase) -or -not [string]::Equals([IO.Path]::GetFullPath($whereJavac[0]), [IO.Path]::GetFullPath($javacExe), [StringComparison]::OrdinalIgnoreCase)) { throw 'PATH does not select both tools from the verified JDK root' }
$javaProperties = (& $javaExe -XshowSettings:properties -version 2>&1 | Out-String); $runtimeRaw = Get-JavaProperty $javaProperties 'java.runtime.version'; if ($runtimeRaw -notmatch '^25[.]0[.]4\+7(?:-LTS)?$') { throw "Unexpected Java runtime: $runtimeRaw" }; $runtimeNormalized = '25.0.4+7'; $vendor = Get-JavaProperty $javaProperties 'java.vendor'; $vmVendor = Get-JavaProperty $javaProperties 'java.vm.vendor'; $arch = Get-JavaProperty $javaProperties 'os.arch'; $javaHome = (Resolve-Path -LiteralPath (Get-JavaProperty $javaProperties 'java.home')).Path.TrimEnd('\'); if ($vendor -notmatch '(?i)Eclipse Adoptium|Temurin' -or $vmVendor -notmatch '(?i)Eclipse Adoptium|Temurin' -or $arch -notin @('amd64','x86_64') -or -not [string]::Equals($javaHome, $jdkRoot, [StringComparison]::OrdinalIgnoreCase)) { throw 'Java vendor, VM vendor, architecture, or home does not match verified Temurin x64 root' }; $javacVersion = (& $javacExe -version 2>&1 | Out-String).Trim(); if ($javacVersion -notmatch '^javac\s+25[.]0[.]4(?:\s|$)') { throw "Unexpected javac version: $javacVersion" }; $javaHash = (Get-FileHash -LiteralPath $javaExe -Algorithm SHA256).Hash.ToLowerInvariant(); $javacHash = (Get-FileHash -LiteralPath $javacExe -Algorithm SHA256).Hash.ToLowerInvariant(); $jdkPathHash = Get-StringSha256 ($jdkRoot.ToLowerInvariant())
foreach ($pair in @(@('jdk_artifact_filename','OpenJDK25U-jdk_x64_windows_hotspot_25.0.4_7.zip'),@('jdk_artifact_source','https://github.com/adoptium/temurin25-binaries/releases/download/jdk-25.0.4%2B7/OpenJDK25U-jdk_x64_windows_hotspot_25.0.4_7.zip'),@('jdk_checksum_source','https://github.com/adoptium/temurin25-binaries/releases/download/jdk-25.0.4%2B7/OpenJDK25U-jdk_x64_windows_hotspot_25.0.4_7.zip.sha256.txt'),@('jdk_official_sha256',$sidecarHashes[0].ToLowerInvariant()),@('jdk_archive_sha256',$archiveHash),@('jdk_runtime_version',$runtimeNormalized),@('jdk_vendor',$vendor),@('jdk_vm_vendor',$vmVendor),@('jdk_arch',$arch),@('jdk_java_sha256',$javaHash),@('jdk_javac_sha256',$javacHash),@('jdk_path_class','ignored-work-toolchain-child'),@('jdk_path_sha256',$jdkPathHash),@('java_version','25.0.4+7'),@('javac_version','25.0.4'))) { if ((Get-EvidenceValue $pair[0]) -ne $pair[1]) { throw "JDK evidence mismatch: $($pair[0])" } }
$ignored = @('.gradle/probe','build/probe','dist/developers-hell-0.1.0.jar','run/production-client/world/probe','run/production-server/eula.txt','run/production-server/logs/latest.log','.idea/probe','.vscode/settings.json','hs_err_1.log','replay_1.log','local.properties'); & git check-ignore -q --no-index 'gradle/wrapper/gradle-wrapper.jar'; if ($LASTEXITCODE -eq 0) { throw 'Wrapper JAR is ignored' }; if ($LASTEXITCODE -gt 1) { throw 'Wrapper ignore audit failed' }; foreach ($path in $ignored) { & git check-ignore -q --no-index $path; if ($LASTEXITCODE -ne 0) { throw "Expected ignored path: $path" } }
$fixed = '.work/fabric-example-mod-26.2'; if (-not (Test-Path -LiteralPath "$fixed/.git")) { throw 'Retained fixed template checkout missing' }; $origin = (& git -C $fixed remote get-url origin).Trim(); $branch = (& git -C $fixed branch --show-current).Trim(); $commit = (& git -C $fixed rev-parse HEAD).Trim(); $remoteCommit = (& git -C $fixed rev-parse refs/remotes/origin/26.2).Trim(); $tree = (& git -C $fixed rev-parse 'HEAD^{tree}').Trim(); if ($origin -ne 'https://github.com/FabricMC/fabric-example-mod.git' -or $branch -ne '26.2' -or $commit -notmatch '^[0-9a-f]{40}$' -or $remoteCommit -ne $commit -or $tree -notmatch '^[0-9a-f]{40}$') { throw 'Official template origin/ref/commit/tree mismatch' }; & git -C $fixed diff --cached --quiet; if ($LASTEXITCODE -ne 0) { throw 'Fixed checkout contains staged changes' }; & git -C $fixed diff --quiet HEAD -- . ':(exclude)gradle.properties'; if ($LASTEXITCODE -ne 0) { throw 'Fixed checkout changes files beyond gradle.properties' }; $unexpectedUntracked = @(& git -C $fixed ls-files --others --exclude-standard); if ($unexpectedUntracked.Count -ne 0) { throw "Fixed checkout has unignored files: $($unexpectedUntracked -join ',')" }; $fixedDiff = @(& git -C $fixed diff --name-only); if ($fixedDiff.Count -ne 1 -or $fixedDiff[0] -ne 'gradle.properties') { throw "Fixed experiment changed unexpected files: $($fixedDiff -join ',')" }; $loomPatch = @(& git -C $fixed diff --unified=0 HEAD -- gradle.properties | Where-Object { ($_ -match '^[+-]') -and ($_ -notmatch '^(?:---|[+][+][+])') }); if ($loomPatch.Count -ne 2 -or $loomPatch[0] -ne '-loom_version=1.17-SNAPSHOT' -or $loomPatch[1] -ne '+loom_version=1.17.19') { throw "Fixed Loom semantic patch mismatch: $($loomPatch -join ' | ')" }
$baselineProperties = (& git -C $fixed show ($commit + ':gradle.properties') | Out-String); $fixedProperties = Get-Content -LiteralPath "$fixed/gradle.properties" -Raw; $fixedBuild = Get-Content -LiteralPath "$fixed/build.gradle" -Raw; $fixedWrapper = Get-Content -LiteralPath "$fixed/gradle/wrapper/gradle-wrapper.properties" -Raw; foreach ($pattern in @('(?m)^minecraft_version=26[.]2\s*$','(?m)^loader_version=0[.]19[.]3\s*$','(?m)^(?:fabric_version|fabric_api_version)=0[.]158[.]0\+26[.]2\s*$')) { if ($baselineProperties -notmatch $pattern -or $fixedProperties -notmatch $pattern) { throw "Official/fixed property mismatch: $pattern" } }; if ($baselineProperties -notmatch '(?m)^loom_version=1[.]17-SNAPSHOT\s*$' -or $fixedProperties -notmatch '(?m)^loom_version=1[.]17[.]19\s*$') { throw 'Fixed Loom baseline/change mismatch' }; if ($fixedBuild -notmatch '(JavaLanguageVersion[.]of[(]25[)]|JavaVersion[.]VERSION_25|release\s*(?:[.]set[(]|=)\s*25)') { throw 'Official build does not target Java release 25' }; if ($fixedWrapper -notmatch 'distributionUrl=.*gradle-9[.]5[.]1-(?:bin|all)[.]zip') { throw 'Official wrapper distribution is not Gradle 9.5.1' }
foreach ($pair in @(@('template_url','https://github.com/FabricMC/fabric-example-mod.git'),@('template_ref','26.2'),@('template_commit',$commit),@('template_remote_commit',$remoteCommit),@('template_tree',$tree),@('template_origin_verified','true'),@('gradle_version','9.5.1'),@('minecraft_version','26.2'),@('loader_version','0.19.3'),@('fabric_api_version','0.158.0+26.2'),@('loom_requested','1.17.19'),@('fixed_help_command','.\gradlew.bat help --no-daemon --stacktrace --init-script ..\loom-resolution.init.gradle'),@('fixed_build_command','.\gradlew.bat build --no-daemon --stacktrace --init-script ..\loom-resolution.init.gradle'),@('fixed_resolution_command','.\gradlew.bat help --no-daemon --stacktrace --init-script ..\loom-resolution.init.gradle'))) { if ((Get-EvidenceValue $pair[0]) -ne $pair[1]) { throw "Evidence mismatch: $($pair[0])" } }
$probePath = '.work/loom-resolution.init.gradle'; if (-not (Test-Path -LiteralPath $probePath)) { throw 'Ignored Loom resolution probe missing' }; $probeText = Get-Content -LiteralPath $probePath -Raw; if ($probeText -notmatch 'protectionDomain|codeSource|resolvedArtifacts|implementationVersion' -or $probeText -match '(?m)^\s*(?:def|String)\s+resolved\s*=\s*["''](?:1[.]17[.]19|1[.]17-SNAPSHOT)') { throw 'Loom probe must derive plugin resolution rather than echo a configured value' }
$fixedHelp = Invoke-CapturedGradle $fixed @('help','--no-daemon','--stacktrace','--init-script','..\loom-resolution.init.gradle') '.work/verify-fixed-help.log'; $fixedBuildRun = Invoke-CapturedGradle $fixed @('build','--no-daemon','--stacktrace','--init-script','..\loom-resolution.init.gradle') '.work/verify-fixed-build.log'; if ((Get-EvidenceExit 'fixed_help_exit') -ne $fixedHelp.Exit -or (Get-EvidenceExit 'fixed_build_exit') -ne $fixedBuildRun.Exit) { throw 'Fixed command exit evidence does not match fresh captured rerun' }
$snapshotFallback = Get-EvidenceValue 'snapshot_fallback_used'; if ($snapshotFallback -notin @('true','false')) { throw 'snapshot_fallback_used must be true or false' }; $selected = $fixed; if ($snapshotFallback -eq 'false') { $fixedResolution = Invoke-CapturedGradle $fixed @('help','--no-daemon','--stacktrace','--init-script','..\loom-resolution.init.gradle') '.work/verify-fixed-resolution.log'; foreach ($run in @($fixedHelp,$fixedBuildRun,$fixedResolution)) { if ((Get-ProbeValue $run.Text 'LOOM_SELECTED') -ne '1.17.19' -or (Get-ProbeValue $run.Text 'LOOM_RESOLVED') -ne '1.17.19' -or (Get-ProbeValue $run.Text 'LOOM_ARTIFACT_SHA256') -ne (Get-EvidenceValue 'resolved_loom_sha256')) { throw 'A fixed Loom invocation did not resolve the evidenced artifact' } }; if ($fixedHelp.Exit -ne 0 -or $fixedBuildRun.Exit -ne 0 -or $fixedResolution.Exit -ne 0 -or (Get-EvidenceExit 'fixed_resolution_exit') -ne 0 -or (Get-EvidenceValue 'loom_selected') -ne '1.17.19' -or (Get-EvidenceValue 'resolved_loom_build') -ne '1.17.19' -or (Get-EvidenceValue 'template_diff_mode') -ne 'fixed-one-file') { throw 'Fixed Loom success/resolution contract failed' } } else { $failureExit = Get-EvidenceExit 'fixed_failure_exit'; $failureCommand = Get-EvidenceValue 'fixed_failure_command'; $failureCategory = Get-EvidenceValue 'fixed_failure_category'; $freshFailure = if ($failureCommand -eq (Get-EvidenceValue 'fixed_help_command')) { $fixedHelp } elseif ($failureCommand -eq (Get-EvidenceValue 'fixed_build_command')) { $fixedBuildRun } else { throw 'Fixed failure command is not an exact attempted command' }; if ($failureExit -eq 0 -or $freshFailure.Exit -ne $failureExit -or $failureCategory -notin @('plugin-resolution','minecraft-setup')) { throw 'Ineligible fixed Loom fallback evidence/current exit' }; $failureText = $freshFailure.Text; if ($failureText -match '(?i)compilation failed|compilation error|there were failing tests|test[s]? failed|timed?\s*out|connection\s+(?:reset|refused)|unknown\s+host|could not\s+(?:get|head)|network|TLS|SSL|PKIX|HTTP\s+(?:429|5\d\d)|rate.?limit') { throw 'Compile/test/network/transient failure is not eligible for Loom fallback' }; if (($failureCategory -eq 'plugin-resolution' -and $failureText -notmatch '(?i)plugin.*(?:not found|resolve|resolution)|could not resolve.*plugin|UnknownPluginException') -or ($failureCategory -eq 'minecraft-setup' -and $failureText -notmatch '(?i)(?:loom|minecraft).*(?:setup|provision|resolve)|failed to.*(?:setup|provide).*(?:loom|minecraft)')) { throw 'Fresh fixed-failure stream does not support the eligible category' }; if ((Get-EvidenceValue 'fixed_failure_log') -ne '.work/fixed-loom-failure.log' -or (Get-EvidenceValue 'fixed_failure_log_sha256') -notmatch '^[0-9a-fA-F]{64}$') { throw 'Initial fixed failure evidence lacks its same-run deterministic log/hash' }; $fallback = '.work/fabric-example-mod-26.2-fallback'; if (-not (Test-Path -LiteralPath "$fallback/.git")) { throw 'Retained snapshot fallback checkout missing' }; $fallbackOrigin = (& git -C $fallback remote get-url origin).Trim(); $fallbackBranch = (& git -C $fallback branch --show-current).Trim(); $fallbackCommit = (& git -C $fallback rev-parse HEAD).Trim(); $fallbackRemoteCommit = (& git -C $fallback rev-parse refs/remotes/origin/26.2).Trim(); & git -C $fallback diff --cached --quiet; $fallbackCachedExit = $LASTEXITCODE; $fallbackDiff = @(& git -C $fallback diff --name-only); $fallbackUntracked = @(& git -C $fallback ls-files --others --exclude-standard); if ($fallbackOrigin -ne $origin -or $fallbackBranch -ne '26.2' -or $fallbackCommit -ne $commit -or $fallbackRemoteCommit -ne $commit -or $fallbackCachedExit -ne 0 -or $fallbackDiff.Count -ne 0 -or $fallbackUntracked.Count -ne 0 -or (Get-EvidenceValue 'template_diff_mode') -ne 'snapshot-clean-selected') { throw 'Fallback checkout provenance or clean-tree mismatch' }; $fallbackProperties = Get-Content -LiteralPath "$fallback/gradle.properties" -Raw; if ($fallbackProperties -notmatch '(?m)^loom_version=1[.]17-SNAPSHOT\s*$') { throw 'Fallback checkout does not use official snapshot value' }; foreach ($pair in @(@('fallback_help_command','.\gradlew.bat help --no-daemon --stacktrace --init-script ..\loom-resolution.init.gradle'),@('fallback_build_command','.\gradlew.bat build --no-daemon --stacktrace --init-script ..\loom-resolution.init.gradle'),@('fallback_resolution_command','.\gradlew.bat help --no-daemon --stacktrace --init-script ..\loom-resolution.init.gradle'),@('loom_selected','1.17-SNAPSHOT'))) { if ((Get-EvidenceValue $pair[0]) -ne $pair[1]) { throw "Fallback evidence mismatch: $($pair[0])" } }; $fallbackHelp = Invoke-CapturedGradle $fallback @('help','--no-daemon','--stacktrace','--init-script','..\loom-resolution.init.gradle') '.work/verify-fallback-help.log'; $fallbackBuildRun = Invoke-CapturedGradle $fallback @('build','--no-daemon','--stacktrace','--init-script','..\loom-resolution.init.gradle') '.work/verify-fallback-build.log'; $fallbackResolution = Invoke-CapturedGradle $fallback @('help','--no-daemon','--stacktrace','--init-script','..\loom-resolution.init.gradle') '.work/verify-fallback-resolution.log'; $probeSelected = Get-ProbeValue $fallbackResolution.Text 'LOOM_SELECTED'; $probeResolved = Get-ProbeValue $fallbackResolution.Text 'LOOM_RESOLVED'; $probeArtifactHash = Get-ProbeValue $fallbackResolution.Text 'LOOM_ARTIFACT_SHA256'; foreach ($run in @($fallbackHelp,$fallbackBuildRun,$fallbackResolution)) { if ((Get-ProbeValue $run.Text 'LOOM_SELECTED') -ne '1.17-SNAPSHOT' -or (Get-ProbeValue $run.Text 'LOOM_RESOLVED') -ne $probeResolved -or (Get-ProbeValue $run.Text 'LOOM_ARTIFACT_SHA256') -ne $probeArtifactHash) { throw 'A snapshot Loom invocation did not resolve the same frozen artifact' } }; if ($fallbackHelp.Exit -ne 0 -or $fallbackBuildRun.Exit -ne 0 -or $fallbackResolution.Exit -ne 0 -or (Get-EvidenceExit 'fallback_help_exit') -ne 0 -or (Get-EvidenceExit 'fallback_build_exit') -ne 0 -or (Get-EvidenceExit 'fallback_resolution_exit') -ne 0 -or $probeSelected -ne '1.17-SNAPSHOT' -or $probeResolved -eq '1.17-SNAPSHOT' -or $probeResolved -notmatch '^(?:1[.]17-[0-9]{8}[.][0-9]{6}-[0-9]+|1[.]17[.][0-9]+(?:[-+][0-9A-Za-z.-]+)?|[0-9a-f]{7,40})$' -or $probeArtifactHash -notmatch '^[0-9a-fA-F]{64}$' -or (Get-EvidenceValue 'resolved_loom_build') -ne $probeResolved -or (Get-EvidenceValue 'resolved_loom_sha256') -ne $probeArtifactHash) { throw 'Snapshot fallback fresh command/concrete-resolution contract failed' }; $selected = $fallback }
$scaffold = @('settings.gradle','gradle.properties','build.gradle','gradlew','gradlew.bat','gradle/wrapper/gradle-wrapper.jar','gradle/wrapper/gradle-wrapper.properties'); foreach ($relative in $scaffold) { $upstreamPath = Join-Path $selected $relative; if (-not (Test-Path -LiteralPath $relative) -or (Get-FileHash -LiteralPath $upstreamPath -Algorithm SHA256).Hash -ne (Get-FileHash -LiteralPath $relative -Algorithm SHA256).Hash) { throw "Copied scaffold differs from selected tested checkout: $relative" } }; $wrapperHash = (Get-FileHash -LiteralPath 'gradle/wrapper/gradle-wrapper.jar' -Algorithm SHA256).Hash; if ($wrapperHash -notmatch '^[0-9A-Fa-f]{64}$' -or $wrapperHash -ne (Get-EvidenceValue 'wrapper_sha256') -or $wrapperHash -ne (Get-FileHash -LiteralPath (Join-Path $selected 'gradle/wrapper/gradle-wrapper.jar') -Algorithm SHA256).Hash) { throw 'Wrapper JAR checksum evidence mismatch' }; $selectedWrapper = Get-Content -LiteralPath (Join-Path $selected 'gradle/wrapper/gradle-wrapper.properties') -Raw; $checksumMatch = [regex]::Match($selectedWrapper, '(?m)^distributionSha256Sum=([0-9A-Fa-f]{64})\s*$'); $recordedDistributionChecksum = Get-EvidenceValue 'wrapper_distribution_sha256'; if (($checksumMatch.Success -and $recordedDistributionChecksum -ne $checksumMatch.Groups[1].Value) -or (-not $checksumMatch.Success -and $recordedDistributionChecksum -ne 'absent-in-official-template')) { throw 'Wrapper distribution checksum-pin evidence mismatch' }; if ((Get-EvidenceValue 'wrapper_distribution') -notmatch 'gradle-9[.]5[.]1-(?:bin|all)[.]zip') { throw 'Recorded wrapper distribution mismatch' }
$dirt = & git status --short --untracked-files=all; if ($dirt -match '(?m)(?:^| )(?:[.]gradle/|build/|run/|[.]work/|[.]idea/|[.]vscode/|.*eula[.]txt|.*logs/)') { throw "Build/run dirt visible to Git: $dirt" }</automated>
  </verify>
  <done>The actual untouched official template is proven and exactly evidenced, repository hygiene protects the wrapper and excludes generated/private dirt, and the nine-file prerequisite is committed atomically before customization.</done>
</task>

<task type="tracer">
  <name>Task 2: Complete the walking skeleton with one registered client-visible Foundation Token</name>
  <reversibility rating="costly">The public mod ID, artifact, package, version, entrypoints, and first content ID become save/resource/build contracts for all later phases.</reversibility>
  <read_first>
    - `settings.gradle`, `gradle.properties`, `build.gradle` — preserve Task 1's proven tuple/Loom result while applying only the final identity and split-source configuration.
    - `src/main/java/dev/developershell/DevelopersHell.java`, `src/client/java/dev/developershell/client/DevelopersHellClient.java` — create if absent; these are the common/client tracer endpoints.
    - `src/main/resources/fabric.mod.json`, `src/main/resources/assets/developers_hell/items/foundation_token.json`, `src/main/resources/assets/developers_hell/models/item/foundation_token.json` — create the minimal current 26.2 metadata/resource path.
    - `.planning/phases/01-java-25-and-fabric-26-2-foundation/01-TOOLCHAIN-EVIDENCE.md` — authoritative Loom outcome and tuple that this task may not re-decide.
    - `.planning/phases/01-java-25-and-fabric-26-2-foundation/01-CONTEXT.md` — locked identity, side split, unconditional-registration, fictional-default, and dependency boundaries.
    - `.planning/phases/01-java-25-and-fabric-26-2-foundation/01-RESEARCH.md` and `01-PATTERNS.md` — current no-remap ordinary-JAR, Loader metadata, item registration, and two-JSON client resource patterns.
  </read_first>
  <files>settings.gradle, gradle.properties, build.gradle, src/main/java/dev/developershell/DevelopersHell.java, src/client/java/dev/developershell/client/DevelopersHellClient.java, src/main/resources/fabric.mod.json, src/main/resources/assets/developers_hell/items/foundation_token.json, src/main/resources/assets/developers_hell/models/item/foundation_token.json</files>
  <action>
    This is the walking-skeleton tracer and must complete before Plan 01 ends. Re-select deterministic ignored `.work/toolchain/temurin-25.0.4+7-x64`, recheck its path hash and Java/Javac binary hashes against toolchain evidence, set task-local `JAVA_HOME`/`PATH`, disable Gradle toolchain auto-detection/download, and pass that exact root as the only Gradle Java installation for every wrapper invocation. Adapt the proven root files to exact identity `developers_hell`, artifact/root name `developers-hell`, group/package `dev.developershell`, version `0.1.0`, and Installer `1.1.2` without changing Minecraft 26.2, Loader 0.19.3, Fabric API 0.158.0+26.2, Wrapper 9.5.1, exact Temurin `25.0.4+7`, or the evidenced Loom outcome. Configure official `splitEnvironmentSourceSets()` with common and client sources in one mod. Use `net.fabricmc.fabric-loom`, unobfuscated names, the ordinary `jar`, an empty project repositories block, and direct runtime dependencies only for Minecraft, Fabric Loader, and Fabric API. Preserve the official Jar task's license rename so the production archive contains exactly `LICENSE_developers-hell`.

    Create public-safe `fabric.mod.json` with `environment: \"*\"`, main/client entrypoints, Minecraft `~26.2`, Java `>=25`, Loader `>=0.19.3`, Fabric API, and Unlicense; omit template mixins and missing icons. In the single common `DevelopersHell` file, expose `MOD_ID`, Loader-provided SLF4J, `id(String)`, `FOUNDATION_TOKEN_ID`, and `FOUNDATION_TOKEN`; perform the one current-26.2 `Registry.register` path unconditionally during common initialization with no gate/toggle. Add no speculative IDs. Create a separate no-authority `DevelopersHellClient` only under `src/client`.

    Add both required 26.2 client item/model JSON files so `/give @s developers_hell:foundation_token` renders through `developers_hell:item/foundation_token` using vanilla paper. Translation polish moves to Plan 02, but the tracer must not fake visibility: build the ordinary JAR and assert both resource files and both entrypoint classes are packaged. Run repository `help` only after Task 2 source/metadata exists, then run the first repository `build` with the retained ignored Loom-resolution probe attached to that exact invocation and a freshly overwritten log. Derive its actual selected/resolved Loom build and artifact SHA-256, and fail unless they equal `loom_selected`, `resolved_loom_build`, and `resolved_loom_sha256` from toolchain evidence; a configured snapshot alias is never sufficient. Fail on example/mixin/Yarn/remap residue, wrong pins, any physical-client reference under `src/main`, a runtime network/telemetry/OpenAI dependency declaration, or Git-visible build/run dirt. Commit this tracer as the second atomic task commit in Plan 01.
  </action>
  <acceptance_criteria>
    - Plan 01 ends with a real ordinary `build/libs/developers-hell-0.1.0.jar`, not a scaffold-only checkpoint.
    - Every repository wrapper command runs on the exact checksum-bound Temurin `25.0.4+7` root from Task 1, and the ordinary JAR contains exact official-rule license entry `LICENSE_developers-hell` rather than `LICENSE`.
    - The exact tracer build's fresh probe derives the same concrete Loom build and artifact SHA-256 recorded in toolchain evidence, for both fixed and snapshot-selected paths.
    - Exact final identity and tuple are present; no Yarn/mappings/remap plugin, template mixin/example residue, or extra direct runtime library exists.
    - `DevelopersHell` registers exactly `developers_hell:foundation_token` unconditionally; the separate client initializer exists only in `src/client`.
    - The ordinary JAR contains `fabric.mod.json`, both entrypoint classes, and both current 26.2 Foundation Token item/model JSON files, enabling a real `/give`-visible item path.
    - Wrapper JAR remains trackable and `git status --short`/`git ls-files` show no generated build/run/private dirt after `help` and `build`.
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'
$jdkRoot = (Resolve-Path -LiteralPath '.work/toolchain/temurin-25.0.4+7-x64').Path; $env:JAVA_HOME = $jdkRoot; $env:Path = "$(Join-Path $jdkRoot 'bin');$env:Path"; $jvmArgs = @("-Dorg.gradle.java.installations.paths=$jdkRoot", '-Dorg.gradle.java.installations.auto-detect=false', '-Dorg.gradle.java.installations.auto-download=false'); $evidence = Get-Content -LiteralPath '.planning/phases/01-java-25-and-fabric-26-2-foundation/01-TOOLCHAIN-EVIDENCE.md' -Raw; foreach ($tool in @('java','javac')) { $exe = Join-Path $jdkRoot "bin/$tool.exe"; $hash = (Get-FileHash -LiteralPath $exe -Algorithm SHA256).Hash; $match = [regex]::Match($evidence, '(?m)^jdk_' + $tool + '_sha256:\s*([0-9a-fA-F]{64})\s*$'); if (-not $match.Success -or $hash -ne $match.Groups[1].Value) { throw "Verified $tool binary changed" } }; $sha = [Security.Cryptography.SHA256]::Create(); try { $pathHash = ([BitConverter]::ToString($sha.ComputeHash([Text.Encoding]::UTF8.GetBytes($jdkRoot.ToLowerInvariant())))).Replace('-','').ToLowerInvariant() } finally { $sha.Dispose() }; $pathMarker = [regex]::Match($evidence, '(?m)^jdk_path_sha256:\s*([0-9a-f]{64})\s*$'); if (-not $pathMarker.Success -or $pathHash -ne $pathMarker.Groups[1].Value) { throw 'Verified JDK canonical path hash changed' }
& .\gradlew.bat @jvmArgs help --no-daemon --stacktrace; if ($LASTEXITCODE -ne 0) { throw "Repository help failed with exit $LASTEXITCODE" }
$loomProbe = '.work/loom-resolution.init.gradle'; if (-not (Test-Path -LiteralPath $loomProbe)) { throw 'Retained Loom resolution probe missing' }; $buildLog = '.work/tracer-build-loom.log'; Remove-Item -LiteralPath $buildLog -Force -ErrorAction SilentlyContinue; $buildLines = @(& .\gradlew.bat @jvmArgs build --no-daemon --stacktrace --init-script $loomProbe 2>&1); $buildExit = $LASTEXITCODE; [IO.File]::WriteAllLines([IO.Path]::GetFullPath($buildLog), [string[]]$buildLines); if ($buildExit -ne 0) { throw "Repository build failed with exit $buildExit" }; $buildText = $buildLines -join "`n"; function Probe([string]$name) { $m=[regex]::Match($buildText,'(?m)^DEVELOPERS_HELL_'+[regex]::Escape($name)+'=(.+?)\s*$');if(-not$m.Success){throw "Build Loom marker missing: $name"};$m.Groups[1].Value.Trim() }; function Evidence([string]$name) { $m=[regex]::Match($evidence,'(?m)^'+[regex]::Escape($name)+':\s*(.+?)\s*$');if(-not$m.Success){throw "Evidence marker missing: $name"};$m.Groups[1].Value.Trim() }; $configured=[regex]::Match((Get-Content -LiteralPath 'gradle.properties' -Raw),'(?m)^loom_version=(.+?)\s*$').Groups[1].Value; if ((Probe 'LOOM_SELECTED') -ne $configured -or (Probe 'LOOM_RESOLVED') -ne (Evidence 'resolved_loom_build') -or (Probe 'LOOM_ARTIFACT_SHA256') -ne (Evidence 'resolved_loom_sha256')) { throw 'Tracer build did not resolve the frozen evidenced Loom artifact' }; if (($configured -eq '1.17.19' -and (Probe 'LOOM_RESOLVED') -ne '1.17.19') -or ($configured -eq '1.17-SNAPSHOT' -and (Probe 'LOOM_RESOLVED') -eq '1.17-SNAPSHOT')) { throw 'Tracer Loom resolution is not concrete' }
$jar = 'build/libs/developers-hell-0.1.0.jar'; if (-not (Test-Path -LiteralPath $jar)) { throw 'Ordinary production JAR missing' }; $entries = @(& jar --list --file $jar); if ($LASTEXITCODE -ne 0) { throw "JAR listing failed with exit $LASTEXITCODE" }; foreach ($entry in @('fabric.mod.json','LICENSE_developers-hell','dev/developershell/DevelopersHell.class','dev/developershell/client/DevelopersHellClient.class','assets/developers_hell/items/foundation_token.json','assets/developers_hell/models/item/foundation_token.json')) { if ($entries -notcontains $entry) { throw "Missing JAR entry: $entry" } }; if (@($entries | Where-Object { $_ -match '^LICENSE(?:_|$)' }).Count -ne 1) { throw 'Production JAR must contain exactly one renamed license entry' }
$bad = rg -n 'net[.]minecraft[.]client|com[.]mojang[.]blaze3d|net[.]fabricmc[.]fabric-loom-remap|(^|\s)mappings\s|yarn|com/example|example-mod|modid[.]mixins' src/main build.gradle settings.gradle gradle.properties src/main/resources; if ($LASTEXITCODE -eq 0) { throw "Forbidden tracer residue: $bad" }; if ($LASTEXITCODE -gt 1) { throw 'Tracer audit failed' }
& git check-ignore -q --no-index 'gradle/wrapper/gradle-wrapper.jar'; if ($LASTEXITCODE -eq 0) { throw 'Wrapper JAR became ignored' }; if ($LASTEXITCODE -gt 1) { throw 'Wrapper ignore audit failed' }; $dirt = & git status --short --untracked-files=all; if ($dirt -match '(?m)(?:^| )(?:[.]gradle/|build/|run/|[.]work/|[.]idea/|[.]vscode/|.*eula[.]txt|.*logs/)') { throw "Build/run dirt visible to Git: $dirt" }</automated>
  </verify>
  <done>The first plan finishes with the proven wrapper-to-common-registration-to-client-resource ordinary-JAR tracer, after preserving the untouched scaffold as its own earlier atomic commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Official sources → ignored proof checkout/cache | JDK, Gradle, Loom, Loader, Fabric API, and Minecraft inputs enter locally before pins are trusted. |
| Ignored proof checkout → atomic scaffold commit | Only verified first-party root/wrapper files and public evidence may cross. |
| Loader metadata → common/client code | Entrypoint and source placement control dedicated-server linkage safety. |
| Public metadata/runtime → player | Public defaults must remain fictional and runtime behavior must remain local. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Tampering | JDK, wrapper, and Fabric dependencies | high | mitigate | Official Temurin release ZIP/sidecar equality, machine-derived runtime/vendor/arch/home plus executable/path hashes, first-party template origin/commit, exact pins, fresh captured help/build/Loom-resolution proof, wrapper integrity, and evidence validation. |
| T-01-02 | Information Disclosure | local proof/cache/run paths | high | mitigate | Update ignore rules before wrapper use, forbid local paths/private data in evidence, and audit Git status/index after every build. |
| T-01-03 | Repudiation | fixed-Loom fallback | medium | mitigate | Overwrite a deterministic log per current invocation, bind exit/category to that same stream, reject compile/test/network/transient causes, and mechanically derive exact fixed or concrete snapshot plugin build/artifact hash. |
| T-01-04 | Denial of Service | common/client boundary | high | mitigate | Official split source sets, distinct entrypoints, common-source audit, later GameTest, and production-server proof. |
| T-01-05 | Tampering | stable registry identity | high | mitigate | One unconditional registration with no behavior gate; Plan 02 extracts and tests the stable catalog. |
| T-01-06 | Information Disclosure | runtime/public metadata | high | mitigate | Only approved direct runtime dependencies, fictional metadata, and comprehensive network/SDK audits in Plans 03–04. |
</threat_model>

## Artifacts this phase produces

- `.gitignore` — explicit wrapper-JAR negation and cache/build/run/world/EULA/log/IDE/dump/recording/local-setting exclusions.
- `settings.gradle`, `gradle.properties`, `build.gradle` — evidenced official build surface adapted to final identity, split sources, Java 25, exact tuple, and ordinary-JAR output.
- `gradlew`, `gradlew.bat`, `gradle/wrapper/gradle-wrapper.jar`, `gradle/wrapper/gradle-wrapper.properties` — committed Gradle 9.5.1 wrapper.
- `01-TOOLCHAIN-EVIDENCE.md` — exact official Temurin archive/sidecar/runtime/vendor/arch/executable/path hashes plus source/ref/commit, tuple, fresh command logs/exits, wrapper integrity, and mechanically resolved fixed/fallback Loom result.
- `DevelopersHell` — `MOD_ID`, `LOGGER`, `id(String)`, inline `FOUNDATION_TOKEN_ID`/`FOUNDATION_TOKEN`, and unconditional `onInitialize()` tracer registration.
- `DevelopersHellClient` — isolated `onInitializeClient()`.
- `fabric.mod.json`, `items/foundation_token.json`, `models/item/foundation_token.json` — Loader and real current-26.2 client resource path.

<verification>
- Task 1 independently proves and commits hygiene plus the actual untouched official-template `help`/`build` result before source customization.
- Task 2 performs the first repository `help`/`build`, ordinary-JAR archive inspection, side/residue checks, and Git dirt audit.
</verification>

<success_criteria>
- The first plan contains the locked atomic scaffold prerequisite and then delivers the real registered client-visible item tracer before completion.
- Every wrapper/build fact is exact and fail-closed; no generated/private state becomes trackable.
- The final identity, side split, ordinary output, and unconditional Foundation Token ID are ready for registry extraction and unit testing.
</success_criteria>

<output>Create `.planning/phases/01-java-25-and-fabric-26-2-foundation/01-01-SUMMARY.md` when done.</output>
