---
phase: 01-java-25-and-fabric-26-2-foundation
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - build.gradle
  - src/gametest/java/dev/developershell/gametest/FoundationGameTests.java
  - src/gametest/resources/fabric.mod.json
  - scripts/loom-resolution.init.gradle
  - scripts/audit-foundation.ps1
  - scripts/verify-foundation.ps1
autonomous: true
requirements: [FND-02, FND-03, FND-04]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Wrapper `build` now owns a fail-first server GameTest that resolves the live Foundation Token registry key exactly."
    - "Separate production client/server tasks launch the ordinary project JAR from the checksum-bound Eclipse Temurin 25.0.4+7 root with exact Fabric API production runtime support and isolated ignored run directories."
    - "One fail-closed audit covers physical-side linkage, runtime network/remote-service surfaces across both main and client source sets, exact direct dependency scopes, official repositories, forbidden shaded SDK namespaces, and Git hygiene without rejecting expected transitive Minecraft/Fabric modules."
    - "The final verification harness is committed before Plan 04 and is capable of proving FND-02 from a disposable detached clean project worktree at HEAD rather than the dirty implementation workspace."
  artifacts:
    - path: "build.gradle"
      provides: "Server GameTest, production launch tasks, productionRuntimeMods, and model-based direct-dependency audit task"
      exports: ["auditDirectDependencies", "runProductionClient", "runProductionServer"]
    - path: "src/gametest/java/dev/developershell/gametest/FoundationGameTests.java"
      provides: "Live registry identity proof"
      exports: ["FoundationGameTests", "foundationTokenIsRegistered", "invokeTestMethod"]
    - path: "src/gametest/resources/fabric.mod.json"
      provides: "Server-only fabric-gametest entrypoint"
    - path: "scripts/audit-foundation.ps1"
      provides: "Reusable comprehensive side/network/dependency/repository/archive/Git audit"
      exports: ["SourceAndDependencies", "JarPath", "EvidencePath"]
    - path: "scripts/loom-resolution.init.gradle"
      provides: "Committed mechanical Loom plugin component/version/code-source SHA probe used by every later build including detached online/offline builds"
    - path: "scripts/verify-foundation.ps1"
      provides: "Precommitted clean-worktree, artifact/hash, production-runtime, isolation, and evidence harness"
      exports: ["SelfCheck", "PrimeAndCompare", "RunServerSmoke", "ClientPreflight", "SuperviseInteractiveUat", "ValidateEvidence", "RequireUatPass", "EvidencePath", "SessionDirectory", "SessionPointerPath", "SessionReceiptPath", "DistributionPath"]
  key_links:
    - from: "src/gametest/resources/fabric.mod.json"
      to: "FoundationGameTests"
      via: "fabric-gametest entrypoint"
      pattern: "FoundationGameTests"
    - from: "FoundationGameTests"
      to: "ModItems.FOUNDATION_TOKEN"
      via: "BuiltInRegistries.ITEM live lookup"
      pattern: "FOUNDATION_TOKEN"
    - from: "scripts/audit-foundation.ps1"
      to: "auditDirectDependencies"
      via: "wrapper invocation of model-based direct declaration allowlist"
      pattern: "auditDirectDependencies"
    - from: "runProductionServer"
      to: "productionRuntimeMods"
      via: "ordinary project JAR plus exact Fabric API runtime"
      pattern: "productionRuntimeMods"
    - from: "scripts/verify-foundation.ps1"
      to: "detached project worktree at HEAD"
      via: "tracked-file audit, committed wrapper online prime, same-cache offline rebuild, and guaranteed cleanup"
      pattern: "git worktree add --detach"
    - from: "detached clean-checkout ordinary JAR"
      to: "dist/developers-hell-0.1.0.jar"
      via: "copy before exact worktree removal plus online/offline/distribution SHA-256 equality"
      pattern: "distribution_sha256"
  flagged_assumptions: []
  prohibitions: []
---

# Phase 1 Plan 03 — Live GameTest, Production Tasks, and Offline-Surface Audit

## Phase Goal

**As a** player or contributor, **I want to** install, launch, and rebuild one frozen Fabric 26.2 JAR on client and dedicated server, **so that** Developer's Hell remains playable offline after its official dependencies are primed.

<objective>
Connect the tested registry/gate slice to a real Minecraft server test, production launch tasks, and a reusable comprehensive offline-security audit.

Purpose: Prove the registry in-runtime and eliminate side-linkage, hidden network/SDK, dependency, repository, and artifact ambiguity before final launches.
Output: GameTest wiring, production client/server tasks, direct-dependency model audit, comprehensive audit script, and the fully committed verification harness Plan 04 executes unchanged.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.agents/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-java-25-and-fabric-26-2-foundation/01-CONTEXT.md
@.planning/phases/01-java-25-and-fabric-26-2-foundation/01-RESEARCH.md
@.planning/phases/01-java-25-and-fabric-26-2-foundation/01-PATTERNS.md
@.planning/phases/01-java-25-and-fabric-26-2-foundation/01-VALIDATION.md
@.planning/phases/01-java-25-and-fabric-26-2-foundation/01-02-SUMMARY.md

<interfaces>
- Consume exact `DevelopersHell.id(String)`, `ModItemIds.FOUNDATION_TOKEN`, `ModItems.FOUNDATION_TOKEN`, and unconditional `ModItems.initialize()` from Plan 02.
- Preserve direct declarations: Minecraft `com.mojang:minecraft:26.2`, implementation Loader `0.19.3`, implementation Fabric API `0.158.0+26.2`, test-only Loader JUnit `0.19.3`, and productionRuntimeMods Fabric API `0.158.0+26.2`; expected transitive Minecraft/Fabric modules are reported but are not direct-declaration failures.
- `runProductionClient` and `runProductionServer` use the exact checksum-bound Eclipse Temurin `25.0.4+7` launcher selected through task-local `JAVA_HOME` plus the sole Gradle installation path, separate `run/production-*` directories, the ordinary project `jar`, and Installer `1.1.2` for server setup.
- `scripts/audit-foundation.ps1 -SourceAndDependencies [-JarPath <jar>] [-EvidencePath <path>]` is the single reusable audit entrypoint called again by Plan 04.
- `scripts/verify-foundation.ps1 -SelfCheck|-PrimeAndCompare|-RunServerSmoke|-ClientPreflight|-SuperviseInteractiveUat|-ValidateEvidence [-RequireUatPass] [-EvidencePath <path>] [-SessionDirectory <path>] [-SessionPointerPath <path>] [-SessionReceiptPath <path>] [-DistributionPath <path>]` is complete and committed in this plan so Plan 04 automation can supervise both visible UAT clients while the checkpoint only observes gameplay.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire fail-first GameTest and production tasks behind the comprehensive offline-surface audit</name>
  <read_first>
    - `build.gradle` — preserve exact tuple, split sources, ordinary JAR, and Loader JUnit; add GameTest, production tasks, and the direct-dependency audit task.
    - `src/gametest/java/dev/developershell/gametest/FoundationGameTests.java`, `src/gametest/resources/fabric.mod.json` — create if absent from current official 26.2 testing patterns.
    - `scripts/loom-resolution.init.gradle`, `scripts/audit-foundation.ps1`, `scripts/verify-foundation.ps1` — commit the mechanical Loom probe plus reusable audit/harness before Plan 04 invokes a clean project checkout or interactive isolated client.
    - `settings.gradle`, `gradle.properties`, `.gitignore`, `src/main`, and `src/client` — sources of truth for repositories, tuple, hygiene, side split, and network/SDK audit scope.
    - `.planning/phases/01-java-25-and-fabric-26-2-foundation/01-TOOLCHAIN-EVIDENCE.md` and `.work/toolchain/temurin-25.0.4+7-x64` — checksum-bound runtime, executable hashes, redacted path hash, and retained exact JDK used by every wrapper/production process.
    - `src/main/java/dev/developershell/registry/ModItemIds.java`, `ModItems.java`, and `DevelopersHell.java` — exact live key and initialization interfaces under test.
    - `.planning/phases/01-java-25-and-fabric-26-2-foundation/01-CONTEXT.md` — production-side, no-network/SDK, exact-dependency, and unconditional-registration decisions.
    - `.planning/phases/01-java-25-and-fabric-26-2-foundation/01-RESEARCH.md`, `01-PATTERNS.md`, and `01-VALIDATION.md` — official GameTest/production DSL, dependency policy, full audit patterns, and task 01-03-01 proof.
    - `.planning/phases/01-java-25-and-fabric-26-2-foundation/01-02-SUMMARY.md` — actual registry/gate symbols and tests.
  </read_first>
  <files>build.gradle, src/gametest/java/dev/developershell/gametest/FoundationGameTests.java, src/gametest/resources/fabric.mod.json, scripts/loom-resolution.init.gradle, scripts/audit-foundation.ps1, scripts/verify-foundation.ps1</files>
  <action>
    Before any wrapper or production task, re-select `.work/toolchain/temurin-25.0.4+7-x64`, verify its canonical-path hash plus Java/Javac binary hashes against toolchain evidence, set task-local `JAVA_HOME`/`PATH`, disable Gradle Java-toolchain auto-detection/download, and pass this root as the sole installation path. Commit `scripts/loom-resolution.init.gradle` as the one fail-closed probe that emits the configured Loom spelling, concrete resolved plugin component/version, exact Loom implementation artifact selected by that invocation, and its SHA-256. It must reject ambiguous/multiple artifacts, a literal unresolved snapshot alias, or missing markers. Every build in this task—including the deliberate failing GameTest build and the restored build—must delete its prior deterministic log, capture that invocation's combined stdout/stderr and numeric exit into a fresh ignored log, attach this probe, derive all three Loom values from that same stream/cache resolution, and require exact equality with `loom_selected`, `resolved_loom_build`, and `resolved_loom_sha256` in toolchain evidence before interpreting the build result.

    In `build.gradle`, use official `fabricApi.configureTests` to create the GameTest source set with mod ID `developers_hell_test`, server GameTests enabled, client GameTests disabled, and visible EULA acceptance. Create only the `fabric-gametest` entrypoint. Keep this test source set out of the ordinary production `jar`; do not add GameTest outputs/resources to `sourceSets.main`, `jar.from`, or production runtime mod contents. Compile-check current 26.2 generated names, then have `FoundationGameTests` obtain `ModItems.FOUNDATION_TOKEN`, resolve its live `BuiltInRegistries.ITEM` key, require `DevelopersHell.id("foundation_token")`, and succeed through the official invoker lifecycle. Deliberately break the key assertion once and record the exact nonzero wrapper `build` exit/log, restore it, and record the zero-exit build plus GameTest success marker. This fail-first/restored evidence proves execution; production-JAR inclusion does not.

    Add `runProductionClient` and `runProductionServer` tasks with isolated ignored directories, Installer 1.1.2 and exact Loader/Minecraft values on the server, ordinary no-remap project artifact selection, and exact Fabric API in `productionRuntimeMods`. Bind both launchers to an Adoptium Java-25 toolchain and require the verification harness to prove the resolved launcher executable is under the exact retained `25.0.4+7` root; language-major matching alone is insufficient. Confirm resolved task/artifact names with `tasks --all`; never manually invoke or attach legacy `remapJar`.

    Add `auditDirectDependencies` as a fail-closed Gradle verification task over direct dependencies declared on the project-owned declarable configurations, not the resolved transitive tree. Its exact allowlist is Minecraft `com.mojang:minecraft:${minecraft_version}`, implementation Loader `net.fabricmc:fabric-loader:${loader_version}`, implementation Fabric API `net.fabricmc.fabric-api:fabric-api:${fabric_api_version}`, testImplementation Loader JUnit `net.fabricmc:fabric-loader-junit:${loader_version}`, and productionRuntimeMods Fabric API `net.fabricmc.fabric-api:fabric-api:${fabric_api_version}`. Reject unknown direct configurations/declarations, project/file/Git/URL dependencies, missing groups/versions, dynamic/range/latest versions, runtime Loader JUnit, and any extra runtime library. Generate a normal `runtimeClasspath` dependency report only as evidence; do not reject expected transitive Minecraft libraries or Fabric API modules merely because they appear beneath an allowed direct coordinate.

    Create `scripts/audit-foundation.ps1` with `-SourceAndDependencies`, optional `-JarPath`, and optional `-EvidencePath`. It must fail closed on `rg`, wrapper, archive, or Git errors. Recursively scan both `src/main` and `src/client` Java and resource text for runtime networking/remote-service surfaces: `java.net` imports or qualified uses; `URL`, `URLConnection`, `HttpURLConnection`, `Socket`, `ServerSocket`, `DatagramSocket`, `HttpClient`, `HttpRequest`, `HttpResponse`, `WebSocket`, `openConnection`, or `openStream`; HTTP(S)/WebSocket endpoints; OpenAI/ChatGPT SDK/client symbols; analytics, telemetry, Sentry, Mixpanel, Amplitude, Segment, remote-config, account/token/key authorization, or runtime download code. Separately reject `net.minecraft.client` and Blaze3D references only under `src/main`, allowing client APIs under `src/client`. Audit `settings.gradle`/`build.gradle` for only official Fabric/Maven Central/Gradle plugin repositories, invoke `auditDirectDependencies`, and capture the runtimeClasspath report without applying the direct allowlist to transitive nodes.

    When `-JarPath` is supplied, require the exact ordinary JAR and reject shaded/network SDK namespaces such as `com/openai`, `okhttp3`, `retrofit2`, `io/sentry`, Mixpanel/Amplitude/Segment packages, example/mixin residue, remote-config clients, or credential files. Require exactly one root `fabric.mod.json`, exactly one `LICENSE_developers-hell`, inspect metadata as production ID `developers_hell` with no `fabric-gametest` entrypoint, and reject an unrenamed/duplicate license. Fail if the archive contains `dev/developershell/gametest/**`, `FoundationGameTests*.class`, `ModuleGateTest*.class`, any other test output, any `developers_hell_test` string/metadata, or a test `fabric.mod.json` output. Always use `git check-ignore` to prove the wrapper JAR is not ignored and representative build/run dirt is ignored; reject tracked or visible cache/run/world/EULA/log/local-JDK/private files. Emit exact PASS/FAIL sections and evidence-safe command/results to `-EvidencePath` when provided. Run the script after the restored GameTest passes.

    Create the complete `scripts/verify-foundation.ps1` now, not in Plan 04. It exposes `-SelfCheck`, `-PrimeAndCompare`, `-RunServerSmoke`, `-ClientPreflight`, `-SuperviseInteractiveUat`, `-ValidateEvidence`, `-RequireUatPass`, and optional `-EvidencePath`, `-SessionDirectory`, `-SessionPointerPath`, `-SessionReceiptPath`, and `-DistributionPath`; normal defaults include the phase evidence, ignored `.work/interactive-uat-active.json`, and ignored `dist/developers-hell-0.1.0.jar`, while every supervised session must receive its own GUID directory. It resolves repository root from its committed location, invokes `audit-foundation.ps1`, propagates native exits, validates toolchain evidence, and owns only explicitly scoped temporary/network state. `-SelfCheck` validates every mode/parameter, tracked manifest (including Loom probe), archive lists, distribution binding, supervisor state machine, receipt hash, two-rule cleanup, and safe path guards without launching runtime. `-ValidateEvidence -EvidencePath <nonexistent-file>` must return nonzero.

    Its `-PrimeAndCompare` path must resolve the canonical repository root, canonical `[System.IO.Path]::GetTempPath()`, and canonical home root with ordinal case-insensitive comparison and Win32 final-path/long-name resolution so 8.3 aliases cannot bypass guards. Generate a direct temp child container named exactly `developers-hell-<32 lowercase hex>`, require its canonical parent equal the canonical temp root, reject reparse/pre-existing targets, and require the container/worktree paths are not equal to or ancestors of temp, repository, or home. A temp child may naturally live below the Windows home profile; that is allowed. Put the worktree at `<container>\worktree` and any preserved online JAR at a sibling within that exact owned container.

    Invoke `git -C <canonical-repository> worktree add --detach <canonical-worktree> HEAD`, then parse `git worktree list --porcelain -z` and require an exact normalized case-insensitive path, expected detached `HEAD`, and `detached` marker before setting a registered flag or trusting the target. Do not reuse the upstream Fabric template checkout. Inside that detached project worktree, use `git status --porcelain` to require clean state and `git ls-files --error-unmatch` for the exact manifest: `settings.gradle`, `gradle.properties`, `build.gradle`, `gradlew`, `gradlew.bat`, both `gradle/wrapper` files, source `LICENSE`, `01-TOOLCHAIN-EVIDENCE.md`, `DevelopersHell.java`, `DevelopersHellClient.java`, `ModItemIds.java`, `ModItems.java`, `ModuleId.java`, `ModuleGate.java`, `ModuleGateTest.java`, production `fabric.mod.json`, `en_us.json`, both Foundation Token item/model JSONs, `FoundationGameTests.java`, GameTest `fabric.mod.json`, `scripts/loom-resolution.init.gradle`, `scripts/audit-foundation.ps1`, and `scripts/verify-foundation.ps1`. Revalidate and select the retained exact Temurin `25.0.4+7` root and pass it as the sole Gradle Java installation. Run only that worktree's committed wrapper. For both the online `clean build` and same-cache `--offline clean build`, first delete the mode's old ignored log, attach the clean checkout's committed `scripts/loom-resolution.init.gradle`, capture combined output plus numeric exit, and require its mechanically derived configured/resolved/artifact-SHA tuple to equal the three toolchain-evidence Loom markers. Perform the comprehensive source/dependency/JAR audit, require exact `LICENSE_developers-hell` plus production entries and exclusions, and require equal online/offline SHA-256.

    Before exact worktree removal, atomically copy the verified equal clean-checkout JAR to the workspace-relative ignored `dist/developers-hell-0.1.0.jar`; reject another name or more than one production archive. Hash the destination after the copy and require `online_jar_sha256 = offline_jar_sha256 = distribution_sha256`. Retain it after worktree cleanup. `-RunServerSmoke`, `-ClientPreflight`, and `-SuperviseInteractiveUat` must consume only this file (or a byte-for-byte explicit runtime copy), verify SHA-256 before/after every launch, and reject an independently rebuilt/unequal JAR. If Loom requires `build/libs`, copy distribution bytes there without building and prove launch command/mod classpath uses that exact copy.

    Implement one fail-closed Windows Defender Firewall isolation primitive used by both server and interactive-client modes; it is the sole isolation method. Each invocation creates a fresh unique group and exactly two unique outbound-block rule names, one scoped to the verified `java.exe` and one to sibling `javaw.exe`. Before creation require both names/group absent and administrative control available; record the group ID, both exact rule IDs, both executable SHA-256 values, and group membership count exactly two. From the exact verified Java runtime, require a bounded probe to the recorded target to succeed before isolation and fail after both rules are active. In `finally`, remove both rules by their exact names, independently require each name absent and group membership zero, and never enumerate/delete unrelated rules.

    `-RunServerSmoke` applies the two-rule primitive for isolated server evidence. `-ClientPreflight` primes one ignored production-client profile online. `-SuperviseInteractiveUat` is the automation-owned two-session state machine: it requires a fresh canonical ignored GUID session directory containing `control.json`, `status.json`, `receipt.json`, and supervisor log; consumes Task 01-04-01's validated evidence/distribution/profile/JDK contract; and rejects a reused/nonempty session. It launches the ONLINE production Minecraft client visibly from the exact distribution/profile, records `ONLINE_READY` only after distribution/runtime-copy hashes, expected client process, menu-ready marker, supervisor PID, and client PID are live, then waits without auto-closing for normal human exit. It next proves exact-Java online probe reachability, creates the unique exactly-two-rule group, proves blocked probe/membership, launches the ISOLATED Minecraft client visibly with the same bytes/profile, records `ISOLATED_READY`, and again waits for normal exit while both rules stay active. In `finally` it removes both exact IDs, proves each absent/group zero, rehashes distribution/runtime copy, and writes terminal `COMPLETE` or `FAILED` status plus receipt. The receipt contains session/evidence/profile IDs; both client PIDs/exits/readiness; firewall group/rule/program hashes/membership/probes/cleanup; relative distribution and pre/post hashes; timestamps; and `receipt_payload_sha256` computed over a canonical payload so the finalizer detects alteration. It polls the atomic `control.json`; automation-only `CANCEL` enters the same `finally` cleanup and terminal `FAILED` receipt path, so a readiness failure cannot leave isolation state behind. It never hides Minecraft, never asks the user to run setup, and fails on elevation/control, probe, launch, liveness, normal-exit, hash, or cleanup uncertainty.

    Before adding the owned worktree, capture the exact byte sequence from `git worktree list --porcelain -z`. In `finally`, re-run the canonical containment/leaf/ownership guards. If registered, re-confirm the exact target/HEAD/detached tuple, call only `git -C <canonical-repository> worktree remove --force -- <exact canonical registered worktree>`, and require zero exit. Perform no repository-wide registration cleanup, never recursively delete a computed path, and never alter unrelated registrations. Require the exact target record/path gone and require the post-remove porcelain byte sequence to equal the pre-add sequence byte-for-byte. Remove the now-empty exact GUID container/sibling artifact only after the same guards; on pre-registration failure, remove only that guarded empty GUID container. Record `temp_child_valid`, `worktree_registered`, `worktree_removed`, `worktree_registry_restored`, `temp_container_removed`, and root-preservation PASS markers. The remaining production server/client, OS-isolation, and evidence-validation behavior follows RESEARCH/VALIDATION and executes in Plan 04 without modifying this script.
  </action>
  <acceptance_criteria>
    - Deliberately wrong GameTest assertion makes wrapper `build` fail; restored assertion makes `build` pass and resolves exact `developers_hell:foundation_token` in the live registry, and both fresh captured invocations mechanically equal the evidenced configured/resolved Loom build and artifact SHA-256.
    - Production client/server tasks resolve with Java 25, separate ignored directories, Installer 1.1.2 server values, ordinary JAR selection, and exact Fabric API productionRuntimeMods.
    - The direct-dependency task permits only the five exact configuration/coordinate pairs, rejects non-module/dynamic/unknown direct dependencies, and does not misclassify reported transitive Minecraft/Fabric components.
    - The PowerShell audit covers both source sets and every named network/HTTP/socket/WebSocket/OpenAI/analytics/telemetry/remote-config/account/key/download surface; common-only client linkage is checked separately.
    - Production archive audit requires the root `developers_hell` metadata and rejects every GameTest class/resource, `developers_hell_test`, and test metadata output; GameTest execution is established only by captured fail-first/restored wrapper-build evidence.
    - Production archive audit requires exactly one `LICENSE_developers-hell` and rejects unrenamed/duplicate license entries.
    - Source, repository, dependency, optional JAR-namespace, wrapper-ignore, and Git-dirt checks all fail closed and emit reusable evidence.
    - The fully implemented verification script syntax/self-check passes, validation against a deliberately nonexistent evidence path fails nonzero, and its clean-worktree path requires the exact verified Temurin root, committed tracked inputs and Loom probe, fresh per-build resolution/SHA equality, committed wrapper execution, online/same-cache-offline/distribution hash equality, canonical GUID-child containment, exact worktree registration, guarded exact-registration cleanup, root-preservation checks, and no dependency on uncommitted Plan 04 changes.
    - The committed runtime modes use only the ignored canonical distribution JAR, hash it before and after each launch, create exactly two named firewall rules per unique group, prove online/blocked connectivity, remove both exact rules in `finally`, and require zero remaining group members.
    - `-SuperviseInteractiveUat` owns setup and both visible production-client launches, exposes machine-proven `ONLINE_READY` for the auto-to-human handoff, advances automatically to isolated launch after normal online exit, and emits a canonical-payload-hashed COMPLETE receipt only after both normal exits and exact two-rule cleanup.
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'
$jdkRoot = (Resolve-Path -LiteralPath '.work/toolchain/temurin-25.0.4+7-x64').Path; $env:JAVA_HOME = $jdkRoot; $env:Path = "$(Join-Path $jdkRoot 'bin');$env:Path"; $jvmArgs = @("-Dorg.gradle.java.installations.paths=$jdkRoot", '-Dorg.gradle.java.installations.auto-detect=false', '-Dorg.gradle.java.installations.auto-download=false'); $evidence = Get-Content -LiteralPath '.planning/phases/01-java-25-and-fabric-26-2-foundation/01-TOOLCHAIN-EVIDENCE.md' -Raw; foreach ($tool in @('java','javac')) { $actual = (Get-FileHash -LiteralPath (Join-Path $jdkRoot "bin/$tool.exe") -Algorithm SHA256).Hash; $expected = [regex]::Match($evidence, '(?m)^jdk_' + $tool + '_sha256:\s*([0-9a-fA-F]{64})\s*$'); if (-not $expected.Success -or $actual -ne $expected.Groups[1].Value) { throw "Verified $tool binary mismatch" } }; $sha = [Security.Cryptography.SHA256]::Create(); try { $pathHash = ([BitConverter]::ToString($sha.ComputeHash([Text.Encoding]::UTF8.GetBytes($jdkRoot.ToLowerInvariant())))).Replace('-','').ToLowerInvariant() } finally { $sha.Dispose() }; $pathMarker = [regex]::Match($evidence, '(?m)^jdk_path_sha256:\s*([0-9a-f]{64})\s*$'); if (-not $pathMarker.Success -or $pathHash -ne $pathMarker.Groups[1].Value) { throw 'Verified JDK canonical path hash mismatch' }
$buildLog = '.work/plan03-restored-build-loom.log'; Remove-Item -LiteralPath $buildLog -Force -ErrorAction SilentlyContinue; $buildLines = @(& .\gradlew.bat @jvmArgs build --no-daemon --stacktrace --init-script .\scripts\loom-resolution.init.gradle 2>&1); $buildExit = $LASTEXITCODE; [IO.File]::WriteAllLines([IO.Path]::GetFullPath($buildLog), [string[]] $buildLines); if ($buildExit -ne 0) { throw "Restored GameTest build failed with exit $buildExit" }; $buildText = $buildLines -join "`n"; function Get-ProbeValue([string] $Name) { $m = [regex]::Match($buildText, '(?m)^DEVELOPERS_HELL_' + [regex]::Escape($Name) + '=(.+?)\s*$'); if (-not $m.Success) { throw "Build Loom marker missing: $Name" }; $m.Groups[1].Value.Trim() }; function Get-EvidenceValue([string] $Name) { $m = [regex]::Match($evidence, '(?m)^' + [regex]::Escape($Name) + ':\s*(.+?)\s*$'); if (-not $m.Success) { throw "Toolchain evidence marker missing: $Name" }; $m.Groups[1].Value.Trim() }; $configured = [regex]::Match((Get-Content -LiteralPath 'gradle.properties' -Raw), '(?m)^loom_version=(.+?)\s*$').Groups[1].Value.Trim(); if ((Get-ProbeValue 'LOOM_SELECTED') -ne $configured -or (Get-ProbeValue 'LOOM_SELECTED') -ne (Get-EvidenceValue 'loom_selected') -or (Get-ProbeValue 'LOOM_RESOLVED') -ne (Get-EvidenceValue 'resolved_loom_build') -or (Get-ProbeValue 'LOOM_ARTIFACT_SHA256') -ne (Get-EvidenceValue 'resolved_loom_sha256')) { throw 'Restored GameTest build did not use the frozen Loom artifact' }; if ($configured -eq '1.17-SNAPSHOT' -and (Get-ProbeValue 'LOOM_RESOLVED') -eq '1.17-SNAPSHOT') { throw 'Snapshot Loom probe did not derive a concrete build' }
& .\gradlew.bat @jvmArgs tasks --all --no-daemon; if ($LASTEXITCODE -ne 0) { throw "Task discovery failed with exit $LASTEXITCODE" }
& .\gradlew.bat @jvmArgs auditDirectDependencies --no-daemon; if ($LASTEXITCODE -ne 0) { throw "Direct-dependency audit failed with exit $LASTEXITCODE" }
& powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\audit-foundation.ps1 -SourceAndDependencies -JarPath .\build\libs\developers-hell-0.1.0.jar; if ($LASTEXITCODE -ne 0) { throw "Comprehensive audit failed with exit $LASTEXITCODE" }
$verifyText = Get-Content -LiteralPath '.\scripts\verify-foundation.ps1' -Raw; [void] [scriptblock]::Create($verifyText); $missingEvidence = Join-Path ([System.IO.Path]::GetTempPath()) ('developers-hell-missing-' + [guid]::NewGuid().ToString('N') + '.md'); & powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-foundation.ps1 -ValidateEvidence -EvidencePath $missingEvidence; if ($LASTEXITCODE -eq 0) { throw 'Evidence validator accepted a nonexistent file' }; & powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-foundation.ps1 -SelfCheck; if ($LASTEXITCODE -ne 0) { throw "Verification harness self-check failed with exit $LASTEXITCODE" }</automated>
  </verify>
  <done>The real registry passes fail-first GameTest while the ordinary production JAR excludes all test outputs; production tasks, comprehensive audits, and the full clean-project-checkout verification harness are ready and committed before Plan 04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Test source → production claim | An orphaned or never-failing GameTest could create false confidence. |
| Client source → common/server | Physical-client linkage in common code can crash dedicated servers. |
| Project source/declarations → offline runtime | Hidden network code, SDKs, endpoints, or direct dependencies can violate the offline contract. |
| Direct dependency → resolved graph | Allowed first-party dependencies bring expected transitives that must not be confused with unauthorized direct choices. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-10 | Repudiation | GameTest/build wiring | high | mitigate | Machine-observed fail-first assertion followed by restored wrapper build and exact live-key assertion. |
| T-01-11 | Denial of Service | common/client boundary | high | mitigate | Split sources, common-only client-symbol audit, server GameTest, and production-server proof in Plan 04. |
| T-01-12 | Information Disclosure | runtime network/telemetry/account surface | high | mitigate | Comprehensive recursive main+client source/resource scan and shaded-JAR namespace audit, repeated in Plan 04. |
| T-01-13 | Tampering | direct dependencies/repositories | high | mitigate | Model-based exact direct allowlist, official repository audit, dynamic/file/Git rejection, and transitive-report separation. |
| T-01-14 | Information Disclosure | generated run/private data and disposable worktree | high | mitigate | Canonical Win32 final-path checks, GUID direct-child containment, exact porcelain registration/HEAD proof, only non-recursive `git worktree remove --force -- <exact path>` in `finally`, byte-identical preservation of prior registrations, exact-child disappearance, and repo/home/temp-root survival checks. |
</threat_model>

## Artifacts this phase produces

- `build.gradle`: `fabricApi.configureTests`, `auditDirectDependencies`, `runProductionClient`, `runProductionServer`, Java 25 launcher, exact `productionRuntimeMods`.
- `FoundationGameTests`: `foundationTokenIsRegistered(...)`, `invokeTestMethod(...)`.
- GameTest `fabric.mod.json`: `developers_hell_test` plus server-only `fabric-gametest` entrypoint.
- `scripts/audit-foundation.ps1`: `SourceAndDependencies`, `JarPath`, `EvidencePath`; comprehensive source/side/dependency/repository/archive/Git checks.
- `scripts/loom-resolution.init.gradle`: fresh-invocation configured/resolved Loom component and artifact-SHA probe used by Plan 03 and every Plan 04 build.
- `scripts/verify-foundation.ps1`: `SelfCheck`, `PrimeAndCompare`, `RunServerSmoke`, `ClientPreflight`, `SuperviseInteractiveUat`, `ValidateEvidence`, `RequireUatPass`, `EvidencePath`, `SessionDirectory`, `SessionPointerPath`, `SessionReceiptPath`, `DistributionPath`; precommitted clean-worktree, single-distribution, background-supervisor, two-visible-client, two-rule, hashed-receipt, and final evidence harness.

<verification>
- Run restored wrapper `build`, task discovery, `auditDirectDependencies`, comprehensive PowerShell audit against the ordinary JAR, verification-script parse, and `-SelfCheck`.
- Preserve deliberate GameTest RED/restored GREEN, explicit production-JAR test-output exclusions, direct allowlist, transitive report, and audit output in `01-03-SUMMARY.md`.
</verification>

<success_criteria>
- The live registry identity is proven inside Minecraft and owned by wrapper build.
- Both production task surfaces exist before final runtime evidence.
- Runtime offline/security claims have one comprehensive, reusable, fail-closed source/dependency/archive audit rather than a narrow HTTP grep.
- FND-02 final proof is executable from committed HEAD in a disposable detached clean Developer's Hell worktree whose wrapper and required sources/resources are demonstrably tracked.
</success_criteria>

<output>Create `.planning/phases/01-java-25-and-fabric-26-2-foundation/01-03-SUMMARY.md` when done.</output>
