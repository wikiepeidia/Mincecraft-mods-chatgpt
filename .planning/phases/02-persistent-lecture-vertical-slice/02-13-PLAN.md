---
phase: 02-persistent-lecture-vertical-slice
plan: 13
type: execute
wave: 15
depends_on: [02-18]
files_modified:
  - scripts/verify-lecture.ps1
  - .planning/phases/02-persistent-lecture-vertical-slice/02-LECTURE-EVIDENCE.md
  - .planning/phases/02-persistent-lecture-vertical-slice/02-VALIDATION.md
  - README.md
autonomous: true
requirements: [FND-05, FND-06, FND-07, CAMP-01, CAMP-02, LECT-01, LECT-02]
estimate: {tokens: 42000, raw_tokens: 42000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "The checksum-pinned Java 25 verifier produces a fresh ordinary JAR, passes all unit/GameTests/dependency/source/archive gates, and proves a bounded dedicated server reaches ready then invokes the real stop-cleanup callback before clean exit."
    - "Build/source/dist hashes are equal, the production archive excludes test/client-leak/network/API/credential residue, and evidence contains only sanitized public-safe facts."
    - statement: "Manual client smoke at small, normal, and large GUI scales plus a narrow window proves boss/action copy avoids material clipping or overlap."
      verification: backstop
    - statement: "A held-out long localization and large GUI scale prove quiz prompt/options wrap acceptably in chat while boss/action strings stay within fixed budgets."
      verification: backstop
    - statement: "Normal and reduced-effects modes visibly preserve the same understandable lane, pad, and ring geometry."
      verification: backstop
    - statement: "Muted audio and minimal particles still leave enough text and stable shape information to complete every act."
      verification: backstop
    - statement: "A real client run shows no camera shake, nausea, full-screen flash, strobe, or stale post-cleanup marker."
      verification: backstop
    - statement: "The accepted vanilla-backed items and Professor/Homework silhouettes render without missing-model textures."
      verification: backstop
    - statement: "At common GUI scales and a narrow window, the native Remote overlay, 20-second tooltip, recharge line, and one ready cue remain recognizable without covering critical boss instructions."
      verification: backstop
  artifacts:
    - {path: "scripts/verify-lecture.ps1", provides: "Self-checking bounded Phase 2 build/GameTest/audit/server/evidence harness"}
    - {path: ".planning/phases/02-persistent-lecture-vertical-slice/02-LECTURE-EVIDENCE.md", provides: "Sanitized exact automated results/hash and distinct manual rows"}
    - {path: ".planning/phases/02-persistent-lecture-vertical-slice/02-VALIDATION.md", provides: "Reconciled nine-row Nyquist matrix"}
  key_links:
    - {from: "scripts/verify-lecture.ps1", to: "build/libs/developers-hell-0.1.0.jar", via: "clean build then exact archive/hash contract", pattern: "SHA256"}
    - {from: "scripts/verify-lecture.ps1", to: ".planning/phases/02-persistent-lecture-vertical-slice/02-LECTURE-EVIDENCE.md", via: "validated sanitized evidence generation", pattern: "DEVELOPERS_HELL_SERVER_STOPPING_CLEANUP_COMPLETE"}
  flagged_assumptions:
    - "[UNRESOLVED][verification=null][FND-07][unclassified] Probe: unclassified — review manually."
  prohibitions:
    - statement: "MUST NOT report client readability, playability, or exact distribution-JAR UAT as passed based only on compilation, unit tests, GameTests, logs, or acceptance of placeholder art."
      status: flagged-unverified
---

# Phase 2 Plan 13 — Reproducible Evidence and Honest Client Backstops

## Phase Goal

**As a** survival player, **I want to** discover, fail, retry, and complete Professor Infinite Slides, **so that** my Lecture progress and rewards survive restarts without exploits.

<objective>
Produce one fresh hash-identified Phase 2 JAR/evidence record while preserving all seven visual/playability judgments as real-client backstops.

Purpose: Close FND-07 and archive/server/offline claims without reusing Phase 1 evidence or inferring human observations.
Output: Bounded verifier, sanitized evidence, reconciled validation matrix, README handoff, distribution candidate, and manual checklist.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.agents/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-CONTEXT.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-UI-SPEC.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-RESEARCH.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-PATTERNS.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-VALIDATION.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-18-SUMMARY.md
@.planning/phases/01-java-25-and-fabric-26-2-foundation/01-03-SUMMARY.md
@.planning/phases/01-java-25-and-fabric-26-2-foundation/01-04-SUMMARY.md
@AGENTS.md
@README.md

<interfaces>
Do not edit sealed Phase 1 evidence/hash semantics. Reuse `audit-foundation.ps1` safety conventions and exact project pins. Plan 05 exposes `DEVELOPERS_HELL_SERVER_STOPPING_CLEANUP_COMPLETE`; this plan must observe it in a real bounded dedicated-server shutdown. Decision coverage: D-01 through D-20 verification; validation row 02-GATE-01 and reconciliation of all nine rows.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build a self-checking fresh-artifact and real server-stop verifier</name>
  <read_first>
    - `scripts/audit-foundation.ps1` and `verify-foundation.ps1` — copy safety patterns, not Phase 1 evidence identity.
    - `build.gradle`, pins, Loom probe, and `02-VALIDATION.md` all automated rows.
    - Plan 05 stop marker and all Phase 2 stable class/resource IDs.
  </read_first>
  <files>scripts/verify-lecture.ps1, .planning/phases/02-persistent-lecture-vertical-slice/02-LECTURE-EVIDENCE.md</files>
  <behavior>
    - `-SelfCheck` fails closed on path/process/archive/evidence tampering under PowerShell 5.1 and 7.
    - Automated mode runs clean unit/GameTests/audit/build, inspects only the fresh JAR, launches bounded production server, requests clean stop, and requires ready+stop-cleanup marker+exit.
    - Only after all gates pass does it copy exact bytes to dist and record equal source/build/dist SHA-256.
  </behavior>
  <action>
    Implement strict canonical containment, literal paths, checksum-bound Java, disabled toolchain discovery/download, bounded exact-child PID supervision, deterministic self-check, safe failure aggregation, and `finally` cleanup. Run `clean test runGameTest auditDirectDependencies build`, foundation audit plus an explicit Phase 2 archive contract, and `runProductionServer`; require ready, issue clean stop, require `DEVELOPERS_HELL_SERVER_STOPPING_CLEANUP_COMPLETE`, process exit, and zero owned residue. Enumerate stable items/entities, recipe/advancement/lang/model/classes; reject tests, common-client links, HTTP/OpenAI/API/telemetry/credentials/private defaults, dependency/repository drift, and non-single license. Copy/hash only the inspected fresh JAR. Runtime stays offline/local with no external service.
  </action>
  <acceptance_criteria>
    - Both shells pass self-check and deliberate stale-JAR/missing-marker/hash/process/archive failures are rejected.
    - One automated invocation proves every test/audit/archive/hash fact and the real stop callback, not merely its GameTest seam.
    - No broad kill/delete/firewall action or private path/token enters evidence.
  </acceptance_criteria>
  <verify><automated>$ErrorActionPreference='Stop';$jdkRoot=(Resolve-Path '.work/toolchain/temurin-25.0.4+7-x64').Path;$env:JAVA_HOME=$jdkRoot;$env:Path="$(Join-Path $jdkRoot 'bin');$env:Path";$scriptPath=(Resolve-Path '.\scripts\verify-lecture.ps1').Path;$evidencePath=Join-Path (Get-Location) '.planning\phases\02-persistent-lecture-vertical-slice\02-LECTURE-EVIDENCE.md';$distributionPath=Join-Path (Get-Location) 'dist\developers-hell-0.1.0.jar';& powershell.exe -NoProfile -ExecutionPolicy Bypass -File $scriptPath -SelfCheck;$selfCheckExit=$LASTEXITCODE;if($selfCheckExit -ne 0){throw "Windows PowerShell self-check failed with exit $selfCheckExit"};$pwshCommand=Get-Command 'pwsh' -ErrorAction SilentlyContinue;if($null -ne $pwshCommand){& $pwshCommand.Source -NoProfile -File $scriptPath -SelfCheck;$pwshExit=$LASTEXITCODE;if($pwshExit -ne 0){throw "PowerShell 7 self-check failed with exit $pwshExit"}};& powershell.exe -NoProfile -ExecutionPolicy Bypass -File $scriptPath -Verify -EvidencePath $evidencePath -DistributionPath $distributionPath;$verifyExit=$LASTEXITCODE;if($verifyExit -ne 0){throw "lecture verification failed with exit $verifyExit"};& powershell.exe -NoProfile -ExecutionPolicy Bypass -File $scriptPath -ValidateEvidence -EvidencePath $evidencePath -DistributionPath $distributionPath;$validateExit=$LASTEXITCODE;if($validateExit -ne 0){throw "lecture evidence validation failed with exit $validateExit"};$evidenceText=Get-Content -LiteralPath $evidencePath -Raw;foreach($evidencePattern in @('(?i)hash.*equal','(?i)child.*(clean|zero|none)','(?i)server.*ready','DEVELOPERS_HELL_SERVER_STOPPING_CLEANUP_COMPLETE','(?i)clean.*exit')){if($evidenceText -notmatch $evidencePattern){throw "required evidence marker missing: $evidencePattern"}};$distributionHash=(Get-FileHash -LiteralPath $distributionPath -Algorithm SHA256).Hash;if($evidenceText -notmatch [regex]::Escape($distributionHash)){throw 'distribution hash missing from evidence'}</automated></verify>
  <done>One fresh ordinary JAR is mechanically tied to complete automated evidence including the real dedicated-server stop callback.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile all nine validation rows and preserve seven observed-only judgments</name>
  <read_first>
    - `02-VALIDATION.md` nine rows/Wave 0/sign-off.
    - `02-UI-SPEC.md` seven manual visual backstops and UI Considerations.
    - `README.md` Phase 1 installation/offline distinctions.
    - Task 1 evidence and exact artifact hash.
  </read_first>
  <files>.planning/phases/02-persistent-lecture-vertical-slice/02-LECTURE-EVIDENCE.md, .planning/phases/02-persistent-lecture-vertical-slice/02-VALIDATION.md, README.md</files>
  <action>
    Map final plan/test IDs to all nine rows, mark only genuinely green automated facts, and set `nyquist_compliant: true` only when all automated rows pass. Write exact commands/exits/test anchors/hash/server lifecycle/source/archive facts with sanitized paths. Keep all seven UI/playability rows PENDING until direct isolated-client observation. Update README with Contract discovery, config, arena/retry, three acts, Sheet/Remote, exact build/install/verification instructions, current hash, and clear automated-versus-UAT status; preserve offline/no-API and Phase 1 historical proof distinctions.
  </action>
  <acceptance_criteria>
    - 02-CFG-01, STATE-01, GEO-01, ITEM-01, BOSS-01, LIFE-01, REWARD-01, DISC-01, and GATE-01 each name existing green evidence.
    - Evidence/README never infer visual readability, fun, clipping, audio balance, model rendering, or exact-client UAT from automation.
    - All public text remains fictional and contains no identity/employer/real spending/sponsorship/API claim.
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop';$jdkRoot=(Resolve-Path '.work/toolchain/temurin-25.0.4+7-x64').Path;$env:JAVA_HOME=$jdkRoot;$env:Path="$(Join-Path $jdkRoot 'bin');$env:Path";$scriptPath=(Resolve-Path '.\scripts\verify-lecture.ps1').Path;$evidencePath=Join-Path (Get-Location) '.planning\phases\02-persistent-lecture-vertical-slice\02-LECTURE-EVIDENCE.md';$distributionPath=Join-Path (Get-Location) 'dist\developers-hell-0.1.0.jar';& powershell.exe -NoProfile -ExecutionPolicy Bypass -File $scriptPath -ValidateEvidence -EvidencePath $evidencePath -DistributionPath $distributionPath;$validateExit=$LASTEXITCODE;if($validateExit -ne 0){throw "lecture evidence validation failed with exit $validateExit"};$evidenceText=Get-Content -LiteralPath $evidencePath -Raw;$rowIds=@('02-CFG-01','02-STATE-01','02-GEO-01','02-ITEM-01','02-BOSS-01','02-LIFE-01','02-REWARD-01','02-DISC-01','02-GATE-01');foreach($rowId in $rowIds){if($evidenceText -notmatch [regex]::Escape($rowId)){throw "automated evidence row missing: $rowId"}};$manualRows=@([regex]::Split($evidenceText,'\r?\n') | Where-Object{$_ -match '(?i)manual|backstop' -and $_ -match '(?i)PENDING|PASS|FAIL'});if($manualRows.Count -ne 7){throw "expected seven distinct manual rows, found $($manualRows.Count)"};$distributionHash=(Get-FileHash -LiteralPath $distributionPath -Algorithm SHA256).Hash;if($evidenceText -notmatch [regex]::Escape($distributionHash)){throw 'exact distribution hash missing from evidence'}</automated>
    <human-check>Using the exact hash-recorded JAR in the isolated Fabric 26.2 profile: discover/craft/use the Contract; inspect rejection copy; fail/reload/recover Retake; complete all acts; recover Sheet; fire/reject/rejoin/wait for Remote; test small/normal/large GUI scales plus narrow window; held-out long localization; normal/reduced effects; muted audio/minimal particles; forbidden motion/strobe/stale markers; and missing-model appearance. Record each of the seven backstops independently as PASS/FAIL.</human-check>
  </verify>
  <done>Automated evidence is complete and reproducible while every genuinely visual/playability claim remains an honest direct-observation row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Build/test/server output -> evidence/dist | Stale artifacts, partial logs, and machine-private data could overstate proof. |
| Automation -> manual claims | Rendering/readability/playability require observation and cannot be inferred. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-EVID-01 | Repudiation | Automated/manual status | high | mitigate | Exact commands/exits/hashes; validator blocks unobserved PASS. |
| T-02-EVID-02 | Information Disclosure | Logs/evidence | high | mitigate | Sanitized normalized facts and credential/private-path scans. |
| T-02-EVID-03 | Denial of Service | Server/process harness | medium | mitigate | Exact child ownership, timeouts, clean stop, finally cleanup. |
| T-02-EVID-04 | Tampering | Fresh JAR/dist | high | mitigate | Clean build, mtime/entries/hash checks, copy only after all gates. |
</threat_model>

## Artifacts this phase produces

- `verify-lecture.ps1` self-check/build/GameTest/audit/archive/real-server/hash/evidence harness.
- `02-LECTURE-EVIDENCE.md` sanitized automated results and seven separate manual rows.
- Reconciled `02-VALIDATION.md` nine-row matrix and README player/contributor handoff.

## Source Coverage Audit

| Source | ID/group | Plan coverage | Status |
|---|---|---|---|
| GOAL | Discoverable complete Lecture loop with reload/failure/cleanup/retry/exact rewards | 02-01..02-18 | COVERED |
| REQ | FND-05 | 02-02, 02-13 | COVERED |
| REQ | FND-06 | 02-01, 02-04, 02-05, 02-07, 02-08, 02-11, 02-12, 02-14, 02-15, 02-17, 02-18, 02-13 | COVERED |
| REQ | FND-07 | Every plan; final matrix 02-13 | COVERED |
| REQ | CAMP-01 | 02-03, 02-06, 02-16, 02-13 | COVERED |
| REQ | CAMP-02 | 02-01, 02-06, 02-08, 02-13 | COVERED |
| REQ | LECT-01 | 02-01, 02-06, 02-09, 02-10, 02-13 | COVERED |
| REQ | LECT-02 | 02-01, 02-03..02-05, 02-07..02-14, 02-16..02-18 | COVERED |
| CONTEXT | D-01..D-04 config/offline/public-safe | 02-02, 02-13 | COVERED |
| CONTEXT | D-05..D-08 discovery/arena/retry | 02-06..02-08, 02-16 | COVERED |
| CONTEXT | D-09..D-14 entity/three acts/readability | 02-01, 02-03, 02-09, 02-10, 02-14, 02-15 | COVERED |
| CONTEXT | D-15..D-20 persistence/cleanup/rewards/tests | 02-01, 02-04..02-05, 02-07..02-15, 02-17..02-18 | COVERED |
| RESEARCH | Config, reducer/codec, lifecycle, stable entities/resources, arena, Retake, combat, presentation, rewards, cooldown, commands, evidence | 02-01..02-18 | COVERED |
| VALIDATION | CFG, STATE, GEO, ITEM, BOSS, LIFE, REWARD, DISC, GATE (nine rows) | 02-02..02-18 named tests/evidence | COVERED |
| UI | 40 explicit + 16 manual considerations | Implementation plans + seven structured backstops here | COVERED |
| EDGE | Six idempotency/concurrency truths | 02-01, 02-04, 02-11 | COVERED |
| EDGE | Four unclassified rows | Exactly one each in 02-02, 02-09, 02-13, 02-16 | COVERED |
| PROHIBITION | Five bespoke constraints | Exactly two in 02-02; one each 02-05, 02-10, 02-13 | COVERED |

Deferred Jury/Chairman/Codex, custom art/audio/HUD, objective journal, sandbox tools, and Metadata Roulette behavior remain excluded and are not gaps.

<verification>
- The verifier proves all automated facts and the real dedicated-server stop marker on a fresh artifact.
- Seven UI truths remain manual backstops until directly observed.
</verification>

<success_criteria>
- All seven requirements, 20 decisions, nine validation rows, UI/edge/prohibition contracts, and offline boundaries have implementation/proof owners.
- Artifact, evidence, validation, README, and manual status agree exactly.
</success_criteria>

<output>Create `.planning/phases/02-persistent-lecture-vertical-slice/02-13-SUMMARY.md` when done.</output>
