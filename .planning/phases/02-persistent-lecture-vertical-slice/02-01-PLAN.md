---
phase: 02-persistent-lecture-vertical-slice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/dev/developershell/DevelopersHell.java
  - src/main/java/dev/developershell/registry/ModItems.java
  - src/main/java/dev/developershell/registry/ModEntities.java
  - src/main/java/dev/developershell/campaign/CampaignSavedData.java
  - src/main/java/dev/developershell/campaign/CampaignService.java
  - src/main/java/dev/developershell/lecture/LectureEncounterManager.java
  - src/main/java/dev/developershell/item/CursedInternshipContractItem.java
  - src/gametest/java/dev/developershell/gametest/FoundationGameTests.java
autonomous: true
requirements: [FND-06, FND-07, CAMP-02, LECT-01, LECT-02]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "A server player can use one registered Contract on a valid lectern, fight one owner-scoped Professor through a real vulnerability window, and receive one persisted Attendance Sheet entitlement plus first-only Infinite Slides Remote grant."
    - "Repeated Contract activation for the same player and desk creates at most one active encounter and retry checkpoint; invalid or duplicate activation consumes nothing and changes no blocks."
    - "Competing starts are accepted atomically only when no matching active attempt exists; exactly one attempt owns the boss/runtime, with block state unchanged."
  artifacts:
    - path: "src/main/java/dev/developershell/campaign/CampaignService.java"
      provides: "State-first start/victory facade used by the tracer and later reducers"
      exports: ["CampaignService", "start", "victory"]
    - path: "src/main/java/dev/developershell/campaign/CampaignSavedData.java"
      provides: "Schema-v1 Overworld SavedData path"
      exports: ["CampaignSavedData", "TYPE", "get"]
    - path: "src/main/java/dev/developershell/lecture/LectureEncounterManager.java"
      provides: "One owner/encounter runtime and Slide Deck window"
      exports: ["LectureEncounterManager", "start", "tick", "cleanup"]
  key_links:
    - from: "src/main/java/dev/developershell/item/CursedInternshipContractItem.java"
      to: "src/main/java/dev/developershell/campaign/CampaignService.java"
      via: "logical-server validation followed by accepted START"
      pattern: "CampaignService"
    - from: "src/main/java/dev/developershell/campaign/CampaignService.java"
      to: "src/main/java/dev/developershell/campaign/CampaignSavedData.java"
      via: "replace and setDirty before materializing effects"
      pattern: "setDirty"
---

# Phase 2 Plan 01 — Contract-to-Reward Tracer

## Phase Goal

**As a** survival player, **I want to** discover, fail, retry, and complete Professor Infinite Slides, **so that** my Lecture progress and rewards survive restarts without exploits.

<objective>
Prove the thinnest production-quality Contract-to-Professor-to-first-reward path before expanding discovery, lifecycle, and combat matrices.

Purpose: Retire the exact Minecraft 26.2 SavedData, custom-entity, logical-server transaction, GameTest, and client/common seams on one runnable path.
Output: One state-first encounter tracer whose stable IDs and interfaces are retained by every later expansion plan.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.agents/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-CONTEXT.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-UI-SPEC.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-RESEARCH.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-PATTERNS.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-VALIDATION.md
@AGENTS.md

<interfaces>
Preserve `DevelopersHell.id(String)`, unconditional registry initialization, the split common/client source sets, and the existing `CustomTestMethodInvoker` GameTest entrypoint. Use inspected 26.2 `SavedDataType`, `SavedDataStorage.computeIfAbsent`, `ValueInput`/`ValueOutput`, `EntityType.Builder.build(ResourceKey)`, `ServerBossEvent`, and vanilla renderer APIs. Decision coverage: D-07, D-09, D-10, D-15, D-16, D-19, D-20.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Run one state-first Contract-to-first-reward server path</name>
  <read_first>
    - `src/main/java/dev/developershell/DevelopersHell.java`, `registry/ModItems.java`, and `src/gametest/java/dev/developershell/gametest/FoundationGameTests.java` — retain initialization and GameTest conventions.
    - `02-RESEARCH.md` exact 26.2 API table, registration skeleton, and SavedData skeleton — do not translate old Yarn examples.
    - `02-CONTEXT.md` D-07, D-09, D-15, D-16, and D-19 — atomic activation, stable boss, owner identity, state-before-effects, and first-only reward.
    - `02-UI-SPEC.md` boss-bar and Slide Deck contracts — one participant, one named cue, and one damage window.
  </read_first>
  <files>src/main/java/dev/developershell/DevelopersHell.java, src/main/java/dev/developershell/registry/ModItems.java, src/main/java/dev/developershell/registry/ModEntities.java, src/main/java/dev/developershell/campaign/CampaignSavedData.java, src/main/java/dev/developershell/campaign/CampaignService.java, src/main/java/dev/developershell/lecture/LectureEncounterManager.java, src/main/java/dev/developershell/item/CursedInternshipContractItem.java, src/gametest/java/dev/developershell/gametest/FoundationGameTests.java</files>
  <action>
    Start red with `contractStartsSlideWindowAndCommitsFirstReward` in the existing GameTest invoker. Build one padded Overworld desk arena, use the registered Contract, assert one active owner/encounter tuple and one Professor, advance through one five-second Slide Deck cue and owner-only vulnerability window, then assert committed PASSED/Sheet/Remote ledger before inventory effects. Use a fixed UUID/seed in failures.

    Implement the tracer with stable lower-snake-case Contract, Sheet, Remote, and Professor identities registered regardless of gates. `CampaignSavedData` uses schema 1 and Overworld `SavedDataType`; `CampaignService` serializes START/victory on the server thread and calls `setDirty()` before stack/entity/reward/presentation effects. `LectureEncounterManager` owns one bounded runtime and `ServerBossEvent`; the Professor begins as a compile-proven nested Vindicator identity in `ModEntities`, has no loot table, and accepts damage only from its owner during the open window. Keep reward issuance outside entity loot and all client decisions out of common code per D-07/D-09/D-15/D-16/D-19.
  </action>
  <acceptance_criteria>
    - The real transformed GameTest reaches start, boss window, matching victory, and one committed first-reward state through production entrypoints.
    - Duplicate and same-tick competing starts leave one active runtime, one boss, one encounter UUID, one retry record, and unchanged arena blocks.
    - START and victory state are dirty before Contract shrink, spawn, grant, message, or boss-bar effects; stale/wrong-owner/replayed victory is a complete no-op.
    - The tracer uses no network, OpenAI/API, wall clock, database, new dependency, client authority, or block-damaging effect.
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop';$started=[DateTime]::UtcNow;$jdkRoot=(Resolve-Path '.work/toolchain/temurin-25.0.4+7-x64').Path;if(-not (Test-Path -LiteralPath (Join-Path $jdkRoot 'bin/java.exe') -PathType Leaf)){throw 'pinned Java executable missing'};$env:JAVA_HOME=$jdkRoot;$env:Path="$(Join-Path $jdkRoot 'bin');$env:Path";$jvm=@("-Dorg.gradle.java.installations.paths=$jdkRoot",'-Dorg.gradle.java.installations.auto-detect=false','-Dorg.gradle.java.installations.auto-download=false');& .\gradlew.bat @jvm clean test compileJava processResources runGameTest jar --offline --no-daemon --console=plain --init-script scripts/loom-resolution.init.gradle;$gradleExit=$LASTEXITCODE;if($gradleExit -ne 0){throw "tracer gate failed with exit $gradleExit"};$jar=Get-Item -LiteralPath 'build/libs/developers-hell-0.1.0.jar';if($jar.LastWriteTimeUtc -le $started){throw 'fresh jar was not produced'};Add-Type -AssemblyName System.IO.Compression.FileSystem;$zip=[IO.Compression.ZipFile]::OpenRead($jar.FullName);try{$entries=$zip.Entries.FullName;if(-not ($entries -contains 'dev/developershell/campaign/CampaignService.class')){throw 'fresh tracer class missing'};if($entries -match 'gametest|FoundationGameTests'){throw 'test output leaked'}}finally{$zip.Dispose()}</automated>
  </verify>
  <done>The clean invocation produces and inspects a fresh ordinary JAR, and the real server tracer proves one state-first Contract-to-reward path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Item request -> logical server | Client use is untrusted until server validation and accepted START. |
| Saved state -> effects | State and ledger must commit before inventory, entity, reward, or presentation effects. |
| Common entity -> client renderer | Only stable identity crosses source sets; gameplay authority never does. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-TR-01 | Spoofing | Start/victory callbacks | high | mitigate | Match server owner plus active encounter UUID and prove stale/wrong-owner/replayed no-ops. |
| T-02-TR-02 | Tampering | First reward | high | mitigate | Persist PASSED/ledger and call `setDirty()` before grant; no entity loot. |
| T-02-TR-03 | Denial of Service | Runtime/presentation | medium | mitigate | One runtime/bar, bounded timers/particles, and owner-scoped tick work. |
| T-02-TR-04 | Elevation of Privilege | Client renderer | high | mitigate | Client-only renderer; logical-server position, damage, progression, and reward decisions. |
</threat_model>

## Artifacts this phase produces

- Stable Contract, Attendance Sheet, Infinite Slides Remote, and Professor registry identities used by the tracer.
- `CampaignSavedData` and `CampaignService` state-first tracer contracts.
- `LectureEncounterManager` with one Slide Deck telegraph/vulnerability path.
- Real start-to-reward, duplicate-start, competing-start, and replayed-victory GameTests in the existing invoker.

<verification>
- A clean pinned-Java invocation creates and timestamp-checks the exact JAR before archive claims.
- The transformed server test proves the thin path before interface extraction or presentation expansion.
</verification>

<success_criteria>
- One real Contract-to-reward path is runnable and retained as the architectural skeleton.
- Start competition and reward replay cannot create a second runtime, ledger entry, or block mutation.
</success_criteria>

<output>Create `.planning/phases/02-persistent-lecture-vertical-slice/02-01-SUMMARY.md` when done.</output>
