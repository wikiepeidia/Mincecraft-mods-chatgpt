---
phase: 02-persistent-lecture-vertical-slice
plan: 04
type: execute
wave: 5
depends_on: [02-02]
files_modified:
  - src/main/java/dev/developershell/campaign/PlayerCampaignState.java
  - src/main/java/dev/developershell/campaign/CampaignEvent.java
  - src/main/java/dev/developershell/campaign/CampaignTransition.java
  - src/main/java/dev/developershell/campaign/CampaignReducer.java
  - src/main/java/dev/developershell/campaign/CampaignSavedData.java
  - src/main/java/dev/developershell/campaign/CampaignService.java
  - src/test/java/dev/developershell/campaign/CampaignReducerTest.java
  - src/test/java/dev/developershell/campaign/CampaignCodecTest.java
autonomous: true
requirements: [FND-06, FND-07, LECT-02]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Replaying the same persisted event, encounter UUID, reward callback, or cooldown restoration is a no-op after its first committed transition; chapter, entitlement, ledger, and deadline never regress."
    - "Schema-v1 round-trips every supported field; future schema is explicit read-only state that cannot be overwritten or mutated."
    - "Only one production facade loads current state, reduces once, persists accepted next state, marks it dirty, and then dispatches bounded effect intents."
  artifacts:
    - path: "src/main/java/dev/developershell/campaign/CampaignEvent.java"
      provides: "Closed owner/encounter-scoped durable event family"
      exports: ["CampaignEvent"]
    - path: "src/main/java/dev/developershell/campaign/CampaignTransition.java"
      provides: "Accepted/no-op next state and effect intents"
      exports: ["CampaignTransition", "EffectIntent"]
    - path: "src/main/java/dev/developershell/campaign/CampaignReducer.java"
      provides: "Pure monotonic state machine"
      exports: ["CampaignReducer", "reduce"]
    - path: "src/main/java/dev/developershell/campaign/CampaignSavedData.java"
      provides: "Versioned codec and UUID-keyed world storage"
      exports: ["CampaignSavedData", "TYPE", "get"]
  key_links:
    - from: "src/main/java/dev/developershell/campaign/CampaignService.java"
      to: "src/main/java/dev/developershell/campaign/CampaignReducer.java"
      via: "load current state and reduce one closed event"
      pattern: "CampaignReducer.reduce"
    - from: "src/main/java/dev/developershell/campaign/CampaignService.java"
      to: "src/main/java/dev/developershell/campaign/CampaignSavedData.java"
      via: "accepted replacement plus setDirty before intent execution"
      pattern: "setDirty"
---

# Phase 2 Plan 04 — Monotonic Reducer and Schema-v1 Persistence

## Phase Goal

**As a** survival player, **I want to** discover, fail, retry, and complete Professor Infinite Slides, **so that** my Lecture progress and rewards survive restarts without exploits.

<objective>
Expand the tracer's state seam into the complete pure reducer and versioned persistence contract.

Purpose: Make every later Fabric callback a replay-safe input rather than a competing source of progression truth.
Output: Closed events, pure transitions, stable codec, read-only future-schema handling, one state-first service, and exhaustive JUnit proof.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.agents/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-CONTEXT.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-RESEARCH.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-PATTERNS.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-VALIDATION.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-14-SUMMARY.md
@AGENTS.md

<interfaces>
Plan 14 freezes schema 1, `PlayerCampaignState`, `CampaignSavedData.TYPE`, and the state-before-effect `CampaignService` facade. Extend those exact identities. Decision coverage: D-15, D-16, D-17, D-18, D-19, D-20; validation rows 02-STATE-01 and reducer portions of 02-REWARD-01.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify every monotonic event and replay rule in a pure reducer</name>
  <read_first>
    - `PlayerCampaignState.java` and `CampaignService.java` from Plan 01 — preserve fields and entrypoint names.
    - `02-CONTEXT.md` D-15 through D-19 — one active encounter, owner matching, terminal cleanup intent, entitlements, and first-only ledger.
    - `02-RESEARCH.md` Pattern 2 and durable-field table — stable encoded names and effect-intent ordering.
    - `02-VALIDATION.md` rows 02-STATE-01 and 02-REWARD-01 — mismatches, replays, cooldown, and normalization cases.
  </read_first>
  <files>src/main/java/dev/developershell/campaign/PlayerCampaignState.java, src/main/java/dev/developershell/campaign/CampaignEvent.java, src/main/java/dev/developershell/campaign/CampaignTransition.java, src/main/java/dev/developershell/campaign/CampaignReducer.java, src/test/java/dev/developershell/campaign/CampaignReducerTest.java</files>
  <behavior>
    - START accepts only READY/RETAKE_READY without an active ref, records exact owner/desk/retry/encounter identity, and increments attempts once.
    - Matching terminal events clear active state, retain all monotonic values, create one Retake entitlement, and become no-ops when replayed or mismatched.
    - Matching first victory commits PASSED, Sheet entitlement, and first-Remote ledger once; stale/wrong-owner/replayed events yield zero intents.
    - Retake fallback, Sheet recovery, cooldown start, and ready-notice events cannot regress ledgers, attempts, chapter, or deadlines.
  </behavior>
  <action>
    Write fail-first `CampaignReducerTest` covering START, death, escape, timeout, dimension change, disconnect, abort, server stop, entity unload, reload normalization, victory, Retake reconciliation/fallback, Sheet recovery, Remote cooldown, and ready notice. Use explicit UUIDs/game times/seeds. Implement a closed `CampaignEvent` family and immutable `CampaignTransition.accepted`/`noOp`; stable encoded values must not derive from enum `name()`. Enforce owner plus encounter matching and all monotonic invariants per D-15 through D-19.
  </action>
  <acceptance_criteria>
    - Every event has matching, stale, wrong-owner, wrong-encounter, and replay assertions with exact next state/intents.
    - Pure domain classes import no Fabric event, client, filesystem, network, wall-clock, or unseeded randomness surface.
    - Reordering cannot decrement chapter/attempts, clear permanent entitlements, duplicate ledger entries, or shorten a committed cooldown.
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop';$jdkRoot=(Resolve-Path '.work/toolchain/temurin-25.0.4+7-x64').Path;$env:JAVA_HOME=$jdkRoot;$env:Path="$(Join-Path $jdkRoot 'bin');$env:Path";$jvm=@("-Dorg.gradle.java.installations.paths=$jdkRoot",'-Dorg.gradle.java.installations.auto-detect=false','-Dorg.gradle.java.installations.auto-download=false');& .\gradlew.bat @jvm test --tests 'dev.developershell.campaign.CampaignReducerTest' compileJava --offline --no-daemon --console=plain --init-script scripts/loom-resolution.init.gradle;$gradleExit=$LASTEXITCODE;if($gradleExit -ne 0){throw "reducer gate failed with exit $gradleExit"};$pureFiles=@('src/main/java/dev/developershell/campaign/CampaignEvent.java','src/main/java/dev/developershell/campaign/CampaignTransition.java','src/main/java/dev/developershell/campaign/CampaignReducer.java','src/main/java/dev/developershell/campaign/PlayerCampaignState.java');if(Select-String -Path $pureFiles -Pattern 'net\.fabricmc|net\.minecraft\.client|java\.io|java\.nio|java\.net|currentTimeMillis|nanoTime|Math\.random'){throw 'pure reducer dependency violation'}</automated>
  </verify>
  <done>Every durable campaign event is owner-scoped, pure, monotonic, replay-safe, and exhaustively asserted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Encode schema 1 and enforce the sole persist-then-effect boundary</name>
  <read_first>
    - Task 1 state/event/transition contracts — encode every durable field and no runtime-only object.
    - `02-RESEARCH.md` exact `SavedDataType`/`SavedDataStorage` API and Saved Data skeleton — use 26.2 signatures.
    - Plan 01 `CampaignSavedData` and `CampaignService` — evolve in place without a second map or static world singleton.
  </read_first>
  <files>src/main/java/dev/developershell/campaign/PlayerCampaignState.java, src/main/java/dev/developershell/campaign/CampaignSavedData.java, src/main/java/dev/developershell/campaign/CampaignService.java, src/test/java/dev/developershell/campaign/CampaignCodecTest.java</files>
  <behavior>
    - Supported schema 1 round-trips every field and validates UUID map key against encoded owner.
    - Future schema decodes to explicit incompatible read-only disposition; mutation and overwrite are rejected.
    - Persisted ACTIVE normalization accepts once to RETAKE_READY without reconstructing cast/runtime state.
    - `CampaignService` executes effects only after accepted replacement and `setDirty()`.
  </behavior>
  <action>
    Write fail-first `CampaignCodecTest` for complete round trip, stable names, missing/corrupt supported data, map-key mismatch, future schema, and active-to-Retake normalization. Implement the 26.2 `Codec`/`SavedDataType` using the Overworld data storage. Keep in-flight timers, boss bars, helpers, and particles out of the codec. Make `CampaignService` the only Minecraft adapter that loads state, reduces, replaces, marks dirty, then dispatches immutable intents per D-16/D-17/D-20.
  </action>
  <acceptance_criteria>
    - Schema 1 preserves all durable data and future schema stays visible/read-only without default overwrite.
    - Active saved intent becomes one safe Retake transition; no cast or boss presentation resumes from disk.
    - A rejected/no-op transition performs no write and no effect; accepted effects are unreachable before `setDirty()`.
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop';$jdkRoot=(Resolve-Path '.work/toolchain/temurin-25.0.4+7-x64').Path;$env:JAVA_HOME=$jdkRoot;$env:Path="$(Join-Path $jdkRoot 'bin');$env:Path";$jvm=@("-Dorg.gradle.java.installations.paths=$jdkRoot",'-Dorg.gradle.java.installations.auto-detect=false','-Dorg.gradle.java.installations.auto-download=false');& .\gradlew.bat @jvm test --tests 'dev.developershell.campaign.*' compileJava auditDirectDependencies --offline --no-daemon --console=plain --init-script scripts/loom-resolution.init.gradle;$gradleExit=$LASTEXITCODE;if($gradleExit -ne 0){throw "codec/service gate failed with exit $gradleExit"}</automated>
  </verify>
  <done>Campaign persistence is versioned, owner-consistent, future-safe, and coupled to effects through exactly one commit-first facade.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Runtime callbacks -> reducer | Repeated, stale, reordered, or mismatched events enter progression. |
| Decoded SavedData -> mutation | Corrupt/future data must not be silently reset or overwritten. |
| Accepted transition -> effects | Inventory/world/presentation effects must follow durable state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-STATE-01 | Tampering | Event replay/order | high | mitigate | Closed events, owner+encounter guards, monotonic invariants, and exhaustive matrix. |
| T-02-STATE-02 | Tampering | Saved schema | high | mitigate | Explicit schema/stable names, UUID-key validation, read-only future disposition, no overwrite. |
| T-02-STATE-03 | Repudiation | Accepted/no-op result | medium | mitigate | Stable debug reason and exact state/intent assertions for every input family. |
</threat_model>

## Artifacts this phase produces

- `CampaignEvent`, `CampaignTransition`, and pure `CampaignReducer` covering every Phase 2 durable input.
- Complete schema-v1 `CampaignSavedData` codec and read-only future-schema disposition.
- Sole `CampaignService` load-reduce-persist-dirty-effect boundary.
- `CampaignReducerTest` and `CampaignCodecTest` matrices for transitions, replays, ordering, codec, and normalization.

<verification>
- Focused pure tests prove all transition and schema rules without a transformed world.
- Compile/dependency scans preserve exact 26.2 APIs and offline dependency tuple.
</verification>

<success_criteria>
- Save/reload and callback replay cannot duplicate or regress any campaign/reward/cooldown field.
- Later world adapters have one deterministic accepted/no-op interface and no authority to mutate progression directly.
</success_criteria>

<output>Create `.planning/phases/02-persistent-lecture-vertical-slice/02-04-SUMMARY.md` when done.</output>
