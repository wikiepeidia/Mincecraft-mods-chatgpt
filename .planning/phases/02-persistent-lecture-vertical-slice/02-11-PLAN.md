---
phase: 02-persistent-lecture-vertical-slice
plan: 11
type: execute
wave: 11
depends_on: [02-07, 02-08, 02-10]
files_modified:
  - src/main/java/dev/developershell/campaign/PlayerCampaignState.java
  - src/main/java/dev/developershell/campaign/CampaignEvent.java
  - src/main/java/dev/developershell/campaign/CampaignReducer.java
  - src/main/java/dev/developershell/campaign/CampaignService.java
  - src/test/java/dev/developershell/campaign/CampaignReducerTest.java
autonomous: true
requirements: [FND-06, FND-07, LECT-02]
estimate: {tokens: 18000, raw_tokens: 18000, tasks: 1, confidence: low}
must_haves:
  truths:
    - "Duplicate or stale victory, failure, cleanup, inventory fallback, and reward callbacks issue no duplicate Attendance Sheet entitlement, Remote, Retake Form, or presentation."
    - "Victory racing cleanup, disconnect, or reload resolves only for the matching owner and encounter UUID after state is committed; one terminal state and one reward ledger entry win."
  artifacts:
    - {path: "src/main/java/dev/developershell/campaign/CampaignReducer.java", provides: "First-terminal-wins victory/failure ordering"}
    - {path: "src/test/java/dev/developershell/campaign/CampaignReducerTest.java", provides: "Victory race/replay ordering proof"}
  key_links:
    - {from: "src/main/java/dev/developershell/campaign/CampaignService.java", to: "src/main/java/dev/developershell/campaign/CampaignReducer.java", via: "every terminal callback receives one accepted/no-op result", pattern: "CampaignReducer"}
---

# Phase 2 Plan 11 — Exactly-Once Victory and Sheet Recovery

## Phase Goal

**As a** survival player, **I want to** discover, fail, retry, and complete Professor Infinite Slides, **so that** my Lecture progress and rewards survive restarts without exploits.

<objective>
Close victory-versus-cleanup races in persisted campaign state before physical reward reconciliation.

Purpose: Ensure callback reordering cannot create two terminal winners or two reward intents.
Output: Reward-race reducer/service rules and exhaustive pure ordering tests.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.agents/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-CONTEXT.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-UI-SPEC.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-RESEARCH.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-VALIDATION.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-10-SUMMARY.md
@AGENTS.md

<interfaces>
Victory and all terminal events already enter the same reducer/service. Retake physical projection exists and may be reused for full-inventory fallback without sharing entitlements. Decision coverage: D-16, D-17, D-18, D-19, D-20; validation row 02-REWARD-01.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Decide every victory/terminal ordering in persisted state</name>
  <read_first>
    - Plan 04 reducer/service and Plan 05 lifecycle event ordering.
    - `02-CONTEXT.md` D-16 through D-19.
    - `02-VALIDATION.md` 02-REWARD-01 and Required GameTest 4.
  </read_first>
  <files>src/main/java/dev/developershell/campaign/PlayerCampaignState.java, src/main/java/dev/developershell/campaign/CampaignEvent.java, src/main/java/dev/developershell/campaign/CampaignReducer.java, src/main/java/dev/developershell/campaign/CampaignService.java, src/test/java/dev/developershell/campaign/CampaignReducerTest.java</files>
  <behavior>
    - Matching victory first commits PASSED, Sheet entitlement, and `remoteIssued` ledger exactly once.
    - Victory before/after death, escape, timeout, disconnect, reload, unload, and cleanup yields one terminal winner based on first accepted matching transition.
    - Stale/wrong-owner/replayed physical reconciliation has zero state and presentation intents.
  </behavior>
  <action>
    Add fail-first explicit UUID/order tests for matching first victory, every terminal race in both orders, stale/wrong owner, repeated death/victory, and reconciliation replay. Tighten reducer/service accepted/no-op results so the first persisted matching terminal state wins and effects exist only on that transition per D-16 through D-19/D-20.
  </action>
  <acceptance_criteria>
    - Every ordering converges to one terminal state, entitlement, ledger entry, and result intent.
    - No replay can generate a second reward/presentation intent.
    - Ordinary entity loot remains unrelated to Sheet/Remote.
  </acceptance_criteria>
  <verify><automated>$ErrorActionPreference='Stop';$jdkRoot=(Resolve-Path '.work/toolchain/temurin-25.0.4+7-x64').Path;$env:JAVA_HOME=$jdkRoot;$env:Path="$(Join-Path $jdkRoot 'bin');$env:Path";$jvm=@("-Dorg.gradle.java.installations.paths=$jdkRoot",'-Dorg.gradle.java.installations.auto-detect=false','-Dorg.gradle.java.installations.auto-download=false');& .\gradlew.bat @jvm test --tests 'dev.developershell.campaign.CampaignReducerTest' compileJava --offline --no-daemon --console=plain --init-script scripts/loom-resolution.init.gradle;$gradleExit=$LASTEXITCODE;if($gradleExit -ne 0){throw "victory ordering gate failed with exit $gradleExit"}</automated></verify>
  <done>One persisted terminal transition wins every reward/cleanup race.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Terminal callbacks -> persisted winner | Reordered victory/failure inputs compete for one state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-REWARD-01 | Tampering | Victory/reward replay | high | mitigate | Matching owner+encounter, first accepted terminal, persisted ledger before effects, exhaustive races. |
</threat_model>

## Artifacts this phase produces

- Victory/terminal race reducer tests and exactly-once reward intent semantics.

<verification>
- Pure ordering tests prove the first-terminal-wins state and intent contract.
</verification>

<success_criteria>
- One matching terminal event wins and produces at most one committed reward intent.
</success_criteria>

<output>Create `.planning/phases/02-persistent-lecture-vertical-slice/02-11-SUMMARY.md` when done.</output>
