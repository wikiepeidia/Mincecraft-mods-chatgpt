---
phase: 02-persistent-lecture-vertical-slice
plan: 08
type: execute
wave: 9
depends_on: [02-05, 02-06, 02-07]
files_modified:
  - src/main/java/dev/developershell/registry/ModItems.java
  - src/main/java/dev/developershell/lecture/RetakeService.java
  - src/main/java/dev/developershell/item/RetakeFormItem.java
  - src/main/java/dev/developershell/server/DeskInteraction.java
  - src/main/java/dev/developershell/command/DevHellCommands.java
  - src/main/java/dev/developershell/DevelopersHell.java
  - src/main/resources/assets/developers_hell/lang/en_us.json
  - src/gametest/java/dev/developershell/gametest/RetakeGameTests.java
  - src/gametest/resources/fabric.mod.json
autonomous: true
requirements: [FND-06, FND-07, CAMP-02, LECT-02]
estimate: {tokens: 38000, raw_tokens: 38000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Cleanup issues one owner-bound Form to inventory or one tracked owner-targeted fallback at the persisted retry point; duplicate callbacks issue nothing."
    - "Empty-hand use on the matching Desk recovers one missing Form only in RETAKE_READY, while existing/wrong-owner/wrong-desk/wrong-state requests are localized no-ops."
    - "Retake use reruns the complete arena validator, commits a new attempt, and only then consumes one Form and clears the old entitlement/fallback."
  artifacts:
    - {path: "src/main/java/dev/developershell/server/DeskInteraction.java", provides: "Empty-hand matching-Desk recovery adapter", exports: ["DeskInteraction", "register"]}
    - {path: "src/gametest/java/dev/developershell/gametest/RetakeGameTests.java", provides: "Inventory/fallback/recovery/retry matrix"}
  key_links:
    - {from: "src/main/java/dev/developershell/registry/ModItems.java", to: "src/main/java/dev/developershell/item/RetakeFormItem.java", via: "the existing stable RETAKE_FORM key is constructed by the custom item factory", pattern: "RetakeFormItem::new"}
    - {from: "src/main/java/dev/developershell/server/DeskInteraction.java", to: "src/main/java/dev/developershell/lecture/RetakeService.java", via: "UseBlockCallback delegates all recovery", pattern: "UseBlockCallback"}
    - {from: "src/main/java/dev/developershell/item/RetakeFormItem.java", to: "src/main/java/dev/developershell/lecture/ArenaValidator.java", via: "complete revalidation before start/consume", pattern: "validate"}
---

# Phase 2 Plan 08 — Retake Delivery, Recovery, and Retry

## Phase Goal

**As a** survival player, **I want to** discover, fail, retry, and complete Professor Infinite Slides, **so that** my Lecture progress and rewards survive restarts without exploits.

<objective>
Materialize the exactly-one Retake domain through real inventory, fallback entity, empty-hand Desk, item, and recovery-command paths.

Purpose: Ensure full inventory or lost physical Forms never strand the owner or create two attempts.
Output: Owner-bound Form behavior, tracked fallback, Desk recovery, safe commands, localized copy, and transformed GameTests.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.agents/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-CONTEXT.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-UI-SPEC.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-RESEARCH.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-VALIDATION.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-07-SUMMARY.md
@AGENTS.md

<interfaces>
Use Plan 07's sole `RetakeService`, Plan 06's complete `ArenaValidator`, and current 26.2 `UseBlockCallback`/`ItemEntity.setTarget(UUID)` APIs. Decision coverage: D-08, D-16, D-17, D-18, D-20; validation rows 02-ITEM-01, 02-LIFE-01, 02-REWARD-01.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Materialize one Form or tracked fallback and recover loss</name>
  <read_first>
    - `RetakeService.java` and Plan 07 reducer tests — accepted intents are the only physical authority.
    - `02-UI-SPEC.md` Retake issuance/recovery steps and exact copy.
    - `02-RESEARCH.md` transactional interaction and current block-use/entity APIs.
  </read_first>
  <files>src/main/java/dev/developershell/lecture/RetakeService.java, src/main/java/dev/developershell/server/DeskInteraction.java, src/main/java/dev/developershell/DevelopersHell.java, src/main/resources/assets/developers_hell/lang/en_us.json, src/gametest/java/dev/developershell/gametest/RetakeGameTests.java, src/gametest/resources/fabric.mod.json</files>
  <behavior>
    - Existing inventory Form/tracked fallback suppresses another grant.
    - Full inventory creates one owner-targeted ItemEntity at validated retry and records its UUID; loss clears once and permits one replacement.
    - Empty-hand matching Desk recovers only when entitled and both representations are absent.
  </behavior>
  <action>
    Write fail-first GameTests for insertion, full-inventory fallback, duplicates, existing representation, wrong-player pickup/use, unload/despawn/destruction, and empty-hand recovery. Implement physical effect ports in `RetakeService`; register one `DeskInteraction` callback. Persist fallback reservation/reference around spawn so failure returns to recoverable state. Emit exact localized issued/fallback/recovered/already/nothing copy per D-17/D-18/D-20.
  </action>
  <acceptance_criteria>
    - One entitlement projects to at most one usable owner representation under every loss/replay case.
    - Another player cannot use fallback/Form to start or clear owner state.
    - Empty-hand recovery is matching-Desk and RETAKE_READY only.
  </acceptance_criteria>
  <verify><automated>$ErrorActionPreference='Stop';$jdkRoot=(Resolve-Path '.work/toolchain/temurin-25.0.4+7-x64').Path;$env:JAVA_HOME=$jdkRoot;$env:Path="$(Join-Path $jdkRoot 'bin');$env:Path";$jvm=@("-Dorg.gradle.java.installations.paths=$jdkRoot",'-Dorg.gradle.java.installations.auto-detect=false','-Dorg.gradle.java.installations.auto-download=false');& .\gradlew.bat @jvm test runGameTest --offline --no-daemon --console=plain --init-script scripts/loom-resolution.init.gradle;$gradleExit=$LASTEXITCODE;if($gradleExit -ne 0){throw "Retake delivery/recovery gate failed with exit $gradleExit"}</automated></verify>
  <done>Inventory loss and fallback loss remain recoverable without duplicate Forms.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Revalidate and commit retry before consuming the Form</name>
  <read_first>
    - `registry/ModItemIds.java` and `registry/ModItems.java` — preserve the existing stable Retake key while replacing only its generic factory with the custom item class.
    - `RetakeFormItem.java`, `RetakeService.java`, and Plan 06 `ArenaValidator`.
    - `02-UI-SPEC.md` Retake validation failure/success states.
    - `02-RESEARCH.md` current command permission API and transactional Form use.
  </read_first>
  <files>src/main/java/dev/developershell/registry/ModItems.java, src/main/java/dev/developershell/lecture/RetakeService.java, src/main/java/dev/developershell/item/RetakeFormItem.java, src/main/java/dev/developershell/command/DevHellCommands.java, src/main/resources/assets/developers_hell/lang/en_us.json, src/gametest/java/dev/developershell/gametest/RetakeGameTests.java, src/gametest/resources/fabric.mod.json</files>
  <behavior>
    - A stack built from the production registry instance `ModItems.RETAKE_FORM` executes owner+matching Desk+RETAKE_READY+valid-arena acceptance before one Form is consumed.
    - Wrong owner/Desk/state, active encounter, or changed arena consumes/spawns/writes nothing.
    - `/devhell abort` and `/devhell recover retake` require game-master permission and call shared services.
  </behavior>
  <action>
    Add fail-first wrong-owner/desk/state/changed-arena and success GameTests. Preserve the existing stable `ModItemIds.RETAKE_FORM` key and replace its generic `ModItems` factory with `RetakeFormItem::new`; do not register a second ID or parallel instance. Obtain the successful Desk-use stack from `ModItems.RETAKE_FORM` in `RetakeGameTests`, then route that production item through complete arena validation and `RetakeService.startRetake`; materialize only accepted intents. Add current `Commands.hasPermission(Commands.LEVEL_GAMEMASTERS)` recovery/testing adapters without making commands normal discovery per D-08/D-16/D-20.
  </action>
  <acceptance_criteria>
    - Rejected retry is a complete inventory/state/entity/block no-op.
    - Accepted retry persists attempt/active state before consuming exactly one Form and clearing old reference.
    - `BuiltInRegistries.ITEM` resolves the stable Retake key to the same `RetakeFormItem` instance exposed by `ModItems.RETAKE_FORM`, and the Desk-use GameTest uses a stack from that instance.
    - Commands cannot bypass owner/state/arena validation.
  </acceptance_criteria>
  <verify><automated>$ErrorActionPreference='Stop';$jdkRoot=(Resolve-Path '.work/toolchain/temurin-25.0.4+7-x64').Path;$env:JAVA_HOME=$jdkRoot;$env:Path="$(Join-Path $jdkRoot 'bin');$env:Path";$jvm=@("-Dorg.gradle.java.installations.paths=$jdkRoot",'-Dorg.gradle.java.installations.auto-detect=false','-Dorg.gradle.java.installations.auto-download=false');& .\gradlew.bat @jvm test runGameTest auditDirectDependencies build --offline --no-daemon --console=plain --init-script scripts/loom-resolution.init.gradle;$gradleExit=$LASTEXITCODE;if($gradleExit -ne 0){throw "Retake registry/retry gate failed with exit $gradleExit"};$registryText=Get-Content -LiteralPath 'src/main/java/dev/developershell/registry/ModItems.java' -Raw;if($registryText -notmatch 'RetakeFormItem::new'){throw 'stable Retake key is not bound to RetakeFormItem'};$testText=Get-Content -LiteralPath 'src/gametest/java/dev/developershell/gametest/RetakeGameTests.java' -Raw;if($testText -notmatch 'ModItems\.RETAKE_FORM'){throw 'Retake GameTest does not use the production registry item'}</automated></verify>
  <done>The owner always has one safe, fully revalidated, state-first route from failure to a new attempt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Entitlement -> inventory/entity | Lossy representations can unload, despawn, or be touched by another player. |
| Form/Desk request -> retry | Owner/state/Desk/arena are server-validated before consume/start. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-RET-01 | Spoofing | Form/fallback ownership | high | mitigate | Owner+failed encounter state, tracked entity UUID, target owner, wrong-owner tests. |
| T-02-RET-02 | Tampering | Retry replay | high | mitigate | Complete revalidation and commit-before-consume/materialize. |
| T-02-RET-03 | Elevation of Privilege | Recovery commands | medium | mitigate | Game-master predicate and shared service path. |
</threat_model>

## Artifacts this phase produces

- Concrete exactly-one Form/fallback materialization and loss recovery in `RetakeService`.
- `DeskInteraction`, owner-bound `RetakeFormItem`, and game-master recovery adapters.
- `RetakeGameTests` covering inventory, fallback, recovery, ownership, changed arena, commands, and successful retry.

<verification>
- Real GameTests prove physical-loss and retry transaction behavior.
- Full build keeps stable IDs, side separation, and exact dependencies.
</verification>

<success_criteria>
- Full inventory or lost Form never strands or duplicates the player.
- Every successful retry is revalidated and committed before physical consumption.
</success_criteria>

<output>Create `.planning/phases/02-persistent-lecture-vertical-slice/02-08-SUMMARY.md` when done.</output>
