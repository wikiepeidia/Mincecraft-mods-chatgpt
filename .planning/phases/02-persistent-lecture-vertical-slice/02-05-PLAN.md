---
phase: 02-persistent-lecture-vertical-slice
plan: 05
type: execute
wave: 7
depends_on: [02-04, 02-16]
files_modified:
  - src/main/java/dev/developershell/server/CampaignLifecycle.java
  - src/main/java/dev/developershell/server/DevelopersHellRuntime.java
  - src/main/java/dev/developershell/lecture/LectureEncounterManager.java
  - src/main/java/dev/developershell/entity/ProfessorInfiniteSlidesEntity.java
  - src/main/java/dev/developershell/DevelopersHell.java
  - src/main/resources/assets/developers_hell/lang/en_us.json
  - src/gametest/java/dev/developershell/gametest/LectureLifecycleGameTests.java
  - src/gametest/resources/fabric.mod.json
autonomous: true
requirements: [FND-06, FND-07, LECT-02]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Death, escape, timeout, dimension change, disconnect, abort, entity unload, reload normalization, and server stop each submit one matching terminal event before runtime cleanup; duplicates are no-ops."
    - "There is no network loading presentation: Contract activation validates synchronously, while reload cancels in-flight combat and visibly returns the player to a safe Retake state."
    - "Missing boss, stale UUID, orphan entity, or incomplete cast never renders partial combat; cleanup wins and preserves chapter, desk, retry point, attempts, entitlements, ledger, and cooldown."
    - "A real dedicated-server clean-stop later proves the registered SERVER_STOPPING callback; GameTest verifies the same stop-handler/service seam in-process and does not claim assertions after its server exits."
  artifacts:
    - path: "src/main/java/dev/developershell/server/CampaignLifecycle.java"
      provides: "One-time Fabric callback adapter and testable stop-handler seam"
      exports: ["CampaignLifecycle", "register", "onServerStopping"]
    - path: "src/main/java/dev/developershell/lecture/LectureEncounterManager.java"
      provides: "State-first bounded cleanup of all owned runtime objects"
    - path: "src/gametest/java/dev/developershell/gametest/LectureLifecycleGameTests.java"
      provides: "Failure/reload/orphan/stop-seam convergence matrix"
  key_links:
    - from: "src/main/java/dev/developershell/server/CampaignLifecycle.java"
      to: "src/main/java/dev/developershell/campaign/CampaignService.java"
      via: "typed callbacks submit closed events only"
      pattern: "CampaignEvent"
    - from: "src/main/java/dev/developershell/server/CampaignLifecycle.java"
      to: "src/main/java/dev/developershell/lecture/LectureEncounterManager.java"
      via: "accepted terminal state precedes runtime close/discard"
      pattern: "cleanup"
  prohibitions:
    - statement: "MUST NOT replace or alter a player's vanilla bed or respawn state, resume an in-flight cast after reload or unload, or mutate or destroy arena blocks by default."
      status: flagged-unverified
---

# Phase 2 Plan 05 — Lifecycle Convergence and Orphan Rejection

## Phase Goal

**As a** survival player, **I want to** discover, fail, retry, and complete Professor Infinite Slides, **so that** my Lecture progress and rewards survive restarts without exploits.

<objective>
Connect real Fabric lifecycle callbacks to the pure persisted transition seam and make every interruption converge once to safe Retake.

Purpose: Prevent reload, unload, failure, and shutdown from leaving partial combat or recursively recording another failure.
Output: One lifecycle adapter, state-first runtime cleanup, orphan rejection, transformed exit-path tests, and a clean-stop log seam for final dedicated-server evidence.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.agents/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-CONTEXT.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-UI-SPEC.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-RESEARCH.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-PATTERNS.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-VALIDATION.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-04-SUMMARY.md
@AGENTS.md

<interfaces>
Plan 04 provides closed failure/normalization events and the sole persist-then-effect service. Use current 26.2 `ServerPlayerEvents.AFTER_RESPAWN/JOIN/LEAVE`, `ServerLivingEntityEvents.AFTER_DEATH`, `ServerEntityEvents.ALLOW_LOAD/ENTITY_LOAD/ENTITY_UNLOAD`, and `ServerLifecycleEvents.SERVER_STOPPING`. Decision coverage: D-17, D-18, D-20; validation row 02-LIFE-01.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Converge every live exit and orphan through one cleanup seam</name>
  <read_first>
    - Plan 04 `CampaignEvent`, `CampaignService`, and reducer tests — adapters may submit events but never mutate state.
    - `LectureEncounterManager` and `ProfessorInfiniteSlidesEntity` — enumerate the complete bounded owned object set and persisted identity.
    - `02-RESEARCH.md` Lifecycle and Cleanup Order plus exact lifecycle API table.
    - `02-UI-SPEC.md` State and Cleanup Matrix — presentation must disappear atomically on every exit.
    - `02-VALIDATION.md` row 02-LIFE-01 and Required GameTests 3/5.
  </read_first>
  <files>src/main/java/dev/developershell/server/CampaignLifecycle.java, src/main/java/dev/developershell/server/DevelopersHellRuntime.java, src/main/java/dev/developershell/lecture/LectureEncounterManager.java, src/main/java/dev/developershell/entity/ProfessorInfiniteSlidesEntity.java, src/gametest/java/dev/developershell/gametest/LectureLifecycleGameTests.java</files>
  <behavior>
    - Matching death/escape/timeout/dimension/disconnect/abort/unload/reload each commits RETAKE_READY and clears active ref before bar/cast/helper/entity cleanup.
    - Wrong-owner/wrong-encounter/repeated and cleanup-generated unload callbacks change neither state nor presentation.
    - JOIN/startup normalizes ACTIVE once and no queued cast lands afterward.
    - `ALLOW_LOAD` rejects any Professor/owned helper not matching the current saved owner+encounter reference.
  </behavior>
  <action>
    Write fail-first `LectureLifecycleGameTests` for each runtime exit except actual process termination, plus duplicates, wrong identities, load normalization, orphan rejection, and no post-rejoin impact. Implement `CampaignLifecycle` as typed callback-to-event translation only. The manager inspects only its bounded active runtimes for escape/timeout/dimension/missing-player conditions; it never scans all entities or rereads disk. On an accepted terminal transition, clear active durable state first, then close the boss bar, cancel timers/emission, discard helpers and Professor, and invoke Retake intent. Cleanup-triggered unload is stale per D-17/D-18/D-20.
  </action>
  <acceptance_criteria>
    - Every testable exit and replay variant has real state plus world/presentation assertions.
    - No in-flight attack resumes after normalization; stale disk entities cannot become active combat objects.
    - Runtime cleanup owns a bounded set and performs no global scan, block mutation, bed/respawn setter, network access, or client call.
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop';$jdkRoot=(Resolve-Path '.work/toolchain/temurin-25.0.4+7-x64').Path;$env:JAVA_HOME=$jdkRoot;$env:Path="$(Join-Path $jdkRoot 'bin');$env:Path";$jvm=@("-Dorg.gradle.java.installations.paths=$jdkRoot",'-Dorg.gradle.java.installations.auto-detect=false','-Dorg.gradle.java.installations.auto-download=false');& .\gradlew.bat @jvm test runGameTest --offline --no-daemon --console=plain --init-script scripts/loom-resolution.init.gradle;$gradleExit=$LASTEXITCODE;if($gradleExit -ne 0){throw "lifecycle GameTests failed with exit $gradleExit"};$testPath='src/gametest/java/dev/developershell/gametest/LectureLifecycleGameTests.java';if(-not (Test-Path -LiteralPath $testPath -PathType Leaf)){throw 'LectureLifecycleGameTests source missing'};$testText=Get-Content -LiteralPath $testPath -Raw;foreach($casePattern in @('(?i)death','(?i)escape','(?i)timeout','(?i)dimension','(?i)disconnect','(?i)abort','(?i)unload','(?i)orphan','(?i)reload')){if($testText -notmatch $casePattern){throw "lifecycle case missing: $casePattern"}}</automated>
  </verify>
  <done>All in-process lifecycle paths converge once to durable safe state and remove every encounter-owned object.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register the real stop callback and prove its handler without post-exit assertions</name>
  <read_first>
    - Task 1 `CampaignLifecycle` — expose one handler called by both the callback and in-process test.
    - `DevelopersHell.java` and `DevelopersHellRuntime.java` — register once after immutable composition.
    - `02-RESEARCH.md` current `SERVER_STOPPING` signature and PowerShell server-smoke guidance.
    - `02-VALIDATION.md` 02-LIFE-01 and 02-GATE-01 distinction — GameTest versus dedicated-server evidence.
  </read_first>
  <files>src/main/java/dev/developershell/server/CampaignLifecycle.java, src/main/java/dev/developershell/DevelopersHell.java, src/main/resources/assets/developers_hell/lang/en_us.json, src/gametest/java/dev/developershell/gametest/LectureLifecycleGameTests.java, src/gametest/resources/fabric.mod.json</files>
  <behavior>
    - Calling the stop handler in-process submits one SERVER_STOP event per active encounter, persists terminal state first, and leaves no runtime object.
    - The actual Fabric callback calls that exact handler and emits one stable sanitized completion marker.
    - A duplicate handler call is a no-op; the GameTest never stops its own server and never asserts after process exit.
  </behavior>
  <action>
    Add fail-first in-process stop-handler cases and register `ServerLifecycleEvents.SERVER_STOPPING` once from bootstrap. The callback delegates to `CampaignLifecycle.onServerStopping(server)` and logs a stable `DEVELOPERS_HELL_SERVER_STOPPING_CLEANUP_COMPLETE` marker only after all accepted transitions and cleanup complete. Do not stop the GameTest server. Reserve proof that the real callback fires during an actual shutdown for Plan 13's bounded `runProductionServer` clean-stop smoke; document this separation in test names and assertions.
  </action>
  <acceptance_criteria>
    - In-process tests prove stop-handler state/effect ordering and idempotency without terminating the test server.
    - Production bootstrap registers the real callback once and the completion marker has no path, UUID, identity, or private data.
    - Final evidence has a concrete marker to require after a real bounded dedicated-server stop.
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop';$jdkRoot=(Resolve-Path '.work/toolchain/temurin-25.0.4+7-x64').Path;$env:JAVA_HOME=$jdkRoot;$env:Path="$(Join-Path $jdkRoot 'bin');$env:Path";$jvm=@("-Dorg.gradle.java.installations.paths=$jdkRoot",'-Dorg.gradle.java.installations.auto-detect=false','-Dorg.gradle.java.installations.auto-download=false');& .\gradlew.bat @jvm test runGameTest auditDirectDependencies build --offline --no-daemon --console=plain --init-script scripts/loom-resolution.init.gradle;$gradleExit=$LASTEXITCODE;if($gradleExit -ne 0){throw "stop seam/build gate failed with exit $gradleExit"};if(-not (Select-String -LiteralPath 'src/main/java/dev/developershell/server/CampaignLifecycle.java' -Pattern 'ServerLifecycleEvents\.SERVER_STOPPING' -Quiet)){throw 'real stop callback missing'}</automated>
  </verify>
  <done>The stop cleanup is unit/integration-testable in-process and wired to a real callback that final dedicated-server evidence can observe honestly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Fabric callbacks -> campaign service | Repeated/reordered runtime notifications enter durable state. |
| Disk-loaded entity -> active runtime | Persisted entity identity is untrusted until matched. |
| Server stopping -> final save/cleanup | Process lifecycle must invoke the same bounded state-first handler. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-LIFE-01 | Spoofing | Callback/entity identity | high | mitigate | Match owner+encounter+entity/attempt before accepting event or load. |
| T-02-LIFE-02 | Denial of Service | Recursive cleanup | medium | mitigate | Clear durable ref first, bound owned sets, and make generated unload stale. |
| T-02-LIFE-03 | Repudiation | Real server stop | medium | mitigate | Shared tested handler plus stable completion marker required by bounded clean-stop evidence. |
</threat_model>

## Artifacts this phase produces

- `CampaignLifecycle` typed Fabric adapters, testable stop handler, and real `SERVER_STOPPING` registration.
- State-first `LectureEncounterManager` cleanup and Professor/orphan admission guards.
- `LectureLifecycleGameTests` for every in-process exit, replay, reload, orphan, and stop-handler case.
- Localized failure/reload/cleanup copy and one sanitized stop-completion marker.

<verification>
- GameTests prove state-before-cleanup and absence of partial combat without stopping their own server.
- Plan 13 is explicitly responsible for observing the real callback marker during bounded dedicated-server shutdown.
</verification>

<success_criteria>
- Save, quit, death, unload, reload, and duplicate callbacks converge safely and once.
- No plan claims an impossible after-exit GameTest assertion.
</success_criteria>

<output>Create `.planning/phases/02-persistent-lecture-vertical-slice/02-05-SUMMARY.md` when done.</output>
