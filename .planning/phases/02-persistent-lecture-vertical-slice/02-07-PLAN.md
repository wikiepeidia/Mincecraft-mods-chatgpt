---
phase: 02-persistent-lecture-vertical-slice
plan: 07
type: execute
wave: 8
depends_on: [02-04, 02-05]
files_modified:
  - src/main/java/dev/developershell/campaign/PlayerCampaignState.java
  - src/main/java/dev/developershell/campaign/CampaignEvent.java
  - src/main/java/dev/developershell/campaign/CampaignReducer.java
  - src/main/java/dev/developershell/campaign/CampaignService.java
  - src/main/java/dev/developershell/lecture/RetakeService.java
  - src/test/java/dev/developershell/campaign/CampaignReducerTest.java
autonomous: true
requirements: [FND-06, FND-07, LECT-02]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Each failed attempt creates one durable owner+failed-encounter Retake entitlement; duplicate cleanup, reconciliation, unload, or fallback-loss events cannot create a second entitlement or physical representation."
    - "Physical inventory/Form/fallback state is a lossy projection of saved authority; missing representation permits one replacement and never changes chapter, attempts, rewards, or active encounter."
    - "A retry transition revalidates and commits a new attempt before prior Form/fallback entitlement is cleared or any new runtime effect occurs."
  artifacts:
    - path: "src/main/java/dev/developershell/lecture/RetakeService.java"
      provides: "Exactly-one reconciliation and accepted retry intent adapter"
      exports: ["RetakeService", "reconcile", "recover", "startRetake"]
    - path: "src/test/java/dev/developershell/campaign/CampaignReducerTest.java"
      provides: "Retake entitlement/fallback/retry ordering matrix"
  key_links:
    - from: "src/main/java/dev/developershell/lecture/RetakeService.java"
      to: "src/main/java/dev/developershell/campaign/CampaignService.java"
      via: "all entitlement/reference/start changes use accepted CampaignEvents"
      pattern: "CampaignEvent"
---

# Phase 2 Plan 07 — Exactly-One Retake Domain

## Phase Goal

**As a** survival player, **I want to** discover, fail, retry, and complete Professor Infinite Slides, **so that** my Lecture progress and rewards survive restarts without exploits.

<objective>
Define exactly-one Retake entitlement, physical projection, fallback-reference, and retry ordering before world interactions are added.

Purpose: Prevent loss or duplication when inventory items and fallback entities disappear, unload, or race cleanup.
Output: Durable Retake state/events/reducer rules, one reconciliation service, and pure replay/order tests.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.agents/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-CONTEXT.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-UI-SPEC.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-RESEARCH.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-VALIDATION.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-04-SUMMARY.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-05-SUMMARY.md
@AGENTS.md

<interfaces>
Lifecycle cleanup already submits one terminal event and `CampaignService` persists accepted transitions before effects. Extend the existing `retakeEntitled` and fallback UUID fields; do not make an item/component/entity the source of progression truth. Decision coverage: D-08, D-16, D-17, D-18, D-20; validation rows 02-ITEM-01, 02-LIFE-01, and 02-REWARD-01.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove one logical entitlement across every replay and loss ordering</name>
  <read_first>
    - Plan 04 `PlayerCampaignState`, `CampaignEvent`, and `CampaignReducerTest` — extend stable schema/events in place.
    - `02-UI-SPEC.md` Retake Form issuance and recovery steps 1–7 — exact logical/physical rules.
    - `02-CONTEXT.md` D-17/D-18 — terminal transition precedes cleanup and repeated callbacks are no-ops.
    - `02-VALIDATION.md` 02-ITEM-01/02-REWARD-01 — duplicate, full inventory, fallback loss, and retry cases.
  </read_first>
  <files>src/main/java/dev/developershell/campaign/PlayerCampaignState.java, src/main/java/dev/developershell/campaign/CampaignEvent.java, src/main/java/dev/developershell/campaign/CampaignReducer.java, src/test/java/dev/developershell/campaign/CampaignReducerTest.java</files>
  <behavior>
    - First matching terminal event records one entitlement keyed by owner and failed encounter; replay records nothing.
    - Inventory Form/fallback present/missing and tracked fallback created/cleared are idempotent state projections.
    - Destroyed/despawned/unloaded fallback reference may clear once and allow one replacement; stale entity callbacks are no-ops.
    - Accepted retry increments attempt once and clears the old entitlement/reference only as part of the new active state.
  </behavior>
  <action>
    Add fail-first explicit-UUID event-order tests for entitlement creation, duplicate cleanup, inventory representation, fallback reservation/commit, missing/lost fallback, empty-hand recovery intent, wrong owner/attempt, changed state, and accepted/rejected retry. Extend state/events/reducer without changing schema names. Keep replacement entitlement separate from physical representation and make all mismatches/replays no-ops per D-16 through D-18/D-20.
  </action>
  <acceptance_criteria>
    - Every ordering yields at most one logical entitlement and one tracked representation intent.
    - Clearing/replacing a physical reference cannot reset progression, grant reward, or start an encounter.
    - Retry state changes are atomic and preserve all monotonic chapter/reward/cooldown fields.
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop';$jdkRoot=(Resolve-Path '.work/toolchain/temurin-25.0.4+7-x64').Path;$env:JAVA_HOME=$jdkRoot;$env:Path="$(Join-Path $jdkRoot 'bin');$env:Path";$jvm=@("-Dorg.gradle.java.installations.paths=$jdkRoot",'-Dorg.gradle.java.installations.auto-detect=false','-Dorg.gradle.java.installations.auto-download=false');& .\gradlew.bat @jvm test --tests 'dev.developershell.campaign.CampaignReducerTest' compileJava --offline --no-daemon --console=plain --init-script scripts/loom-resolution.init.gradle;$gradleExit=$LASTEXITCODE;if($gradleExit -ne 0){throw "Retake reducer gate failed with exit $gradleExit"}</automated>
  </verify>
  <done>Retake authority is durable, exactly one, and replay/loss safe before an item entity is materialized.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce state-first reconciliation through one service</name>
  <read_first>
    - Task 1 Retake events and `CampaignService` — use accepted intents only.
    - `02-RESEARCH.md` Pattern 4 transactional item interactions — inventory first, tracked fallback second, validation before retry consumption.
    - `02-PATTERNS.md` RetakeService responsibility — no duplicated reconciliation in lifecycle/items/commands.
  </read_first>
  <files>src/main/java/dev/developershell/campaign/CampaignEvent.java, src/main/java/dev/developershell/campaign/CampaignService.java, src/main/java/dev/developershell/lecture/RetakeService.java, src/test/java/dev/developershell/campaign/CampaignReducerTest.java</files>
  <behavior>
    - Reconcile checks existing owner inventory Form and tracked fallback before requesting issuance.
    - Fallback spawn uses a reserve-then-materialize-then-record protocol whose failure returns to one recoverable entitlement.
    - Retry submits accepted START before consuming/clearing physical state; rejection has zero effect.
  </behavior>
  <action>
    Implement `RetakeService` as the sole adapter over `CampaignService` for reconcile/recover/startRetake. Represent inventory/fallback operations as bounded effect ports so pure tests simulate full inventory, spawn failure, entity loss, duplicates, and successful/rejected retry without Minecraft world mutation. The later interaction plan supplies concrete inventory/entity adapters; this service already enforces D-08/D-16/D-17/D-18.
  </action>
  <acceptance_criteria>
    - Lifecycle, item, desk, and command callers can only request Retake behavior through this one service.
    - Failed physical materialization remains recoverable and never records two fallbacks.
    - Retry consume/clear effects are unreachable before accepted new-attempt persistence.
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop';$jdkRoot=(Resolve-Path '.work/toolchain/temurin-25.0.4+7-x64').Path;$env:JAVA_HOME=$jdkRoot;$env:Path="$(Join-Path $jdkRoot 'bin');$env:Path";$jvm=@("-Dorg.gradle.java.installations.paths=$jdkRoot",'-Dorg.gradle.java.installations.auto-detect=false','-Dorg.gradle.java.installations.auto-download=false');& .\gradlew.bat @jvm test --tests 'dev.developershell.campaign.CampaignReducerTest' compileJava auditDirectDependencies --offline --no-daemon --console=plain --init-script scripts/loom-resolution.init.gradle;$gradleExit=$LASTEXITCODE;if($gradleExit -ne 0){throw "Retake service gate failed with exit $gradleExit"};$runtimeFiles=(Get-ChildItem -LiteralPath 'src/main/java' -Recurse -File).FullName;$hits=Select-String -Path $runtimeFiles -Pattern 'new\s+(RetakeFallback|ReconcileRetake)' | Where-Object{$_.Path -notmatch 'RetakeService|CampaignReducer|CampaignEvent'};if($hits){throw 'Retake construction escaped service'}</automated>
  </verify>
  <done>One tested service owns Retake reconciliation and guarantees accepted state precedes every lossy physical effect.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Durable entitlement -> item/entity | Inventory and world representations may disappear or replay callbacks. |
| Physical Form -> new attempt | Owner/desk/state/arena must be revalidated before consume/start. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-RETAKE-01 | Spoofing | Owner/attempt representation | high | mitigate | Saved owner+failed encounter identity and wrong-owner/stale no-ops. |
| T-02-RETAKE-02 | Tampering | Reconcile/retry replay | high | mitigate | One entitlement, reserve/record protocol, and commit-before-consume tests. |
| T-02-RETAKE-03 | Denial of Service | Missing physical item | medium | mitigate | Loss clears one reference and preserves recoverable entitlement. |
</threat_model>

## Artifacts this phase produces

- Retake entitlement/fallback/retry event and reducer semantics in schema 1.
- `RetakeService` as the sole state-first reconcile/recover/startRetake adapter.
- Pure explicit-order tests for duplicate cleanup, full inventory, fallback loss, recovery, wrong owner/state, and accepted retry.

<verification>
- Reducer tests prove every logical ordering.
- Service-port tests prove physical failure cannot duplicate or strand entitlement.
</verification>

<success_criteria>
- One failed attempt always retains exactly one recoverable retry right.
- No physical Form or fallback can become an independent progression or duplication authority.
</success_criteria>

<output>Create `.planning/phases/02-persistent-lecture-vertical-slice/02-07-SUMMARY.md` when done.</output>
