---
phase: 02-persistent-lecture-vertical-slice
plan: 10
type: execute
wave: 10
depends_on: [02-05, 02-08, 02-09]
files_modified:
  - src/main/java/dev/developershell/registry/ModEntities.java
  - src/main/java/dev/developershell/lecture/LecturePresentation.java
  - src/main/java/dev/developershell/lecture/LectureEncounterManager.java
  - src/main/java/dev/developershell/entity/HomeworkAddEntity.java
  - src/client/java/dev/developershell/client/DevelopersHellClient.java
  - src/main/resources/assets/developers_hell/lang/en_us.json
  - src/gametest/java/dev/developershell/gametest/LectureBossGameTests.java
  - src/gametest/resources/fabric.mod.json
autonomous: true
requirements: [FND-07, LECT-01, LECT-02]
estimate: {tokens: 42000, raw_tokens: 42000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Every harmful act exposes attack name, response text, recorded geometry/shape, non-color identity, distinct vanilla sound/subtitle, wind-up, and explicit recovery window."
    - "Inactive state presents zero combat UI; one active owner sees one boss bar/current action line and at most one Homework add, with transition-deduplicated chat/sounds."
    - "Reduced effects lowers decorative density while retaining collision, essential marks, text, sound, and timing; cleanup stops every cue/object immediately."
  artifacts:
    - {path: "src/main/java/dev/developershell/lecture/LecturePresentation.java", provides: "Participant-scoped bar/text/particle/sound adapter", exports: ["LecturePresentation", "open", "render", "close"]}
    - {path: "src/main/java/dev/developershell/entity/HomeworkAddEntity.java", provides: "Bounded owner/encounter no-loot quiz consequence"}
    - {path: "src/gametest/java/dev/developershell/gametest/LectureBossGameTests.java", provides: "Real three-act presentation/helper/cleanup proof"}
  key_links:
    - {from: "src/main/java/dev/developershell/lecture/LectureEncounterManager.java", to: "src/main/java/dev/developershell/lecture/LectureStateMachine.java", via: "server tick consumes pure outputs", pattern: "LectureStateMachine"}
    - {from: "src/main/java/dev/developershell/lecture/LectureEncounterManager.java", to: "src/main/java/dev/developershell/lecture/LecturePresentation.java", via: "transition/whole-second bounded rendering", pattern: "LecturePresentation"}
  prohibitions:
    - statement: "MUST NOT make harmful mechanics understandable only through color, particles, sound, or transient text, and MUST NOT use forced camera movement, nausea, full-screen flashes, rapid glow toggling, or strobing."
      status: flagged-unverified
---

# Phase 2 Plan 10 — Three-Act Presentation and Homework Add

## Phase Goal

**As a** survival player, **I want to** discover, fail, retry, and complete Professor Infinite Slides, **so that** my Lecture progress and rewards survive restarts without exploits.

<objective>
Materialize pure three-act outputs through readable bounded vanilla presentation and one cleanup-owned Homework add.

Purpose: Make every joke/mechanic learnable without color/sound/client authority and keep all presentation ephemeral.
Output: One presentation adapter, stable Homework identity, manager wiring, localized copy, and real three-act GameTests.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.agents/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-CONTEXT.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-UI-SPEC.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-RESEARCH.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-VALIDATION.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-09-SUMMARY.md
@AGENTS.md

<interfaces>
Plan 09 emits pure bounded outputs; Plan 05 owns cleanup; client code only registers vanilla renderers. Decision coverage: D-09, D-10, D-11, D-12, D-13, D-14, D-20; validation rows 02-BOSS-01 and 02-LIFE-01.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register one bounded owner-scoped Homework add</name>
  <read_first>
    - Plan 01 entity registration/renderer pattern and Plan 05 orphan/cleanup guard.
    - Plan 09 one-Homework output cap.
    - `02-RESEARCH.md` entity builder/attribute/renderer APIs.
  </read_first>
  <files>src/main/java/dev/developershell/registry/ModEntities.java, src/main/java/dev/developershell/entity/HomeworkAddEntity.java, src/client/java/dev/developershell/client/DevelopersHellClient.java, src/gametest/java/dev/developershell/gametest/LectureBossGameTests.java, src/gametest/resources/fabric.mod.json</files>
  <behavior>
    - Wrong/no quiz may spawn at most one `developers_hell:homework_add` for the active owner+encounter.
    - It persists owner/encounter identity, targets only owner, has bounded health/damage/lifetime, and has no loot.
    - Orphan/stale load is rejected and terminal cleanup removes it.
  </behavior>
  <action>
    Start fail-first with add-cap/owner/orphan/cleanup GameTests. Register a stable unconditional Zombie-derived `HomeworkAddEntity`, attributes common-side, and `ZombieRenderer` only client-side. Store identity through current `ValueInput`/`ValueOutput`; manager owns lifetime and cleanup. No add carries progression/reward authority per D-09/D-12/D-20.
  </action>
  <acceptance_criteria>
    - Exactly one stable add ID exists independent of gates and never enters loot/reward state.
    - Wrong owner is not targeted and stale add cannot load as active.
    - Common source has no client renderer import.
  </acceptance_criteria>
  <verify><automated>$ErrorActionPreference='Stop';$jdkRoot=(Resolve-Path '.work/toolchain/temurin-25.0.4+7-x64').Path;$env:JAVA_HOME=$jdkRoot;$env:Path="$(Join-Path $jdkRoot 'bin');$env:Path";$jvm=@("-Dorg.gradle.java.installations.paths=$jdkRoot",'-Dorg.gradle.java.installations.auto-detect=false','-Dorg.gradle.java.installations.auto-download=false');& .\gradlew.bat @jvm test compileJava compileClientJava runGameTest --offline --no-daemon --console=plain --init-script scripts/loom-resolution.init.gradle;$gradleExit=$LASTEXITCODE;if($gradleExit -ne 0){throw "Homework add gate failed with exit $gradleExit"};$commonFiles=(Get-ChildItem -LiteralPath 'src/main/java' -Recurse -File).FullName;if(Select-String -Path $commonFiles -Pattern 'net\.minecraft\.client'){throw 'common client import'}</automated></verify>
  <done>The wrong-quiz consequence is one bounded cleanup-owned server entity with a vanilla client silhouette.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Present all three acts through redundant bounded vanilla cues</name>
  <read_first>
    - Plan 09 state outputs and Plan 05 manager cleanup.
    - `02-UI-SPEC.md` presentation hierarchy, bar/action/chat/particles/sounds, act copy, accessibility, and cleanup matrix.
    - `02-VALIDATION.md` 02-BOSS-01 and lifecycle requirements.
  </read_first>
  <files>src/main/java/dev/developershell/lecture/LecturePresentation.java, src/main/java/dev/developershell/lecture/LectureEncounterManager.java, src/main/resources/assets/developers_hell/lang/en_us.json, src/gametest/java/dev/developershell/gametest/LectureBossGameTests.java, src/gametest/resources/fabric.mod.json</files>
  <behavior>
    - One YELLOW/PROGRESS bar names Act 1/3, 2/3, 3/3 for participant only.
    - Action updates only on whole-second/transition; chat/sound is transition-deduplicated; geometry is server-collision-backed.
    - Normal refresh is no faster than 4 ticks, reduced effects no faster than 10, both capped and cleanup-atomic.
  </behavior>
  <action>
    Create `LecturePresentation` for one `ServerBossEvent`, translated action/chat, bounded server particles, and distinct vanilla cues. Render lane safe diamonds/danger X, A-square/B-circle/C-diamond pads, and named quadrant ring/center. Wire manager outputs without client input. Add real safe/unsafe lane, correct/wrong/no quiz, PRESENT/ABSENT, owner/wrong-owner damage, density/cap, boss-name, victory, and cleanup GameTests per D-10 through D-14/D-20.
  </action>
  <acceptance_criteria>
    - All acts provide six redundant cues and explicit attack windows without color/sound-only meaning.
    - Reduced effects preserves essential information/collision and uses no forbidden motion/flash effects.
    - Every terminal path removes bar, current action, emitter, sound schedule, and add.
  </acceptance_criteria>
  <verify><automated>$ErrorActionPreference='Stop';$jdkRoot=(Resolve-Path '.work/toolchain/temurin-25.0.4+7-x64').Path;$env:JAVA_HOME=$jdkRoot;$env:Path="$(Join-Path $jdkRoot 'bin');$env:Path";$jvm=@("-Dorg.gradle.java.installations.paths=$jdkRoot",'-Dorg.gradle.java.installations.auto-detect=false','-Dorg.gradle.java.installations.auto-download=false');& .\gradlew.bat @jvm test runGameTest auditDirectDependencies build --offline --no-daemon --console=plain --init-script scripts/loom-resolution.init.gradle;$gradleExit=$LASTEXITCODE;if($gradleExit -ne 0){throw "three-act presentation gate failed with exit $gradleExit"};$commonFiles=(Get-ChildItem -LiteralPath 'src/main/java' -Recurse -File).FullName;if(Select-String -Path $commonFiles -Pattern 'net\.minecraft\.client'){throw 'common client import'};Add-Type -AssemblyName System.IO.Compression.FileSystem;$jar=Get-Item -LiteralPath 'build/libs/developers-hell-0.1.0.jar';$zip=[IO.Compression.ZipFile]::OpenRead($jar.FullName);try{if($zip.Entries.FullName -match 'gametest|LectureBossGameTests'){throw 'test output leaked into production JAR'}}finally{$zip.Dispose()}</automated></verify>
  <done>The complete Professor fight is readable, server-authoritative, bounded, and cleanup-safe through vanilla surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Pure output -> world/presentation | Bounded intents become cues, damage, and helper entities. |
| Common entities -> client renderers | Visual identity crosses source sets without authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-PRES-01 | Spoofing | Client/presentation result | high | mitigate | Server state/position resolves every outcome; client renders only. |
| T-02-PRES-02 | Denial of Service | Particles/messages/add | medium | mitigate | Fixed cadence/caps, one bar/action/add, transition dedupe, cleanup. |
| T-02-PRES-03 | Elevation of Privilege | Renderer | high | mitigate | Renderer registration stays in client source set. |
</threat_model>

## Artifacts this phase produces

- Stable bounded `HomeworkAddEntity` plus unconditional registration and client-only vanilla renderer.
- `LecturePresentation` and manager integration for all three acts, redundant cues, reduced effects, and cleanup.
- `LectureBossGameTests` covering real acts, owner damage, add/presentation caps, victory, and cleanup.

<verification>
- Real GameTests prove semantic state/effect/cap/cleanup behavior; final visual judgment stays a manual backstop.
</verification>

<success_criteria>
- Professor Infinite Slides is a readable three-act spatial boss fight.
- Presentation/helper objects are bounded, owner-scoped, side-safe, and ephemeral.
</success_criteria>

<output>Create `.planning/phases/02-persistent-lecture-vertical-slice/02-10-SUMMARY.md` when done.</output>
