---
phase: 02-persistent-lecture-vertical-slice
plan: 06
type: execute
wave: 8
depends_on: [02-03, 02-04, 02-05, 02-16]
files_modified:
  - src/main/java/dev/developershell/lecture/LectureGeometry.java
  - src/main/java/dev/developershell/lecture/ArenaRejection.java
  - src/main/java/dev/developershell/lecture/ArenaValidationResult.java
  - src/main/java/dev/developershell/lecture/ArenaValidator.java
  - src/main/java/dev/developershell/item/CursedInternshipContractItem.java
  - src/main/resources/assets/developers_hell/lang/en_us.json
  - src/test/java/dev/developershell/lecture/LectureGeometryTest.java
  - src/gametest/java/dev/developershell/gametest/ContractArenaGameTests.java
  - src/gametest/resources/fabric.mod.json
autonomous: true
requirements: [FND-07, CAMP-01, CAMP-02, LECT-01]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A Contract validates the exact Overworld lectern-relative 17x17 boundary, 15x15 interior, solid floor, four-block headroom, loaded/world-border-safe area, and deterministic nearby retry point before mutation."
    - "Every rejection emits a specific localized reason and correction while preserving Contract count, campaign bytes, entity/presentation counts, 17x17 blocks, and vanilla bed/respawn data."
    - "A valid start records the exact desk/facing/combat origin/retry point and active encounter before consuming one Contract or spawning one Professor; no arena block changes."
  artifacts:
    - path: "src/main/java/dev/developershell/lecture/LectureGeometry.java"
      provides: "Exact local axes, arena ranges, lanes, pads, rings, and retry ordering"
      exports: ["LectureGeometry"]
    - path: "src/main/java/dev/developershell/lecture/ArenaValidator.java"
      provides: "Bounded read-only ServerLevel validation"
      exports: ["ArenaValidator", "validate"]
    - path: "src/gametest/java/dev/developershell/gametest/ContractArenaGameTests.java"
      provides: "Atomic rejection and block-preserving valid-start proof"
  key_links:
    - from: "src/main/java/dev/developershell/item/CursedInternshipContractItem.java"
      to: "src/main/java/dev/developershell/lecture/ArenaValidator.java"
      via: "full validation before submitting START"
      pattern: "ArenaValidator.validate"
    - from: "src/main/java/dev/developershell/item/CursedInternshipContractItem.java"
      to: "src/main/java/dev/developershell/campaign/CampaignService.java"
      via: "accepted geometry becomes a state-first START event"
      pattern: "CampaignEvent"
---

# Phase 2 Plan 06 — Exact Arena and Atomic Contract Start

## Phase Goal

**As a** survival player, **I want to** discover, fail, retry, and complete Professor Infinite Slides, **so that** my Lecture progress and rewards survive restarts without exploits.

<objective>
Expand the tracer's accepted lectern path into the complete exact arena validator and all-or-nothing Contract transaction.

Purpose: Make every placement failure actionable while proving selected world geometry and vanilla respawn data remain untouched.
Output: Pure coordinate contracts, typed rejections, read-only world adapter, localized fixes, and padded GameTests.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.agents/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-CONTEXT.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-UI-SPEC.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-RESEARCH.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-PATTERNS.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-VALIDATION.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-03-SUMMARY.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-04-SUMMARY.md
@AGENTS.md

<interfaces>
The Contract is already discoverable and routes accepted starts through `CampaignService`. Preserve its identity. Geometry uses lectern `L`, facing `F`, and clockwise right `R`; no chunk loading/generation or vanilla respawn setter is allowed. Decision coverage: D-05, D-06, D-07, D-08, D-16, D-20; validation rows 02-GEO-01 and 02-DISC-01.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze exact arena, act geometry, and retry coordinates</name>
  <read_first>
    - `02-UI-SPEC.md` World-space layout and lane/pad/ring tables — use the exact coordinate contract.
    - `02-RESEARCH.md` Pattern 5 and GameTest fixture pitfall — four facings, deterministic radius-five retry, padded fixtures.
    - `02-CONTEXT.md` D-06 through D-08 — exact Overworld validation, no block damage, mod-owned retry.
    - Existing tracer geometry — preserve its local forward orientation while filling all cases.
  </read_first>
  <files>src/main/java/dev/developershell/lecture/LectureGeometry.java, src/main/java/dev/developershell/lecture/ArenaRejection.java, src/main/java/dev/developershell/lecture/ArenaValidationResult.java, src/main/java/dev/developershell/lecture/ArenaValidator.java, src/test/java/dev/developershell/lecture/LectureGeometryTest.java</files>
  <behavior>
    - Center is `L+9F-1Y`; boundary `f=1..17,r=-8..8`; interior `f=2..16,r=-7..7`; headroom checks `L.y..L.y+3`.
    - Lanes are `-7..-3`, `-2..2`, `3..7`; pads anchor `-5/0/5`; four attendance quadrants use radius 2.5.
    - Retry begins `L-2F` and follows one documented deterministic order through radius five for support, two passable body blocks, and border safety.
    - Typed results cover wrong target/dimension, unloaded/outside border, floor, headroom, retry, active encounter, and spawn capacity.
  </behavior>
  <action>
    Write fail-first `LectureGeometryTest` for all four horizontal facings, exact ranges, transforms, coverage/nonoverlap, headroom levels, lanes/pads/rings, and retry ordering/radius. Implement immutable coordinate values and typed accepted/rejected results. Keep world probing behind a bounded interface so pure calculations have no `ServerLevel`, client, filesystem, network, wall-clock, or unseeded random dependency per D-06/D-08/D-20.
  </action>
  <acceptance_criteria>
    - Every declared coordinate/range and facing transform is asserted exactly with no gaps or overlaps.
    - Retry search is finite, deterministic, border-safe, and cannot alter a bed/respawn property.
    - Every rejection family has a stable translation key rather than embedded player English.
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop';$jdkRoot=(Resolve-Path '.work/toolchain/temurin-25.0.4+7-x64').Path;$env:JAVA_HOME=$jdkRoot;$env:Path="$(Join-Path $jdkRoot 'bin');$env:Path";$jvm=@("-Dorg.gradle.java.installations.paths=$jdkRoot",'-Dorg.gradle.java.installations.auto-detect=false','-Dorg.gradle.java.installations.auto-download=false');& .\gradlew.bat @jvm test --tests 'dev.developershell.lecture.LectureGeometryTest' compileJava --offline --no-daemon --console=plain --init-script scripts/loom-resolution.init.gradle;$gradleExit=$LASTEXITCODE;if($gradleExit -ne 0){throw "geometry gate failed with exit $gradleExit"}</automated>
  </verify>
  <done>The exact arena, three act geometries, and retry search are finite, deterministic, and pure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make every real Contract acceptance or rejection atomic</name>
  <read_first>
    - Task 1 geometry/results and `CursedInternshipContractItem.java` — validator precedes START.
    - `CampaignService` accepted/no-op contract — only accepted state may materialize effects.
    - `02-UI-SPEC.md` placement errors and Contract/Desk interaction copy.
    - `02-VALIDATION.md` rows 02-GEO-01 and 02-DISC-01 — snapshot inventory/state/entities/blocks/respawn.
  </read_first>
  <files>src/main/java/dev/developershell/lecture/ArenaValidator.java, src/main/java/dev/developershell/item/CursedInternshipContractItem.java, src/main/resources/assets/developers_hell/lang/en_us.json, src/gametest/java/dev/developershell/gametest/ContractArenaGameTests.java, src/gametest/resources/fabric.mod.json</files>
  <behavior>
    - Server probes exact Overworld, already-loaded chunks, world border, solid floor, four headroom blocks, retry, inactivity, and spawn capacity without mutation.
    - Each rejection preserves stack/state/entity/bar/block/respawn snapshots and sends one specific reason plus correction.
    - Valid start persists exact layout/retry/encounter before consuming one Contract and spawning one matching Professor.
  </behavior>
  <action>
    Add padded fail-first GameTests for every rejection and one valid start. Implement `ArenaValidator` through read-only `ServerLevel` queries with no forced chunk load, block/explosion edit, state write, spawn, stack change, or bed/respawn setter. Update Contract use to validate, submit START, and materialize only accepted intents. Localize exact reason/fix copy and preserve command-free discovery per D-05 through D-08/D-16/D-20.
  </action>
  <acceptance_criteria>
    - Every invalid use is a proven five-surface no-op: inventory, campaign state, entities/presentation, blocks, and vanilla respawn.
    - A valid use commits one active encounter and exact retry data before effects and preserves all arena blocks.
    - Tests use padded fixtures, explicit owner/seed, and exact GameTest discovery metadata.
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop';$jdkRoot=(Resolve-Path '.work/toolchain/temurin-25.0.4+7-x64').Path;$env:JAVA_HOME=$jdkRoot;$env:Path="$(Join-Path $jdkRoot 'bin');$env:Path";$jvm=@("-Dorg.gradle.java.installations.paths=$jdkRoot",'-Dorg.gradle.java.installations.auto-detect=false','-Dorg.gradle.java.installations.auto-download=false');& .\gradlew.bat @jvm test --tests 'dev.developershell.lecture.LectureGeometryTest' runGameTest auditDirectDependencies build --offline --no-daemon --console=plain --init-script scripts/loom-resolution.init.gradle;$gradleExit=$LASTEXITCODE;if($gradleExit -ne 0){throw "arena integration gate failed with exit $gradleExit"}</automated>
  </verify>
  <done>A player receives actionable placement feedback, and every rejected/accepted Contract is exact, state-first, and block/respawn safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Selected lectern/world -> START | Arbitrary world geometry must pass bounded server validation before mutation. |
| Typed rejection -> localized player response | Errors must be actionable without exposing raw world/private data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-ARENA-01 | Tampering | Contract selection | high | mitigate | Exact server-only probes before START plus full no-op snapshots. |
| T-02-ARENA-02 | Denial of Service | Boundary/retry scans | medium | mitigate | Fixed ranges/radius, no chunk generation, no global search. |
| T-02-ARENA-03 | Spoofing | Client target/position | high | mitigate | Server derives lectern facing, coordinates, state, and spawn capacity. |
</threat_model>

## Artifacts this phase produces

- Exact `LectureGeometry`, `ArenaRejection`, `ArenaValidationResult`, and bounded `ArenaValidator`.
- Transactional Contract interaction and complete localized placement/fix copy.
- `LectureGeometryTest` and padded `ContractArenaGameTests` covering every facing/rejection/valid start and block/respawn preservation.

<verification>
- Pure tests prove exact coordinate and retry contracts.
- GameTests prove read-only validation and commit-before-effect behavior in a real world.
</verification>

<success_criteria>
- CAMP-02 arena selection is exact, explainable, and non-destructive.
- No rejected or valid Contract path alters vanilla respawn data or arena blocks.
</success_criteria>

<output>Create `.planning/phases/02-persistent-lecture-vertical-slice/02-06-SUMMARY.md` when done.</output>
