---
phase: 02-persistent-lecture-vertical-slice
plan: 15
type: execute
wave: 3
depends_on: [02-14]
files_modified:
  - src/main/java/dev/developershell/DevelopersHell.java
  - src/client/java/dev/developershell/client/DevelopersHellClient.java
  - src/main/java/dev/developershell/campaign/CampaignService.java
  - src/main/java/dev/developershell/lecture/LectureRules.java
  - src/main/java/dev/developershell/lecture/LectureEncounterManager.java
  - src/main/resources/assets/developers_hell/lang/en_us.json
  - src/gametest/java/dev/developershell/gametest/FoundationGameTests.java
autonomous: true
requirements: [FND-06, FND-07, LECT-01]
estimate: {tokens: 18000, raw_tokens: 18000, tasks: 1, confidence: low}
must_haves:
  truths:
    - "The custom Professor is rendered only through the client source set while encounter state, position, damage, reward, boss bar, text, particles, and sound remain logical-server owned."
    - "The retained tracer uses one participant boss bar, whole-second action updates, transition-only translated chat/sound, and bounded particles."
    - "No common source imports client classes and no runtime source introduces external I/O."
  artifacts:
    - {path: "src/main/java/dev/developershell/lecture/LectureRules.java", provides: "Immutable bounded Standard encounter rules"}
    - {path: "src/client/java/dev/developershell/client/DevelopersHellClient.java", provides: "Client-only vanilla Vindicator renderer registration"}
    - {path: "src/main/resources/assets/developers_hell/lang/en_us.json", provides: "Bounded translated tracer presentation"}
  key_links:
    - {from: "src/client/java/dev/developershell/client/DevelopersHellClient.java", to: "src/main/java/dev/developershell/registry/ModEntities.java", via: "client-only vanilla renderer registration", pattern: "EntityRenderers.register"}
    - {from: "src/main/java/dev/developershell/DevelopersHell.java", to: "src/main/java/dev/developershell/lecture/LectureEncounterManager.java", via: "common bootstrap composes one server-authoritative manager", pattern: "LectureEncounterManager"}
---

# Phase 2 Plan 15 — Side-Safe Tracer Composition and Presentation

## Phase Goal

**As a** survival player, **I want to** discover, fail, retry, and complete Professor Infinite Slides, **so that** my Lecture progress and rewards survive restarts without exploits.

<objective>
Complete the retained tracer's client/common composition and translated bounded vanilla presentation.

Purpose: Prove the final Professor type is visible without moving any gameplay authority client-side.
Output: Bounded Standard rules, common bootstrap composition, client-only renderer, translations, and split-source verification.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.agents/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-CONTEXT.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-UI-SPEC.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-RESEARCH.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-VALIDATION.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-14-SUMMARY.md
@AGENTS.md

<interfaces>
Plan 14 freezes stable schema, registry, and Professor types. Compose those exact types and preserve the Plan 01 production path. Decision coverage: D-09, D-10, D-20.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete side-safe composition and translated presentation</name>
  <read_first>
    - `DevelopersHell.java`, `CampaignService.java`, and `LectureEncounterManager.java` after Plan 14 — compose, do not create a parallel service.
    - `DevelopersHellClient.java` — renderer imports remain client-only.
    - `02-UI-SPEC.md` presentation hierarchy and copy budgets — use boss bar, action bar, low-frequency chat, particles, and vanilla sound only.
  </read_first>
  <files>src/main/java/dev/developershell/DevelopersHell.java, src/client/java/dev/developershell/client/DevelopersHellClient.java, src/main/java/dev/developershell/campaign/CampaignService.java, src/main/java/dev/developershell/lecture/LectureRules.java, src/main/java/dev/developershell/lecture/LectureEncounterManager.java, src/main/resources/assets/developers_hell/lang/en_us.json, src/gametest/java/dev/developershell/gametest/FoundationGameTests.java</files>
  <action>
    Introduce immutable bounded Standard `LectureRules`, wire registries then server service/manager while retaining the first-tick marker, and register `VindicatorRenderer` only from `DevelopersHellClient`. Replace player-visible tracer text with translation keys and enforce one participant boss bar, whole-second action updates, transition-only chat/sound, and bounded particles per D-10/D-20. Keep common sources free of `net.minecraft.client` and runtime free of external I/O.
  </action>
  <acceptance_criteria>
    - Common/client compilation is separate; no common class links client types and the renderer uses the stable Professor type.
    - One owner sees one bar/current instruction and an inactive player sees none; all user text is translated and bounded.
    - The original tracer, duplicate starts, and replayed victories remain green.
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop';$jdkRoot=(Resolve-Path '.work/toolchain/temurin-25.0.4+7-x64').Path;$env:JAVA_HOME=$jdkRoot;$env:Path="$(Join-Path $jdkRoot 'bin');$env:Path";$jvm=@("-Dorg.gradle.java.installations.paths=$jdkRoot",'-Dorg.gradle.java.installations.auto-detect=false','-Dorg.gradle.java.installations.auto-download=false');& .\gradlew.bat @jvm test compileJava compileClientJava processResources runGameTest auditDirectDependencies build --offline --no-daemon --console=plain --init-script scripts/loom-resolution.init.gradle;$gradleExit=$LASTEXITCODE;if($gradleExit -ne 0){throw "side-safe build failed with exit $gradleExit"};$commonFiles=(Get-ChildItem -LiteralPath 'src/main/java' -Recurse -File).FullName;if(Select-String -Path $commonFiles -Pattern 'net\.minecraft\.client'){throw 'common client import'}</automated>
  </verify>
  <done>The retained tracer compiles on client and server, presents translated bounded cues, and preserves one authoritative owner-scoped encounter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Common encounter -> client renderer | Only stable entity identity crosses source sets; gameplay authority remains server-side. |
| Server transition -> player presentation | Bounded cues must represent accepted server state without tick spam. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-PRES-01 | Elevation of Privilege | Client renderer | high | mitigate | Client-only renderer and source scan; all decisions remain common/server. |
| T-02-PRES-02 | Denial of Service | Boss bar/messages/particles | medium | mitigate | One participant bar, whole-second/transition cadence, and fixed particle bounds. |
</threat_model>

## Artifacts this phase produces

- `LectureRules` and common bootstrap composition for one retained tracer runtime.
- Client-only vanilla renderer binding for the final Professor identity.
- Localized bounded boss/action/chat presentation and split-source proof.

<verification>
- The full transformed build, dependency audit, and common-source client-import scan run under the pinned Java toolchain.
</verification>

<success_criteria>
- The tracer is visible and readable through vanilla surfaces without client gameplay authority or external I/O.
</success_criteria>

<output>Create `.planning/phases/02-persistent-lecture-vertical-slice/02-15-SUMMARY.md` when done.</output>
