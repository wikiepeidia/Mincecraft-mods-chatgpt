---
phase: 02-persistent-lecture-vertical-slice
plan: 14
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/main/java/dev/developershell/registry/ModItemIds.java
  - src/main/java/dev/developershell/registry/ModItems.java
  - src/main/java/dev/developershell/registry/ModEntities.java
  - src/main/java/dev/developershell/campaign/PlayerCampaignState.java
  - src/main/java/dev/developershell/campaign/CampaignSavedData.java
  - src/main/java/dev/developershell/campaign/CampaignService.java
  - src/main/java/dev/developershell/entity/ProfessorInfiniteSlidesEntity.java
autonomous: true
requirements: [FND-06, FND-07, LECT-01, LECT-02]
estimate: {tokens: 18000, raw_tokens: 18000, tasks: 1, confidence: low}
must_haves:
  truths:
    - "Stable item/entity keys remain byte-for-name identical while the tracer moves into final catalog, campaign-state, and Professor types."
    - "Schema-v1 state stores owner, desk/retry, attempt, active encounter/Professor, entitlements, first-reward ledger, Retake representation, and Remote deadline without moving gameplay authority into the entity."
    - "Professor owner and encounter UUID survive ValueInput/ValueOutput persistence; mismatched identity cannot admit damage or rewards."
  artifacts:
    - {path: "src/main/java/dev/developershell/campaign/PlayerCampaignState.java", provides: "Final immutable schema-v1 player campaign record"}
    - {path: "src/main/java/dev/developershell/entity/ProfessorInfiniteSlidesEntity.java", provides: "Vindicator-derived owner/encounter combat identity"}
    - {path: "src/main/java/dev/developershell/registry/ModItemIds.java", provides: "Final unconditional stable Phase 2 item keys"}
  key_links:
    - {from: "src/main/java/dev/developershell/campaign/CampaignService.java", to: "src/main/java/dev/developershell/campaign/CampaignSavedData.java", via: "sole save-then-effect facade", pattern: "setDirty"}
    - {from: "src/main/java/dev/developershell/registry/ModEntities.java", to: "src/main/java/dev/developershell/entity/ProfessorInfiniteSlidesEntity.java", via: "stable no-loot custom entity registration", pattern: "ProfessorInfiniteSlidesEntity"}
---

# Phase 2 Plan 14 — Stable Campaign and Professor Interfaces

## Phase Goal

**As a** survival player, **I want to** discover, fail, retry, and complete Professor Infinite Slides, **so that** my Lecture progress and rewards survive restarts without exploits.

<objective>
Extract the green tracer into the final stable registry, schema-v1 campaign, and Professor interfaces without changing its behavior.

Purpose: Give every later expansion one durable identity and one state-first service seam.
Output: Final item/entity catalogs, immutable player state, SavedData/service integration, and Vindicator-derived Professor type.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.agents/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-CONTEXT.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-RESEARCH.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-PATTERNS.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-VALIDATION.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-01-SUMMARY.md
@AGENTS.md

<interfaces>
Plan 01 proves the production server tracer. Preserve its exact lower-snake-case IDs, accepted/no-op behavior, state-before-effect ordering, and test entrypoint while extracting final types. Decision coverage: D-09, D-15, D-16, D-19, D-20.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze stable schema and entity interfaces without changing the tracer path</name>
  <read_first>
    - Plan 01 `CampaignSavedData`, `CampaignService`, `ModItems`, and `ModEntities` — preserve stable IDs and behavior while extracting final types.
    - `02-RESEARCH.md` durable fields and entity persistence signatures — encode owner, encounter, Professor, attempt, desk/retry, entitlements, ledger, and cooldown.
    - `02-PATTERNS.md` registry and campaign package boundaries — registration stays independent of config.
  </read_first>
  <files>src/main/java/dev/developershell/registry/ModItemIds.java, src/main/java/dev/developershell/registry/ModItems.java, src/main/java/dev/developershell/registry/ModEntities.java, src/main/java/dev/developershell/campaign/PlayerCampaignState.java, src/main/java/dev/developershell/campaign/CampaignSavedData.java, src/main/java/dev/developershell/campaign/CampaignService.java, src/main/java/dev/developershell/entity/ProfessorInfiniteSlidesEntity.java</files>
  <behavior>
    - Stable item/entity keys remain byte-for-name identical while moving into final catalogs/types.
    - Schema-v1 state explicitly stores stable lecture status, desk/retry identity, attempts, optional owner/encounter/Professor reference, entitlements, first-reward ledger, Retake fields, and Remote deadline.
    - Professor owner/encounter UUID round-trips through `ValueInput`/`ValueOutput`; an absent/mismatched identity cannot grant damage or rewards.
  </behavior>
  <action>
    Extract the tracer's durable values into final immutable `PlayerCampaignState`, the item keys into `ModItemIds`, and the boss into `ProfessorInfiniteSlidesEntity extends Vindicator`, without changing names or the already-green path. Keep `ModItems` and `ModEntities` unconditional. `CampaignSavedData` validates UUID map key versus encoded owner and `CampaignService` remains the sole save-then-effect facade. Add duplicate-start, competing-start, matching/stale/wrong-owner victory assertions before refactoring and retain them green per D-15/D-16/D-19/D-20.
  </action>
  <acceptance_criteria>
    - Final state/entity/catalog types replace temporary nested shapes without changing stable IDs, persisted names, event ordering, or tracer output.
    - Every stable item/entity exists under all ModuleGate snapshots and the Professor remains no-loot and owner-window-only.
    - Replay/competition tests prove one active attempt and one reward ledger after extraction.
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop';$jdkRoot=(Resolve-Path '.work/toolchain/temurin-25.0.4+7-x64').Path;$env:JAVA_HOME=$jdkRoot;$env:Path="$(Join-Path $jdkRoot 'bin');$env:Path";$jvm=@("-Dorg.gradle.java.installations.paths=$jdkRoot",'-Dorg.gradle.java.installations.auto-detect=false','-Dorg.gradle.java.installations.auto-download=false');& .\gradlew.bat @jvm test compileJava runGameTest jar --offline --no-daemon --console=plain --init-script scripts/loom-resolution.init.gradle;$gradleExit=$LASTEXITCODE;if($gradleExit -ne 0){throw "stable-interface gate failed with exit $gradleExit"};$testText=Get-Content -LiteralPath 'src/gametest/java/dev/developershell/gametest/FoundationGameTests.java' -Raw;foreach($testPattern in @('contractStartsSlideWindowAndCommitsFirstReward','(?i)duplicate','(?i)competing','(?i)replay')){if($testText -notmatch $testPattern){throw "missing stable-interface GameTest coverage: $testPattern"}}</automated>
  </verify>
  <done>The tracer rests on final stable schema, registry, and entity interfaces that later plans expand in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Tracer identities -> final types | Extraction must not rename stable registry or persisted identities. |
| Persisted entity identity -> combat/reward callbacks | Owner plus encounter UUID must remain authoritative after entity serialization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-IFACE-01 | Tampering | Stable registry/schema names | high | mitigate | Byte-for-name assertions and green tracer/replay tests before and after extraction. |
| T-02-IFACE-02 | Spoofing | Professor owner/encounter identity | high | mitigate | ValueInput/ValueOutput round trip plus owner/encounter matching for damage and rewards. |
</threat_model>

## Artifacts this phase produces

- Final `ModItemIds`, `ModItems`, and `ModEntities` identities retained from the tracer.
- Immutable `PlayerCampaignState` and schema-v1 SavedData/service seam.
- `ProfessorInfiniteSlidesEntity` with owner/encounter persistence and no ordinary loot authority.

<verification>
- Transformed GameTests prove extraction preserves the tracer, competition, and replay behavior.
</verification>

<success_criteria>
- Later plans extend one stable schema and one Professor identity without parallel registries or state stores.
</success_criteria>

<output>Create `.planning/phases/02-persistent-lecture-vertical-slice/02-14-SUMMARY.md` when done.</output>
