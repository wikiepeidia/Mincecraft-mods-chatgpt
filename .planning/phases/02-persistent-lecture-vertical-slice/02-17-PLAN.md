---
phase: 02-persistent-lecture-vertical-slice
plan: 17
type: execute
wave: 12
depends_on: [02-11]
files_modified:
  - src/main/java/dev/developershell/registry/ModItems.java
  - src/main/java/dev/developershell/item/AttendanceSheetItem.java
  - src/main/java/dev/developershell/lecture/LectureEncounterManager.java
  - src/main/java/dev/developershell/lecture/RewardService.java
  - src/main/java/dev/developershell/server/DeskInteraction.java
  - src/main/resources/assets/developers_hell/lang/en_us.json
  - src/gametest/java/dev/developershell/gametest/RewardGameTests.java
  - src/gametest/resources/fabric.mod.json
autonomous: true
requirements: [FND-06, FND-07, LECT-02]
estimate: {tokens: 26000, raw_tokens: 26000, tasks: 1, confidence: low}
must_haves:
  truths:
    - "Duplicate or stale reward callbacks are suppressed by owner+encounter identity and the committed ledger; the UI never claims a reward that was not atomically granted."
    - "A lost Attendance Sheet is recoverable from PASSED state at the matching Desk without replaying progression or issuing another Remote."
    - "The real encounter final-window victory path reaches RewardService only from the accepted persisted victory intent."
  artifacts:
    - {path: "src/main/java/dev/developershell/lecture/RewardService.java", provides: "Exactly-once physical reward and Sheet recovery adapter", exports: ["RewardService", "reconcileVictory", "recoverSheet"]}
    - {path: "src/main/java/dev/developershell/item/AttendanceSheetItem.java", provides: "Stable physical representation of the durable Sheet entitlement"}
    - {path: "src/gametest/java/dev/developershell/gametest/RewardGameTests.java", provides: "Victory replay/fallback/Sheet recovery proof"}
  key_links:
    - {from: "src/main/java/dev/developershell/lecture/LectureEncounterManager.java", to: "src/main/java/dev/developershell/lecture/RewardService.java", via: "only accepted matching victory intent reconciles rewards", pattern: "reconcileVictory"}
    - {from: "src/main/java/dev/developershell/server/DeskInteraction.java", to: "src/main/java/dev/developershell/lecture/RewardService.java", via: "PASSED matching-Desk missing-Sheet recovery", pattern: "recoverSheet"}
---

# Phase 2 Plan 17 — Physical First Reward and Sheet Recovery

## Phase Goal

**As a** survival player, **I want to** discover, fail, retry, and complete Professor Infinite Slides, **so that** my Lecture progress and rewards survive restarts without exploits.

<objective>
Project the committed first-victory intent into one physical reward and a recoverable Attendance Sheet.

Purpose: Ensure inventory failure, item loss, or reward callback replay cannot duplicate progression, Remote issuance, or presentation.
Output: `RewardService`, Sheet item/recovery, localized result copy, and real production-path GameTests.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.agents/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-CONTEXT.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-UI-SPEC.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-RESEARCH.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-VALIDATION.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-10-SUMMARY.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-11-SUMMARY.md
@AGENTS.md

<interfaces>
Plan 11 supplies one accepted persisted reward intent for every winning ordering. Retake physical projection exists and may be reused for full-inventory fallback without sharing entitlements. Decision coverage: D-16, D-17, D-18, D-19, D-20; validation row 02-REWARD-01.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reconcile one first grant and recover only a missing Sheet</name>
  <read_first>
    - Plan 11 accepted reward intent and Plan 07 physical projection patterns.
    - Plan 10 `LectureEncounterManager.java` final-window victory output — extend that real encounter seam rather than testing only a direct reward-service call.
    - `02-UI-SPEC.md` victory/reward and artifact recovery copy.
    - `02-RESEARCH.md` reward boundary and matching Desk recovery.
  </read_first>
  <files>src/main/java/dev/developershell/registry/ModItems.java, src/main/java/dev/developershell/item/AttendanceSheetItem.java, src/main/java/dev/developershell/lecture/LectureEncounterManager.java, src/main/java/dev/developershell/lecture/RewardService.java, src/main/java/dev/developershell/server/DeskInteraction.java, src/main/resources/assets/developers_hell/lang/en_us.json, src/gametest/java/dev/developershell/gametest/RewardGameTests.java, src/gametest/resources/fabric.mod.json</files>
  <behavior>
    - Accepted first victory issues one Sheet and one already-ledgered Remote through inventory/fallback and one result message.
    - The real `LectureEncounterManager` final-window victory path invokes `RewardService.reconcileVictory` only after `CampaignService` returns the accepted matching victory intent; rejected, stale, or replayed victory invokes it zero times.
    - PASSED+entitled+missing Sheet at matching Desk restores one Sheet; existing representation/wrong owner/Desk/state rejects.
    - Sheet recovery never changes progression, Remote ledger, Retake state, or victory presentation.
  </behavior>
  <action>
    Start fail-first with matching/replay/race/full-inventory/lost/existing Sheet GameTests, including a production-path case named `realEncounterVictoryReconcilesFirstReward`. Implement translated `AttendanceSheetItem` and one `RewardService` that consumes accepted intents only. Wire the `LectureEncounterManager` final vulnerability/victory output through `CampaignService`; call `RewardService.reconcileVictory` only for the returned accepted matching victory intent, after PASSED/entitlement/Remote ledger persistence, and never from direct entity death or a rejected/no-op transition. Drive the named GameTest through a real active encounter and final accepted damage window rather than calling `RewardService` directly. Extend `DeskInteraction` with Retake priority in RETAKE_READY and missing-Sheet recovery in PASSED. Use no loot table or item-carried progression authority per D-19/D-20.
  </action>
  <acceptance_criteria>
    - First victory physically grants at most one Sheet and one Remote and announces only accepted facts.
    - The named production-path GameTest reaches victory through `LectureEncounterManager`, proves the manager-to-service call, and proves stale/replayed manager victory emits no second reconciliation.
    - Loss allows one Sheet replacement without Remote/progression replay.
    - Full inventory remains recoverable and owner-bound.
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop';$jdkRoot=(Resolve-Path '.work/toolchain/temurin-25.0.4+7-x64').Path;$env:JAVA_HOME=$jdkRoot;$env:Path="$(Join-Path $jdkRoot 'bin');$env:Path";$jvm=@("-Dorg.gradle.java.installations.paths=$jdkRoot",'-Dorg.gradle.java.installations.auto-detect=false','-Dorg.gradle.java.installations.auto-download=false');& .\gradlew.bat @jvm test runGameTest auditDirectDependencies build --offline --no-daemon --console=plain --init-script scripts/loom-resolution.init.gradle;$gradleExit=$LASTEXITCODE;if($gradleExit -ne 0){throw "reward integration gate failed with exit $gradleExit"};$managerText=Get-Content -LiteralPath 'src/main/java/dev/developershell/lecture/LectureEncounterManager.java' -Raw;if($managerText -notmatch 'reconcileVictory'){throw 'real encounter victory is not wired to RewardService'};$testText=Get-Content -LiteralPath 'src/gametest/java/dev/developershell/gametest/RewardGameTests.java' -Raw;if($testText -notmatch 'realEncounterVictoryReconcilesFirstReward'){throw 'production encounter victory GameTest missing'}</automated>
  </verify>
  <done>First victory and later Sheet recovery are durable, owner-scoped, physically recoverable, and exactly once.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Accepted reward intent -> inventory/fallback/presentation | Physical effects can fail or replay after commitment. |
| Desk recovery request -> durable entitlement | A physical loss must not create a new progression or Remote grant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-REWARD-02 | Spoofing | Sheet recovery | high | mitigate | PASSED entitlement, matching owner/Desk, and existing representation check. |
| T-02-REWARD-03 | Repudiation | Result presentation | medium | mitigate | Messages arise only from accepted intents; replay emits none. |
| T-02-REWARD-04 | Tampering | Physical reward replay | high | mitigate | Committed ledger before reconciliation and production-path replay GameTests. |
</threat_model>

## Artifacts this phase produces

- `RewardService`, `AttendanceSheetItem`, matching-Desk Sheet recovery, and localized result/recovery copy.
- `RewardGameTests` for first/replay/full-inventory/lost/existing Sheet behavior through the real encounter seam.

<verification>
- Transformed physical tests prove the committed ledger-to-world boundary and production victory wiring.
</verification>

<success_criteria>
- One first reward is announced/granted and Sheet loss is recoverable without Remote or progression replay.
</success_criteria>

<output>Create `.planning/phases/02-persistent-lecture-vertical-slice/02-17-SUMMARY.md` when done.</output>
