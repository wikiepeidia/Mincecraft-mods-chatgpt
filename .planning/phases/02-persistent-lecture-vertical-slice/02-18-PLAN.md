---
phase: 02-persistent-lecture-vertical-slice
plan: 18
type: execute
wave: 14
depends_on: [02-12]
files_modified:
  - src/main/java/dev/developershell/lecture/RewardService.java
  - src/main/java/dev/developershell/server/CampaignLifecycle.java
  - src/main/java/dev/developershell/lecture/LectureEncounterManager.java
  - src/main/resources/assets/developers_hell/lang/en_us.json
  - src/test/java/dev/developershell/item/RemoteCooldownTest.java
  - src/gametest/java/dev/developershell/gametest/RemoteGameTests.java
  - src/gametest/resources/fabric.mod.json
autonomous: true
requirements: [FND-06, FND-07, LECT-02]
estimate: {tokens: 22000, raw_tokens: 22000, tasks: 1, confidence: low}
must_haves:
  truths:
    - "Death, respawn, save, quit, and join restore clamped remaining native cooldown from logical-server game time; logged-out wall time does not advance it."
    - "Cooldown completion emits one deferred-safe ready cue per deadline, never once per tick and never over a critical boss action instruction."
    - "Real successful and rejected uses exercise the production registry-backed Remote instance, not a directly constructed test item."
  artifacts:
    - {path: "src/main/java/dev/developershell/server/CampaignLifecycle.java", provides: "JOIN/AFTER_RESPAWN native cooldown restoration"}
    - {path: "src/gametest/java/dev/developershell/gametest/RemoteGameTests.java", provides: "Real use/reject/death/rejoin/ready proof"}
  key_links:
    - {from: "src/main/java/dev/developershell/server/CampaignLifecycle.java", to: "src/main/java/dev/developershell/lecture/RewardService.java", via: "JOIN/AFTER_RESPAWN restore remaining native overlay", pattern: "restoreRemoteCooldown"}
    - {from: "src/gametest/java/dev/developershell/gametest/RemoteGameTests.java", to: "src/main/java/dev/developershell/registry/ModItems.java", via: "production stable Remote stack and registry identity", pattern: "ModItems.INFINITE_SLIDES_REMOTE"}
---

# Phase 2 Plan 18 — Durable Remote Lifecycle and Ready Edge

## Phase Goal

**As a** survival player, **I want to** discover, fail, retry, and complete Professor Infinite Slides, **so that** my Lecture progress and rewards survive restarts without exploits.

<objective>
Restore the committed Remote deadline into Minecraft's native overlay across lifecycle and emit one safe ready edge.

Purpose: Finish LECT-02 persistence and visible cooldown behavior without client authority, wall clock, or UI spam.
Output: JOIN/respawn restoration, boss-instruction arbitration, once-only ready feedback, and production-registry GameTests.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.agents/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-CONTEXT.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-UI-SPEC.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-RESEARCH.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-VALIDATION.md
@.planning/phases/02-persistent-lecture-vertical-slice/02-12-SUMMARY.md
@AGENTS.md

<interfaces>
Plan 12 commits the authoritative deadline/notice fields before bounded effects and binds the stable registry key to `InfiniteSlidesRemoteItem`. Use current `ItemCooldowns.addCooldown(ItemStack,int)`, JOIN, and AFTER_RESPAWN APIs. Decision coverage: D-16, D-19, D-20; validation rows 02-ITEM-01 and 02-REWARD-01.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restore native overlay and emit one ready edge across lifecycle</name>
  <read_first>
    - Plan 12 deadline/notice semantics and current `CampaignLifecycle` JOIN/AFTER_RESPAWN hooks.
    - `registry/ModItems.java` — use the production `INFINITE_SLIDES_REMOTE` instance established in Plan 12 when constructing every GameTest stack.
    - `02-UI-SPEC.md` after-join, READY transition, item-absent, and boss-instruction priority states.
    - `02-VALIDATION.md` Required GameTest 6.
  </read_first>
  <files>src/main/java/dev/developershell/lecture/RewardService.java, src/main/java/dev/developershell/server/CampaignLifecycle.java, src/main/java/dev/developershell/lecture/LectureEncounterManager.java, src/main/resources/assets/developers_hell/lang/en_us.json, src/test/java/dev/developershell/item/RemoteCooldownTest.java, src/gametest/java/dev/developershell/gametest/RemoteGameTests.java, src/gametest/resources/fabric.mod.json</files>
  <behavior>
    - JOIN/AFTER_RESPAWN applies `clamp(deadline-gameTime,0,400)` to all matching stacks; no repeated chat.
    - `RemoteGameTests` obtains Remote stacks from `ModItems.INFINITE_SLIDES_REMOTE`, and `BuiltInRegistries.ITEM` resolves the stable key to that same custom instance before exercising use.
    - Completion records one notice edge; active critical boss action defers it and no present Remote keeps it silent/ready.
    - Death/save/rejoin preserve remaining server ticks and rejected use remains effect-free.
  </behavior>
  <action>
    Add fail-first GameTests for successful/rejected use, death/respawn, save/join normalization seam, matching-stack overlay, item absent, boss-action deferral, and once-only ready cue. Obtain every tested Remote stack from `ModItems.INFINITE_SLIDES_REMOTE` and assert the stable key resolves to that same `InfiniteSlidesRemoteItem` instance before dispatching ordinary use. Route lifecycle through `RewardService.restoreRemoteCooldown`; manager arbitrates critical action-bar priority. Persist notice identity so replay/tick cannot repeat per D-19/D-20.
  </action>
  <acceptance_criteria>
    - Native overlay reflects persisted remainder after death/join and never uses logged-out wall time.
    - Real GameTests dispatch successful and rejected use through the production registry-backed Remote rather than a directly constructed item.
    - Ready cue is exactly one per deadline and cannot obscure a critical fight instruction.
    - All prior reward/lifecycle/boss tests remain green.
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop';$jdkRoot=(Resolve-Path '.work/toolchain/temurin-25.0.4+7-x64').Path;$env:JAVA_HOME=$jdkRoot;$env:Path="$(Join-Path $jdkRoot 'bin');$env:Path";$jvm=@("-Dorg.gradle.java.installations.paths=$jdkRoot",'-Dorg.gradle.java.installations.auto-detect=false','-Dorg.gradle.java.installations.auto-download=false');& .\gradlew.bat @jvm test runGameTest auditDirectDependencies build --offline --no-daemon --console=plain --init-script scripts/loom-resolution.init.gradle;$gradleExit=$LASTEXITCODE;if($gradleExit -ne 0){throw "Remote lifecycle gate failed with exit $gradleExit"};$testText=Get-Content -LiteralPath 'src/gametest/java/dev/developershell/gametest/RemoteGameTests.java' -Raw;if($testText -notmatch 'ModItems\.INFINITE_SLIDES_REMOTE'){throw 'Remote GameTest does not use the production registry item'}</automated>
  </verify>
  <done>The Remote remains visibly and authoritatively on cooldown across lifecycle, then becomes ready once without UI spam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Persisted deadline -> native overlay | Runtime visual state must derive from durable logical-server time. |
| Lifecycle/manager -> player action bar | A ready cue must not replace a critical boss instruction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-REMOTE-03 | Tampering | Lifecycle restore | medium | mitigate | Persisted deadline/notice, clamped server-time remainder, JOIN/respawn tests. |
| T-02-REMOTE-04 | Denial of Service | Ready feedback | low | mitigate | One persisted edge per deadline and manager priority arbitration. |
</threat_model>

## Artifacts this phase produces

- Lifecycle native-overlay restoration and boss-safe once-only ready feedback.
- `RemoteGameTests` covering production use/rejection, death/join, overlay, deferral, and ready edge.

<verification>
- Real server tests prove durable cooldown presentation without network/client/wall-clock authority.
</verification>

<success_criteria>
- The practical reward remains visibly and authoritatively on one server-owned cooldown across lifecycle.
</success_criteria>

<output>Create `.planning/phases/02-persistent-lecture-vertical-slice/02-18-SUMMARY.md` when done.</output>
