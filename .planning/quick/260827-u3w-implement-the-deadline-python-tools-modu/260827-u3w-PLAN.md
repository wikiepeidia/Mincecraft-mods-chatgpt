---
quick_id: 260827-u3w
type: execute
mode: deadline-junit-first
autonomous: true
status: planned
depends_on: [260827-s61]
requirements: [PYTH-01, PYTH-02]
files_modified:
  - src/main/java/dev/developershell/python/
  - src/main/java/dev/developershell/registry/ModItemIds.java
  - src/main/java/dev/developershell/registry/ModItems.java
  - src/main/java/dev/developershell/DevelopersHell.java
  - src/main/resources/assets/developers_hell/
  - src/test/java/dev/developershell/python/
  - README.md
must_haves:
  truths:
    - "The pip Wand simulates four finite local packages, charges bounded XP, and either applies one deterministic temporary effect or creates one visible Dependency Conflict."
    - "The venv Flask deterministically clears or isolates the simulated conflict without executing Python or touching files, processes, accounts, APIs, or the network."
    - "The Python Pickaxe mines only a whitelist-matching connected ore set within strict count, node, distance, loaded-area, dimension, and cooldown caps, then exposes a RecursionError cooldown."
    - "Pure Java domain logic has broad JUnit coverage and the existing campaign/boss-rush suites remain green."
  artifacts:
    - {path: "src/main/java/dev/developershell/python/PipEnvironment.java", provides: "deterministic fake-package compatibility, selection, XP, effect, conflict, and venv rules"}
    - {path: "src/main/java/dev/developershell/python/BoundedOreTraversal.java", provides: "pure capped deterministic connected-ore traversal"}
    - {path: "src/test/java/dev/developershell/python/", provides: "JUnit-heavy rules, traversal, cooldown, persistence, resource, and gate coverage"}
  key_links:
    - {from: "src/main/java/dev/developershell/registry/ModItems.java", to: "src/main/java/dev/developershell/python/", via: "thin server-authoritative custom item adapters"}
    - {from: "src/main/java/dev/developershell/DevelopersHell.java", to: "src/main/java/dev/developershell/python/PythonToolsRuntime.java", via: "module-gated callbacks and lifecycle"}
---

# Quick Task 260827-u3w — Deadline Python Tools

<objective>
Add the two Python comedy loops as a partially live but heavily unit-tested offline module. The deadline accepts pending manual/client UAT and prioritizes deterministic Java rules over expensive Minecraft integration coverage.

Never invoke Python, pip, a shell, a browser, a source file, an account, an API, or a network service. All “packages,” “venv,” exceptions, and recursion are finite in-game simulations.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the pure Java fake-package and bounded-recursion engines with extensive JUnit coverage</name>
  <files>src/main/java/dev/developershell/python/FakePackage.java, src/main/java/dev/developershell/python/PipEnvironment.java, src/main/java/dev/developershell/python/PipOutcome.java, src/main/java/dev/developershell/python/PythonToolsState.java, src/main/java/dev/developershell/python/PythonToolsSavedData.java, src/main/java/dev/developershell/python/BoundedOreTraversal.java, src/main/java/dev/developershell/python/RecursionCooldown.java, src/test/java/dev/developershell/python/</files>
  <action>
Create Minecraft-independent value types for four authored fake packages, their finite compatibility matrix, XP-level price, duration, effect descriptor, deterministic selection cycle, conflict result, venv clear/isolate result, cooldowns, and idempotent persisted state transitions. Define explicit bounds and validation: no negative XP/durations, no duplicate install authority, one conflict token state, and game-tick time only.

Implement a generic iterative BFS/queue traversal over an injected graph/block-view interface. It must use a stable six-direction order, visit each coordinate once, accept only the origin's compatible ore class/tag, and stop at configured maximum broken blocks, visited nodes, Chebyshev/Manhattan radius, unloaded boundary, world-height boundary, dimension boundary, or blocked/unbreakable target. Return an immutable ordered plan plus truncation/RecursionError reason; never recurse on the Java stack. A cooldown admission object must reject reentry and clamp persisted deadlines.

Write at least 30 new pure JUnit tests before/thoroughly alongside implementation: every package/effect/selection, every compatibility pair, exact XP boundaries, conflict/venv transitions, idempotence, codec/current/future/malformed state behavior where feasible, cooldown start/expiry/clamp, deterministic traversal order, loops/cycles, diagonals, mixed ores, max-count/node/radius/height/unloaded/dimension/unbreakable boundaries, duplicate nodes, empty origin, and seeded repeatability. Use parameterized tests where useful, but keep each behavioral case visible in XML receipts.
  </action>
  <verify>Pinned Java 25 offline `compileJava test`; all existing tests plus at least 30 new Python-domain tests pass with no skipped cases.</verify>
  <done>The dangerous-looking jokes are finite pure data/state machines with exhaustive boundary tests and no external execution surface.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add thin Fabric items/callbacks/resources, preserve regressions, and rebuild the distributable JAR</name>
  <files>src/main/java/dev/developershell/python/PythonToolsRuntime.java, src/main/java/dev/developershell/python/PipWandItem.java, src/main/java/dev/developershell/python/VenvFlaskItem.java, src/main/java/dev/developershell/python/PythonPickaxeItem.java, src/main/java/dev/developershell/registry/ModItemIds.java, src/main/java/dev/developershell/registry/ModItems.java, src/main/java/dev/developershell/DevelopersHell.java, src/main/resources/assets/developers_hell/lang/en_us.json, src/main/resources/assets/developers_hell/items/, src/main/resources/assets/developers_hell/models/item/, src/test/java/dev/developershell/python/, scripts/lecture-test-manifest.json, scripts/verify-lecture.ps1, README.md</files>
  <action>
Register stable vanilla-icon items `pip_wand`, `venv_flask`, `python_pickaxe`, and `dependency_conflict` (plus `recursion_error` only if it materially improves visible state without scope expansion). Keep registrations unconditional; gate behavior through the existing Python module gate.

Make pip Wand server use finite and understandable without a GUI: sneak-use cycles the selected fake package; ordinary use spends the configured XP levels and installs it, or emits exactly one bounded Dependency Conflict item/status when the compatibility table rejects it. Apply only existing bounded vanilla effects and translated chat/actionbar cues. Venv Flask use clears/isolate-conflict state, removes the visible token if present, and starts a short cooldown. Persist state before effects; repeated/rejected callbacks must not double-charge or double-grant.

Hook Python Pickaxe through the narrow supported Fabric block-break surface. Only activate for the registered pickaxe and enabled module, only on server, and guard callback reentry. Plan traversal first with the pure engine, then destroy at most the capped same-ore positions using normal server/player mining semantics; never force-load chunks or cross dimensions. Apply a visible translated RecursionError/cooldown on truncation and reject uses until server game time reaches the deadline. If a safe production mining hook becomes a compile-time blocker, ship the pure planner plus a `/devhell python demo` command adapter and document mining activation as partial rather than delaying the JAR.

Add resource/gate/adapter JUnit tests and one cheap integration/GameTest only if it can be completed without destabilizing the existing exact test harness. Update exact unit receipts, README commands/use/limitations/offline disclaimer, and leave manual visual/client UAT explicitly pending. Run pinned Java 25 offline `clean test runGameTest auditDirectDependencies build`; if the existing GameTest world is reused, clean it before interpreting failures. Replace `dist/developers-hell-0.1.0.jar`, recompute size/SHA-256, and do not claim unperformed live play.
  </action>
  <verify>Pinned Java 25 offline clean unit/GameTest/audit/build passes; unit receipt increases by at least 30 with zero failures/skips; JAR contains Python tool classes and item resources; no forbidden network/process/Python API appears in production bytecode/source.</verify>
  <done>A new checksum-recorded JAR contains the unit-proven Python comedy tools while manual gameplay remains honestly pending.</done>
</task>

</tasks>

<deferred>
- Custom GUI, textures, audio, real Python/package execution, arbitrary scripting, and broad live/client UAT.
- Other Phase-4 developer tools and all Phase-5 Metadata Roulette work.
- Any mining behavior that cannot be implemented safely through a bounded supported callback before the deadline.
</deferred>

<success_criteria>
- At least 30 new pure Java/JUnit behavioral cases pass without skips.
- pip/venv logic cannot charge or grant twice and has no external execution/network surface.
- Ore traversal is iterative, deterministic, whitelist-only, and bounded along every resource axis.
- Existing 106 unit/70 GameTest campaign baseline does not regress.
- A new verified Fabric 26.2 JAR and checksum are produced; manual UAT remains pending.
</success_criteria>
